---
phase: 03-app-search-result-model-app-catalog
plan: 05
type: execute
wave: 5
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/app/main.cpp
  - qml/MainWindow.qml
  - qml/ResultsRow.qml
  - CMakeLists.txt
  - tests/tst_model.cpp
autonomous: false
requirements: [LAUN-01, LAUN-04, LAUN-05]
must_haves:
  truths:
    - "Typing in the search field live-filters and ranks the catalog on every keystroke — first hotkey show stays under 100ms, typing stays instant (criterion 5; catalog is prebuilt by then)"
    - "Empty query shows the full installed app list, alphabetically, first row selected — Enter on it launches (D-01/D-02 vertical slice payoff)"
    - "↑/↓, PageUp/PageDown, Home/End navigate; hover selects; Enter and mouse click launch; Ctrl+Shift+Enter elevates (LAUN-05 + LAUN-04); Escape dismisses"
    - "Launching dismisses the launcher instantly (hideNow — no animation wait); a cancelled UAC leaves the launcher open and quiet (D-11/D-13)"
    - "Ctrl+Shift+Enter on a UWP app shows the transient hint 'Only desktop apps can run as administrator' — no launch attempt, no modal (D-11)"
    - "The results UI uses ONLY Theme.qml tokens — no literal colors/fonts/sizes (UI-SPEC hard rule); copy strings match the 01-UI-SPEC contract exactly"
  artifacts:
    - path: "src/app/main.cpp"
      provides: "Wiring: AppCatalog (real scanners) + ResultsModel context property + LaunchController | model | dismiss chain; catalog started OFF the hotkey path"
      contains: "AppCatalog"
    - path: "qml/MainWindow.qml"
      provides: "Search TextField + results ListView + empty states in the 640×400 shell; nav/hover/click key wiring"
      contains: "ListView"
    - path: "qml/ResultsRow.qml"
      provides: "44px row delegate: monogram, title, subtitle, selection accent/hover highlight — all Theme tokens"
      contains: "Theme."
  key_links:
    - from: "src/app/main.cpp"
      to: "src/core/AppCatalog.h"
      via: "scanner wiring + catalog.start() at startup (never in hotkey path)"
      pattern: "AppCatalog"
    - from: "src/app/main.cpp"
      to: "src/core/ResultsModel.h"
      via: "engine.rootContext()->setContextProperty(\"resultsModel\", &model)"
      pattern: "setContextProperty"
    - from: "src/app/main.cpp"
      to: "src/core/LaunchController.h"
      via: "setModel + setDismissHandler([&controller]{controller.hideNow();}) — D-13"
      pattern: "setDismissHandler"
    - from: "qml/MainWindow.qml"
      to: "src/core/ResultsModel.h"
      via: "TextField onTextChanged → resultsModel.setQuery; ListView delegate roles DisplayName/Subtitle/MatchRanges"
      pattern: "resultsModel"
    - from: "qml/MainWindow.qml"
      to: "src/core/LaunchController.h"
      via: "Keys Enter → launchController.launchSelected; Ctrl+Shift+Enter → launchSelected(true); delegate click → launchIndex(index)"
      pattern: "launchController"
---

<objective>
Wire the vertical slice: `main.cpp` builds the real catalog (Win enumerators) off the hotkey path and exposes ResultsModel + LaunchController to QML; `MainWindow.qml` gains the search TextField, the 44px results ListView with selection/hover, the empty/no-match states, and the full keyboard contract (LAUN-05) — ending in a human-verify checkpoint on the working slice.

**Purpose:** This is the payoff plan — every prior contract (AppEntry, matcher, model, enumerators, catalog, launch) becomes a usable app. The UI stays thin: all policy is C++ (controller/model); QML renders and forwards keys (PATTERNS §6). Theme-token discipline and the copy contract (RESEARCH §7) are enforced; the 01-UI-SPEC is the covering design contract (UI gate — approved).

**Output:** A working searchable launcher: hotkey → type → rank → launch (incl. elevated) → instant dismiss; passing tst_model additions + manual verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-app-search-result-model-app-catalog/03-CONTEXT.md
@.planning/phases/03-app-search-result-model-app-catalog/03-RESEARCH.md
@.planning/phases/03-app-search-result-model-app-catalog/03-PATTERNS.md
@.planning/phases/01-core-shell/01-UI-SPEC.md
@.planning/phases/02-global-hotkey-toggle/02-CONTEXT.md

<interfaces>
<!-- Consumed: 03-01 ResultsModel/AppEntry, 03-03 AppCatalog, 03-04 LaunchController -->
<!-- No new C++ interfaces created — QML consumption + main.cpp wiring only -->

From 03-03 AppCatalog.h (consumed):
```cpp
class AppCatalog : public QObject {
    Q_OBJECT
public:
    void setScanners(std::vector<Scanner> scanners);
    void start();             // worker build, off the hotkey path
    void ensureFresh();       // call on show (age check)
    QVector<AppEntry> entries() const;
signals: void refreshed();
};
```

From 03-04 LaunchController.h (consumed):
```cpp
class LaunchController : public QObject {
    Q_OBJECT
public:
    void setModel(ResultsModel *model);
    void setLauncher(Launcher fn);             // default = WinLaunch bridge
    void setDismissHandler(std::function<void()> fn);
    Q_INVOKABLE void launchSelected(bool elevated = false);
    Q_INVOKABLE void launchIndex(int index, bool elevated = false);
signals: void adminRequestRefused(const QString &displayName);
         void launchFailed(const QString &displayName);
};
```

From 03-01 ResultsModel.h (consumed): setEntries / setQuery / moveSelection / selectIndex / selectedIndex / snapshotSelected; Roles DisplayName, Subtitle, MatchRanges, Aumid (MatchRanges role: QVariantList of two-int lists {start,length} — Phase 5 renders).

Theme tokens (PATTERNS §4 — the ONLY visual values): surface, surfaceSecondary, border, accent #0078D4, accentLight, textPrimary, textSecondary, spaceXs 4, spaceSm 8, spaceMd 12, spaceLg 16, spaceXl 24, space2xl 32, rowHeight 44, radiusSurface 12, fontSizeQuery 18, fontSizeTitle 15, fontSizeSubtitle 12, fontSizeKeycap 12, fontWeightRegular 400, fontWeightSemibold 600, surfaceWidth 640, surfaceHeight 400.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: main.cpp wiring — catalog, model context property, launch chain</name>
  <files>
    src/app/main.cpp
    tests/tst_model.cpp
  </files>
  <behavior>
    - Test 1 (tst_model addition) MatchRanges shape contract: after setEntries + setQuery("cal"), data(index, MatchRangesRole) is a QVariantList whose items are each a 2-element QVariantList of ints {start, length} — the exact shape the QML delegate and Phase 5 consume. Assert type + values for a known match (e.g. "Calculator" query "cal" → [[0,3]]).
    - Test 2 (wiring discipline — automated read-back): after building, `ensureFresh` appears in main.cpp ONLY in the window-visibleChanged wiring and nowhere inside HotkeyManager signal handlers; `catalog.start()` called exactly once; both context properties set before loadFromModule (source-order check).
  </behavior>
  <action>
    Extend main.cpp (mirror existing wiring style — connect chains + lambdas; the ORDER-LOAD-BEARING tray sequence stays intact):

    1. Add includes: core/AppCatalog.h, core/ResultsModel.h, core/LaunchController.h, win/WinStartMenuEnumerator.h, win/WinUwpEnumerator.h.
    2. REORDER: ResultsModel + LaunchController + AppCatalog must be constructed BEFORE loadFromModule so the context properties exist for MainWindow.qml. Keep the `window` extraction after load; only the construction order changes (model first, then engine load, then window cast — the existing window-dependent code must come after loadFromModule; keep tray wiring in its current position relative to hotkeys.start()).
    3. `engine.rootContext()->setContextProperty("resultsModel", &resultsModel);` and `...("launchController", &launch);` — both BEFORE loadFromModule.
    4. `AppCatalog catalog;` with real scanners:
       `catalog.setScanners({ &WinStartMenuEnumerator::scanStartMenu, &WinUwpEnumerator::scanUwpApps });`
       (03-02 factories return QVector<AppEntry> — signatures match the Scanner std::function.)
    5. `launch.setModel(&resultsModel);`
    6. `launch.setDismissHandler([&controller]{ controller.hideNow(); });` — D-13 instant dismissal (LauncherController::hideNow, 02-02 contract).
    7. Catalog → model propagation (silent swap, D-08):
       `QObject::connect(&catalog, &AppCatalog::refreshed, &resultsModel, [&resultsModel, &catalog]{ resultsModel.setEntries(catalog.entries()); });`
       (setEntries resets the query to "" — acceptable: results clear only when the catalog actually rebuilt; the swap is silent per D-08.)
    8. Age check on show (D-08): `QObject::connect(window, &QQuickWindow::visibleChanged, &catalog, [window, &catalog]{ if (window->isVisible()) catalog.ensureFresh(); });` — UI-thread call AFTER the toggle; scanning happens on the worker.
    9. `catalog.start();` — ONCE, after the connects, before app.exec() (alongside hotkeys.start()). NEVER inside a hotkey handler.
    10. Leave HotkeyManager/TrayIcon/capture wiring untouched. The tray-less fallback path keeps working — catalog/model are independent of tray presence.

    tst_model addition per the behavior block (MatchRanges shape test) — extend the existing test file; do NOT break existing slots.

    Do NOT modify LauncherController, HotkeyManager, TrayIcon, or any src/win file in this plan.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File build.ps1; ctest --test-dir build/dev --output-on-failure 2>&1 | Select-String -Pattern "Totals|PASS|FAIL|tst_model|tst_matcher"; Write-Output "---WIRING-CHECK---"; Select-String -Path src\app\main.cpp -Pattern "ensureFresh|catalog.start|setContextProperty" | ForEach-Object { "$($_.LineNumber): $($_.Line.Trim())" }
  </automated>
  <done>wisp builds and runs; tst_model includes the new MatchRanges shape test (green); wiring-check shows ensureFresh ONLY in the visibleChanged lambda, catalog.start exactly once, both setContextProperty calls before loadFromModule; tray order untouched</done>
</task>

<task type="auto">
  <name>task 2: MainWindow.qml — search field, results list, states, keyboard contract</name>
  <files>
    qml/MainWindow.qml
    qml/ResultsRow.qml
  </files>
  <action>
    Create qml/ResultsRow.qml — the 44px row delegate per the 01-UI-SPEC Results List contract (roles from 03-01 ResultsModel; ALL visuals via Theme tokens — zero literals):
    - Row: implicitHeight Theme.rowHeight (44), full width; a 32px monogram circle (radius Theme.radiusSurface — clamp to a corner radius, e.g. Theme.radiusSurface) on the left showing the FIRST character of displayName (uppercased), fill Theme.surfaceSecondary, text Theme.textSecondary fontSizeSubtitle — NO icon pipeline this phase (CONTEXT discretion; icons = Phase 5).
    - Texts: title = displayName (fontSizeTitle 15, fontWeightRegular, Theme.textPrimary); subtitle = SubtitleRole when non-empty (fontSizeSubtitle 12, Theme.textSecondary) — elide right on both.
    - Selection visuals: ListView.isCurrentItem → background Theme.accent with white/near-white title (accent is #0078D4 — white title gives 4.7:1, use Theme.textPrimary); otherwise background transparent; hover (MouseArea hoverEnabled + containsMouse) and NOT current → Theme.surfaceSecondary ONLY (hover never accent — 01-UI-SPEC rule).
    - MouseArea anchors.fill: hoverEnabled; onEntered/onPositionChanged → if (!ListView.isCurrentItem) resultsModel.selectIndex(model.index) (D-02 hover selects); onClicked → launchController.launchIndex(model.index, false) (LAUN-05 click launches; freeze handled in C++ D-12).
    - Expose nothing else; delegate must not steal keys (no focus).

    Modify qml/MainWindow.qml:
    1. Inside the surface Rectangle (after the border), add a search TextField REGION (a Row/TextField + a thin separator): height Theme.rowHeight, anchors.top/left/right with Theme.spaceMd (16) margins — or full-width with padding; placeholderText EXACTLY "Type to search apps and files…" (RESEARCH §7 contract — verbatim, including the ellipsis character), font.pixelSize Theme.fontSizeQuery (18), color Theme.textPrimary, placeholderTextColor Theme.textSecondary; background: transparent (Rectangle with 1px bottom border Theme.border as the field underline), padding left Theme.spaceMd; onTextChanged: resultsModel.setQuery(text) (lean — the model handles empty/full list; no debounce needed at this scale and rankers are &lt;5ms per D-06).
    2. Add the results ListView BELOW the field, filling the rest of the surface (anchors: top = field bottom + Theme.spaceSm, left/right/bottom = surface edges with small padding); model: resultsModel; delegate: ResultsRow {}; clip: true; focus: false; keyNavigationEnabled: false (keys handled by shell — ListView must NOT swallow ↑/↓). **CRITICAL: bind currentIndex: resultsModel.selectedIndex** so the single source of selection truth is the model — ListView.isCurrentItem in the delegate then tracks it, and hover-select (selectIndex via MouseArea) and keyboard moves (moveSelection) both render through the same accent highlight. Do NOT set highlightFollowsCurrentItem/highlight-component — the delegate paints selection itself (item 1). Also set boundsBehavior: Flickable.StopAtBounds (no overscroll bounce in a launcher popup).
    3. Keyboard contract (LAUN-05) on the SHELL Item — extend the existing Keys block (already handles Escape via dismiss()):
       - Keys.onUpPressed / onDownPressed → resultsModel.moveSelection(-1 / +1); event.accepted = true.
       - Keys.onPageUpPressed / onPageDownPressed → resultsModel.moveSelection(-7 / +7) (kVisibleRows=7; the 400px shell shows ~7 rows of 44px); event.accepted = true.
       - Keys.onHomePressed → resultsModel.selectIndex(0); Keys.onEndPressed → resultsModel.selectIndex(rowCountViaView - 1) — get rowCount from the ListView's count property (resultsView.count - 1) since the model doesn't expose count; event.accepted = true.
       - Enter: single handler on the shell for Key_Return/Key_Enter checking event.modifiers — if (event.modifiers & Qt.ControlModifier) && (event.modifiers & Qt.ShiftModifier) → launchController.launchSelected(true) (Ctrl+Shift+Enter = elevated, LAUN-04); else → launchController.launchSelected(false); event.accepted = true.
       - CRITICAL QML focus reality: while the user types, the TextField owns focus — shell Keys fire only when focus leaves the field. Solution: EITHER set Keys.forwardTo: [shell] on the TextField OR put the nav handlers on the TextField itself. Choose: TextField Keys handler duplication is fragile; use Keys.forwardTo on the TextField pointing at the shell Item so nav/Enter/Escape all route through the shell block (document this in a comment). Verify on the live app (checkpoint): typing "calc" then pressing ↑ must move selection, not the text caret.
    4. Empty/no-match states (RESEARCH §7 contract — verbatim copy): an Item overlaid on the ListView (visible when resultsView.count == 0) showing:
       - empty query → heading EXACTLY "Recent apps will appear here" (D-01 normally hides this — full list shows; this renders only if the catalog is empty at startup); body EXACTLY "Press Esc to close".
       - non-empty query, no results → heading EXACTLY "No results for \"{query}\"" with the current query interpolated from the model's `query` property (03-01 exposes Q_PROPERTY QString query — READ ONLY, do NOT re-implement); body EXACTLY "Press Esc to close".
       Both centered, fontSizeTitle/textSecondary or per-UI-SPEC empty state (use font sizes/tokens only).
    5. Transient admin hint (D-11): a bottom-centered Text bound to `hintText` property on the window, shown when non-empty, EXACTLY "Only desktop apps can run as administrator" — set via Connections { target: launchController; function onAdminRequestRefused() { hintText = "Only desktop apps can run as administrator" } }, auto-cleared by a Timer (singleShot, ~2.5s) — non-modal, transient per D-11.
    6. Do NOT touch: the existing open/close animations, hideNow(), dismiss(), onClosing, scale transform, window flags, or resident lifecycle. Do NOT add icons (Phase 5), file search (Phase 4), recency (v2), Settings UI (Phase 6).
    7. WIRE module: add qml/ResultsRow.qml to the wisp_qml QML_FILES list in CMakeLists.txt (PATTERNS §3 rule — missing QML_FILES = silent runtime failure). The engine already imports the wisp module in MainWindow.qml; the delegate lives under the same URI.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File build.ps1 2>&1 | Select-String -Pattern "qmllint|error|warning" -NotMatch; Write-Output "---LITERAL-GATE---"; Select-String -Path qml\MainWindow.qml,qml\ResultsRow.qml -Pattern "#[0-9A-Fa-f]{6}|font.pixelSize|height: [0-9]|width: [0-9]" | Where-Object { $_.Line -notmatch "Theme\." } | ForEach-Object { "$($_.Filename):$($_.LineNumber): $($_.Line.Trim())" }
  </automated>
  <done>wisp builds clean; literal-gate shows NO non-Theme visual literals in MainWindow.qml/ResultsRow.qml (any output lines = violations to fix); ResultsRow.qml registered in QML_FILES; app still runs from 02 (radio smoke via checkpoint below)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: human-verify the vertical slice — search, nav, launch, elevation, feel</name>
  <files>
    src/app/main.cpp
    qml/MainWindow.qml
    qml/ResultsRow.qml
    build/dev/wisp.exe
  </files>
  <action>
    OpenCode has automated: full main.cpp wiring (catalog/model/launch), the search TextField, the ResultsRow delegate, the ListView, empty/no-match states, keyboard contract, and the transient admin hint — plus the complete C++ backend from plans 01-04 (matcher, model, catalog, enumerators, launch). Everything runs and the unit suites pass. This checkpoint pauses for verification only a human can do: actually pressing keys in the real desktop session (UAC prompts, Store apps, live Start Menu, animation feel). Before pausing, re-run the build + full ctest suite so the checkpoint starts from a known-green state.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File build.ps1; ctest --test-dir build/dev --output-on-failure 2>&1 | Select-String -Pattern "Totals"</automated>
  </verify>
  <what-built>
    OpenCode has automated: full main.cpp wiring (catalog/model/launch), the search TextField, the ResultsRow delegate, the ListView, empty/no-match states, keyboard contract, and the transient admin hint — plus the complete C++ backend from plans 01-04 (matcher, model, catalog, enumerators, launch). Everything runs and the unit suites pass. What remains is verification only a human can do: actually pressing keys in the real desktop session.
  </what-built>
  <how-to-verify>
    1. Build & run: `powershell -ExecutionPolicy Bypass -File build.ps1`, then run `build\dev\wisp.exe` (existing dev loop). If the app runs tray-less with no notification, launch from a terminal to see qWarning output.
    2. Press Alt+Space (default hotkey, D-02.5): the launcher appears centered, animation plays at 60fps feel, and the FIRST results are visible promptly — the catalog built on startup (worker thread) so the FIRST open should already list apps. If the first open shows "Recent apps will appear here", wait ~2-3s and reopen (catalog build finished).
    3. Search typing: type `calc` → "Calculator" ranks #1 (golden list); type `term` → "Terminal" near top; `note` → "Notepad" near top. Typing stays instant (no stutter per keystroke — criterion 5).
    4. Navigation: press ↓ / ↑ / PageDown / PageUp / End / Home → selection moves with clamped bounds; the accent highlight follows. While typing (caret in field), ↑/↓ still move selection (Keys.forwardTo working — NOT the caret).
    5. Launch: with "Calculator" selected, press Enter → Calculator LAUNCHES and the launcher disappears INSTANTLY (hideNow, D-13 — no close animation wait). Reopen: press Escape → animated close (D-08 path intact).
    6. Hover + click: move the mouse over a row → row highlights surfaceSecondary (never accent), selection follows; click a row → it launches.
    7. Elevation (LAUN-04): select a classic desktop app (e.g. Command Prompt / Notepad), press Ctrl+Shift+Enter → UAC prompt appears; click Cancel → launcher stays OPEN, NO error message (quiet UAC, D-11). Select it again, Ctrl+Shift+Enter, click Yes → app opens elevated (title bar check via Task Manager optional).
    8. UWP refusal (D-11): select a Store/UWP app (Calculator on Win11 is a Store app — if `calc` shows "Calculator" and it IS a Store package, use it), press Ctrl+Shift+Enter → the transient hint "Only desktop apps can run as administrator" appears near the bottom, no launch attempt, hint fades after ~2.5s.
    9. Empty state: clear the field → full alphabetical list returns, first row selected (D-01/D-02). Type gibberish (e.g. `zzzz`) → "No results for \"zzzz\"" + "Press Esc to close" appears.
    10. Feel (core value): open/close animations smooth; the whole open→type→launch cycle feels <1s; nothing sluggish.
  </how-to-verify>
  <done>Human confirms ALL 10 steps pass (or reports specific issues per step for a fix round) — the vertical slice is verified in the live session</done>
  <resume-signal>Type "approved" to finish the phase, or describe any issue you observed (which step + what you saw) for a fix round.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML inputs → C++ policy | Key presses, text edits, mouse clicks from the UI cross into ResultsModel/LaunchController calls |
| main.cpp wiring | Catalog worker, model, and launch controller are composed here — miswiring = logic on the hotkey path or launch without dismissal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | D | hotkey-path latency | mitigate | catalog.start() called ONCE in main (grep-gated), ensureFresh only in visibleChanged wiring; scanning is worker-threaded (03-03); first show needs no scan (D-08) |
| T-03-05-02 | I | QML literal violation | mitigate | literal-gate grep in Task 2 verify fails the build step on any non-Theme color/font/size; qmllint runs on build |
| T-03-05-03 | I | copy-contract drift | mitigate | Task 2 specifies EXACT strings ("Type to search apps and files…", "No results for \"{query}\"", etc.); checkpoint step 3/8/9 re-checks visually |
| T-03-05-04 | S | elevation key spoof | accept | Ctrl+Shift+Enter is app-local QML; QAbstractKeyEvent from the OS — no injection surface; UAC itself is the OS gate |
| T-03-05-05 | D | launch without dismissal | mitigate | dismissal is INSIDE the controller policy (03-04 Test 3: dismiss fires on success); QML never calls hideNow directly — no path to launch-and-linger |
| T-03-05-06 | E | stale model during rebuild | accept | 03-03 silent swap + setEntries resets query (documented); worst case a cleared result set during a 10-min refresh — coherent, not broken |
</threat_model>

<verification>
1. `powershell -ExecutionPolicy Bypass -File build.ps1` builds clean (C++ + QML; qmllint runs via Qt tooling)
2. `ctest --test-dir build/dev --output-on-failure` — full suite green: tst_shell, tst_hotkey, tst_launcher, tst_capture, tst_tray, tst_matcher, tst_model (incl. MatchRanges shape), tst_enum, tst_catalog, tst_launch
3. literal-gate: no non-Theme visual literals in the two QML files
4. wiring-check: ensureFresh only in visibleChanged, catalog.start once, context properties set before loadFromModule
5. Human checkpoint: steps 1-10 above pass in the live desktop session
</verification>

<success_criteria>
- [ ] main.cpp: catalog (real scanners) + ResultsModel + LaunchController wired; context properties pre-load; start() off hotkey path; ensureFresh on show
- [ ] MainWindow.qml: search field (exact placeholder), ListView + ResultsRow (44px, monogram, Theme-only), nav/Enter/Ctrl+Shift+Enter/Escape contract, empty/no-match states (exact copy), transient admin hint
- [ ] ResultsRow.qml registered in QML_FILES; no Theme-literal violations; all 5 prior suites + new suites green
- [ ] HUMAN-VERIFIED: golden list ranks, instant typing, ↑/↓/Page/Home/End nav, Enter launch + instant dismiss, hover+click, UAC cancel quiet, UWP refusal hint, gibberish state, smooth open/close feel — checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-search-result-model-app-catalog/03-05-SUMMARY.md`
</output>