---
phase: 06-tray-settings-autostart-packaging
plan: 06-03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [src/tray/TrayIcon.h, src/tray/TrayIcon.cpp, tests/tst_tray.cpp, src/ui/SettingsWindow.h, src/ui/SettingsWindow.cpp, CMakeLists.txt]
autonomous: true
requirements: [SYS-01, SYS-03]
must_haves:
  truths:
    - "Tray menu order is locked: Open wisp / Settings / Change hotkey… / separator / Quit (D-03); Settings emits a signal"
    - "Tray disc fill is live-bound to the accent — set via setAccent, repainted on change (UI-SPEC tray icon contract)"
    - "Settings window opens centered with 120ms fade, closes on Esc / click-away with ~150ms grace and the launcher exemption (D-02/D-04)"
    - "The settings hotkey row reopens the existing fullscreen HotkeyCaptureDialog; accepted sequence re-registers via HotkeyManager (D-03, success criterion 3)"
  artifacts:
    - path: "src/tray/TrayIcon.h"
      provides: "Settings action + accent-aware icon API"
      exports: ["settingsRequested", "setAccent"]
    - path: "src/tray/TrayIcon.cpp"
      provides: "Menu construction (4 items + separator) + accent disc repaint"
      contains: "Settings"
    - path: "src/ui/SettingsWindow.h"
      provides: "Settings QML host controller"
      exports: ["open", "close", "applyAccent", "toggleAutostart", "openHotkeyCapture", "commitCustomColor"]
    - path: "tests/tst_tray.cpp"
      provides: "Updated tray assertions (labels, settings signal, accent repaint)"
      contains: "Settings"
  key_links:
    - from: "src/tray/TrayIcon.cpp"
      to: "settingsRequested()"
      via: "QAction triggered"
      pattern: "settingsRequested"
    - from: "src/ui/SettingsWindow.cpp"
      to: "qrc:/qt/qml/wisp/SettingsWindow.qml"
      via: "QML engine load"
      pattern: "SettingsWindow.qml"
    - from: "src/ui/SettingsWindow.cpp"
      to: "HotkeyCaptureDialog::open"
      via: "hotkey row click (D-03)"
      pattern: "open"
---

<objective>
The interactive half of the resident surface: tray menu restructure (Settings action, locked order, accent-aware disc) and the SettingsWindow C++ controller (show/dismiss semantics, hotkey-capture handoff, store wiring).

Purpose: SYS-01 (tray with Open/Settings/Quit) and SYS-03 (settings window behavior: re-registration on capture, dismissal semantics, launcher pop-over exemption) are behavior contracts — this plan implements them against the 06-02 QML surface.
Output: TrayIcon changes + SettingsWindow.{h,cpp} + tst_tray update + CMakeLists additions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tray-settings-autostart-packaging/06-CONTEXT.md
@.planning/phases/06-tray-settings-autostart-packaging/06-UI-SPEC.md
@.planning/phases/06-tray-settings-autostart-packaging/06-RESEARCH.md
@.planning/phases/06-tray-settings-autostart-packaging/06-PATTERNS.md
@.planning/phases/06-tray-settings-autostart-packaging/06-01-SUMMARY.md (if exists — WinSingleInstance/AutostartManager APIs)
@.planning/phases/06-tray-settings-autostart-packaging/06-02-SUMMARY.md (if exists — QML surface state)

<interfaces>
From src/tray/TrayIcon.h (existing — current state):
```cpp
class TrayIcon : public QObject {
    Q_OBJECT
public:
    explicit TrayIcon(QObject *parent = nullptr);
    void show();
    QMenu *menu() const { return m_menu; }
    void notifyHotkeyConflict(const QString &combo);
signals:
    void openWisp();
    void changeHotkeyRequested();
    void quitRequested();
private:
    QPointer<QSystemTrayIcon> m_tray;
    QPointer<QMenu> m_menu;
};
```
Current menu construction (TrayIcon.cpp lines 47-56): addAction("Open wisp") → addAction("Change hotkey.") → addSeparator() → addAction("Quit").

From src/ui/HotkeyCaptureDialog.h (QML host analog — SettingsWindow mirrors this shape):
- `void open(const QString &currentSequence)`, signal `void accepted(const QString &sequence)`, static `bool validateSequence(const QString &)`, hosts `qrc:/qt/qml/wisp/HotkeyCaptureDialog.qml`.

From src/core/HotkeyManager.h (re-registration path): `void setHotkey(const QString &sequence)` (HOTK-01 re-register).
From src/core/SettingsStore.h: `Q_INVOKABLE void setAccent(const QColor &c)` + `QColor accent() const` (D-06 path).
From src/core/AutostartManager.h (06-01): `bool isEnabled() const`, `void setEnabled(bool)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: TrayIcon — Settings action + locked menu order + accent-aware disc</name>
  <files>src/tray/TrayIcon.h, src/tray/TrayIcon.cpp, tests/tst_tray.cpp</files>
  <read_first>
    - src/tray/TrayIcon.h and src/tray/TrayIcon.cpp (full — menu construction, disc painting, signals)
    - tests/tst_tray.cpp (full — existing label/signal assertions that must be updated)
  </read_first>
  <action>
    Modify `src/tray/TrayIcon.h`:
    - Add signal `void settingsRequested();`
    - Add public `void setAccent(const QColor &accent);` (stores + repaints the disc; called by main.cpp on startup and on SettingsStore::accentChanged — TrayIcon NEVER reaches into SettingsStore, PATTERNS anti-pattern 1)
    - Keep `openWisp`, `changeHotkeyRequested`, `quitRequested` signals unchanged (Phase-2 contracts, tests depend on them).
    Modify `src/tray/TrayIcon.cpp`:
    - Menu order (LOCKED, D-03 + UI-SPEC tray menu contract): "Open wisp" → **"Settings"** → "Change hotkey…" (KEEP THE EXACT EXISTING LABEL TEXT — copy the string verbatim from the current source; it is "Change hotkey." in code and must not change) → separator → "Quit". Insert the Settings action between Open wisp and Change hotkey, connected to `&TrayIcon::settingsRequested`.
    - Accent-aware disc: change the disc fill from the hardcoded `#0078D4` to the stored accent color (`m_accent`, default #0078D4); the white bold "w" stays white at all accents (UI-SPEC tray icon contract). Repaint the pixmap on setAccent and re-`setIcon` on the tray. Keep the generated 16px QPainter disc design (no asset, no redesign).
    Update `tests/tst_tray.cpp`:
    - Expected menu labels list becomes [Open wisp, Settings, <existing change-label>, separator, Quit] (mirror the current assertion structure; 5 entries incl. separator).
    - Add assertions: Settings action exists between Open wisp and Change hotkey; triggering it emits `settingsRequested` (QSignalSpy).
    - Add accent test: construct TrayIcon, call setAccent(QColor("#2EA043")), assert the tray icon is non-null and menu structure intact (disc pixel check optional — assert no crash + icon set).
  </action>
  <verify>
    <automated>cmake --build build/dev --target tst_tray && ctest --test-dir build/dev -R tst_tray --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/tray/TrayIcon.h` contains `settingsRequested` and `setAccent` (grep -c both >= 1)
    - `src/tray/TrayIcon.cpp` menu construction order: `Open wisp` before `Settings` before the change-hotkey label before `Quit` (grep -n the addAction lines, verify order; grep -c "Settings" >= 1)
    - `src/tray/TrayIcon.cpp` no longer hardcodes `#0078D4` as the fill — disc fill reads from the accent member (grep -c "m_accent" >= 1); the fallback default may retain the hex literal
    - `tests/tst_tray.cpp` asserts the 5-entry menu (grep -c "Settings" >= 1 and existing label assertions updated)
    - `ctest -R tst_tray` passes (exit 0)
  </acceptance_criteria>
  <done>Tray menu locked (Open wisp / Settings / Change hotkey… / sep / Quit); Settings signal wired; disc accent-aware; tests green.</done>
</task>

<task type="auto">
  <name>task 2: SettingsWindow controller — QML host with dismissal semantics + capture handoff</name>
  <files>src/ui/SettingsWindow.h, src/ui/SettingsWindow.cpp, CMakeLists.txt</files>
  <read_first>
    - src/ui/HotkeyCaptureDialog.h and src/ui/HotkeyCaptureDialog.cpp (full — QML host pattern: engine load, Q_INVOKABLE API, signals)
    - qml/SettingsWindow.qml (06-02 — the QML surface this controller hosts; align property/signal names)
    - qml/ColorDialog.qml (06-02 — commitCustomColor wiring target)
  </read_first>
  <action>
    Create `src/ui/SettingsWindow.h` — QML host controller (HotkeyCaptureDialog analog, RESEARCH Pattern 3), Q_OBJECT. Per D-01: the settings surface is a dedicated QML window backed by this small C++ controller — NOT an extension of HotkeyCaptureDialog, NOT Qt Widgets.
    - `explicit SettingsWindow(QObject *parent = nullptr);` — owns the QML engine/view for `qrc:/qt/qml/wisp/SettingsWindow.qml`; exposes itself to QML as a context property (e.g. `settingsController`) per the 06-02 QML contract.
    - `void open();` — center on primary screen `availableGeometry()` (same centerOnScreen logic as MainWindow, re-applied EVERY show, UI-SPEC Geometry contract), fade-in 120ms (`Theme.animFade` — opacity 0→1 on the surface), `requestActivate()`, refresh `autostartEnabled` state from AutostartManager (D-10: state read when settings opens) and `currentHotkey` from HotkeyManager, then show the window. First focus on the hotkey row (UI-SPEC Interaction Contract).
    - `void close();` — instant hide (no close animation, UI-SPEC hard rule 2); window is hidden, never destroyed (state persists across opens).
    - Dismissal semantics (D-02): Esc → close() (QML Keys handler or window-level); click-away → deactivation-hide with ~150ms grace — on `QGuiApplication::applicationStateChanged`/focus-out, start a 150ms QTimer; if the newly-active window belongs to OUR process (compare `QGuiApplication::focusWindow()` against our window or the launcher window — UI-SPEC: "activating the launcher window must NOT close settings", RESEARCH Pitfall 4), cancel the timer. Otherwise hide on timeout.
    - Q_INVOKABLE API (consumed by SettingsWindow.qml + ColorDialog.qml per 06-02): `QString currentHotkey() const;` (from HotkeyManager), `void applyAccent(const QColor &c);` (→ SettingsStore::setAccent, D-06), `void commitCustomColor(const QString &hex);` (parse → setAccent), `void toggleAutostart();` (→ AutostartManager::setEnabled(!isEnabled()), then refresh state), `bool autostartEnabled() const;`, `void openHotkeyCapture();` — opens the EXISTING fullscreen HotkeyCaptureDialog via `open(currentHotkey())`; on its `accepted(sequence)` → `HotkeyManager::setHotkey(sequence)` (immediate re-registration, success criterion 3) + refresh `currentHotkey` (D-03: validation F12/mod-only + conflict balloon stay exactly as shipped).
    - Signals: `void settingsVisibleChanged(bool visible);` (optional, if QML needs it).
    Add to CMakeLists.txt: `src/ui/SettingsWindow.cpp` to wisp_core sources (next to `src/ui/HotkeyCaptureDialog.cpp`, line 35). No new test target — behavior covered by regression (task 3) + 06-04 wiring smoke + manual dismissal checkpoint.
  </action>
  <verify>
    <automated>cmake --build build/dev --target wisp && ctest --test-dir build/dev -R "tst_capture|tst_hotkey|tst_settings" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/SettingsWindow.h` contains `open`, `close`, `applyAccent`, `toggleAutostart`, `openHotkeyCapture`, `commitCustomColor` (grep -c each >= 1)
    - `src/ui/SettingsWindow.cpp` loads `SettingsWindow.qml` (grep -c "SettingsWindow.qml" >= 1)
    - `src/ui/SettingsWindow.cpp` contains the 150ms grace timer + launcher-exemption check (grep -c "150\|focusWindow" >= 1)
    - `src/ui/SettingsWindow.cpp` contains `HotkeyManager::setHotkey` path via `openHotkeyCapture` (grep -c "setHotkey" >= 1)
    - `src/ui/SettingsWindow.cpp` calls `AutostartManager` for toggle + state refresh (grep -c "AutostartManager\|toggleAutostart" >= 1)
    - CMakeLists.txt wisp_core list contains `src/ui/SettingsWindow.cpp` (grep -c "SettingsWindow.cpp" >= 1)
    - Regression tests pass (tst_capture/tst_hotkey/tst_settings)
  </acceptance_criteria>
  <done>Settings controller hosts the QML window with full dismissal semantics, capture handoff + immediate re-registration, accent/autostart wiring.</done>
</task>

<task type="auto">
  <name>task 3: Wave gate — full build + complete suite green</name>
  <files>CMakeLists.txt</files>
  <read_first>
    - CMakeLists.txt (confirm wisp_core list coherent, no duplicate sources)
  </read_first>
  <action>
    Run the full wave gate: `cmake --preset dev && cmake --build build/dev && ctest --test-dir build/dev --output-on-failure`. Fix any CMake/target wiring issues from tasks 1-2. Do not modify other files.
  </action>
  <verify>
    <automated>cmake --preset dev && cmake --build build/dev && ctest --test-dir build/dev --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Full build succeeds (exit 0)
    - Full ctest suite: 0 failures, >= 19 tests (17 existing + tst_singleinstance + tst_autostart)
  </acceptance_criteria>
  <done>Wave 3 complete: 19/19 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tray menu → app actions | QMenu actions trigger in-process signals; no external input |
| QML → controller → stores | Settings QML invokes Q_INVOKABLE methods; accent/hotkey/autostart values flow to stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | (none — local only) | TrayIcon / SettingsWindow controller | accept | All inputs are in-process QML clicks; accent bounded to swatch list or dialog HSV; hotkey path reuses validated validateSequence (F12/mod-only rejected); no new trust boundary |
| T-06-01 | Tampering | commitCustomColor hex parse | mitigate | QColor::setNamedColor on the hex string; invalid → ignored (D-16 silent discipline, Phase-5 setAccent validation) |
</threat_model>

<verification>
- `ctest -R tst_tray` green (menu labels, settings signal, accent setter)
- Regression: tst_capture/tst_hotkey/tst_settings green (capture path untouched)
- Full suite green (>= 19 tests)
</verification>

<success_criteria>
- Tray: locked menu order with Settings action emitting settingsRequested; disc accent-aware via setAccent (no store reach-in)
- SettingsWindow controller: open/close/dismissal (Esc, click-away 150ms grace, launcher exemption), hotkey capture handoff with immediate setHotkey re-registration, accent apply + custom commit, autostart toggle with open-time state refresh
- CMakeLists coherent; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-tray-settings-autostart-packaging/06-03-SUMMARY.md`
</output>
