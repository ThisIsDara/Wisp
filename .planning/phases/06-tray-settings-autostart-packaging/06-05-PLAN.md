---
phase: 06-tray-settings-autostart-packaging
plan: 06-05
type: execute
wave: 5
depends_on: [06-04]
files_modified: [packaging/installer.nsi, packaging/build-installer.ps1, packaging/verify-lgpl.ps1, packaging/relink-test/main.cpp, packaging/LGPL-COMPLIANCE.md, packaging/VM-RUNBOOK.md, packaging/THIRD-PARTY-NOTICES.txt]
autonomous: false
requirements: [SYS-04]
user_setup:
  - service: "Clean Windows VMs"
    why: "D-16 requires manual install verification on pristine Win10 22H2 and Win11 24H2 machines (no dev tools, no Qt). Cannot be automated."
    manual_steps:
      - "Provide or snapshot a Win10 22H2 VM and a Win11 24H2 VM"
      - "Run the steps in packaging/VM-RUNBOOK.md on each"
must_haves:
  truths:
    - "Installer builds to a per-user NSIS setup (RequestExecutionLevel user, %LOCALAPPDATA%\\Programs\\wisp, Start Menu shortcut) — no UAC (D-13)"
    - "Installer runs on clean Win10 22H2 + Win11 24H2 VMs: install → launch → hotkey → launch app → search file (D-16 runbook)"
    - "LGPL compliance verified mechanically: THIRD-PARTY-NOTICES.txt ships in the installer, Qt is dynamically linked (relink test passes), source offer documented (D-15)"
  artifacts:
    - path: "packaging/installer.nsi"
      provides: "Per-user NSIS script (D-13/D-14)"
      contains: "RequestExecutionLevel user"
    - path: "packaging/build-installer.ps1"
      provides: "deploy output → VC_redist download → makensis pipeline"
      contains: "makensis"
    - path: "packaging/verify-lgpl.ps1"
      provides: "dumpbin /DEPENDENTS + relink test runner"
      contains: "dumpbin"
    - path: "packaging/LGPL-COMPLIANCE.md"
      provides: "Compliance evidence doc + source offer"
      contains: "relink"
    - path: "packaging/VM-RUNBOOK.md"
      provides: "Clean-VM manual checklist (D-16)"
      contains: "Win10 22H2"
  key_links:
    - from: "packaging/build-installer.ps1"
      to: "makensis"
      via: "Exec"
      pattern: "makensis"
    - from: "packaging/installer.nsi"
      to: "deploy output folder (build/deploy/wisp)"
      via: "File /r"
      pattern: "build/deploy"
    - from: "packaging/verify-lgpl.ps1"
      to: "dumpbin.exe (VS2022)"
      via: "direct exe invocation"
      pattern: "dumpbin"
---

<objective>
The release gate: per-user NSIS installer (D-13), VC_redist handling (D-14), LGPL compliance evidence with relink test (D-15), and the clean-VM manual runbook (D-16).

Purpose: SYS-04 is the phase's raison d'être — a clean-machine installer with provable LGPL compliance. Everything else in the phase feeds this plan's inputs (release build + deploy output).
Output: packaging/ pipeline (installer.nsi, build-installer.ps1, verify-lgpl.ps1, relink-test/main.cpp, LGPL-COMPLIANCE.md, VM-RUNBOOK.md, THIRD-PARTY-NOTICES.txt update) + human-verified clean-VM runs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tray-settings-autostart-packaging/06-CONTEXT.md
@.planning/phases/06-tray-settings-autostart-packaging/06-RESEARCH.md
@.planning/phases/06-tray-settings-autostart-packaging/06-PATTERNS.md
@.planning/phases/06-tray-settings-autostart-packaging/06-04-SUMMARY.md (if exists — release build state)

<interfaces>
From deploy.ps1 (existing — the installer's payload producer):
- `param([string]$Config = "release")`; builds `build/{Config}/wisp.exe` → `build/deploy/wisp/` via windeployqt `--release --qmldir qml --no-translations`; copies `packaging/THIRD-PARTY-NOTICES.txt` into the deploy folder; writes `qt.conf` (relocatable prefix).
- Final line: "VC runtime: install VC_redist.x64.exe on target machines (Phase 6) — do not copy dev-machine msvcp/vcruntime DLLs."

From 06-RESEARCH.md verified facts:
- NSIS 3.12 installable via `winget install NSIS.NSIS` (winget v1.29.280 present).
- dumpbin at `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\dumpbin.exe`.
- VC runtime registry check: `HKLM\SOFTWARE\Microsoft\VisualStudio\14.0\VC\Runtimes\x64` → `Installed` DWORD=1 (confirmed present on dev machine 14.51.36247).
- NSIS per-user pattern (verified, nsis.sourceforge.io/Examples/install-per-user.nsi): RequestExecutionLevel user + SetShellVarContext current (install AND uninstall .onInit) + GetKnownFolderPath FOLDERID_UserProgramFiles (fallback $LocalAppData\Programs) + HKCU uninstall keys + CreateShortcut "$SMPrograms\wisp.lnk".
- UI-SPEC locked installer copy: AppName "wisp"; installer window title "wisp — app launcher"; welcome "This will install wisp — the app launcher for Windows."; finish "wisp has been installed. Use the wisp tray icon to open the launcher anytime."; Start Menu shortcut "wisp".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Installer pipeline — NSIS install, installer.nsi, build-installer.ps1, build gate</name>
  <files>packaging/installer.nsi, packaging/build-installer.ps1</files>
  <read_first>
    - deploy.ps1 (full — output folder contract build/deploy/wisp)
    - .planning/phases/06-tray-settings-autostart-packaging/06-RESEARCH.md (NSIS per-user pattern + copy contract)
  </read_first>
  <action>
    Install NSIS via winget (automatable — RESEARCH Environment Availability): `winget install NSIS.NSIS --silent --accept-package-agreements --accept-source-agreements` (if already present, skip). Verify `makensis` resolves (typical path `$env:LOCALAPPDATA\Programs\NSIS\makensis.exe` — locate via winget install location or `Get-Command makensis`; record the path in the SUMMARY).
    Create `packaging/installer.nsi` implementing the VERIFIED per-user pattern + LOCKED copy:
    - `Name "wisp"`, `OutFile "..\build\deploy\wisp-setup.exe"` (path relative to the .nsi's packaging/ dir — deterministic output regardless of makensis cwd), `RequestExecutionLevel user` (CRITICAL — no UAC, RESEARCH Pitfall 2), `InstallDir "$LocalAppData\Programs\wisp"`.
    - `.onInit` (install) AND `un.onInit`: `SetShellVarContext Current` (per-user shell folders).
    - Install section: `SetOutPath "$InstDir"`; `File /r "..\build\deploy\wisp\*"` (windeployqt output — wisp.exe, Qt DLLs, plugins, qml, qt.conf, THIRD-PARTY-NOTICES.txt); `WriteUninstaller "$InstDir\Uninst.exe"`; HKCU uninstall registry keys (DisplayName "wisp", UninstallString, NoModify/NoRepair — per install-per-user.nsi example); **VC_redist gate (D-14)**: `ReadRegDWORD $0 HKLM "SOFTWARE\Microsoft\VisualStudio\14.0\VC\Runtimes\x64" "Installed"`; if != 1 → `ExecWait '"$InstDir\vc_redist.x64.exe" /install /quiet /norestart'` (VC_redist.x64.exe must be present in the deploy folder — build-installer.ps1 downloads it there; RESEARCH Open Question 1 resolution: build-time download, installer carries it, executes only when registry check fails).
    - Start Menu section: `CreateShortcut /NoWorkingDir "$SMPrograms\wisp.lnk" "$InstDir\wisp.exe"`.
    - Uninstall section: delete files/dirs + HKCU uninstall key + `Delete "$SMPrograms\wisp.lnk"`.
    - Page copy (UI-SPEC locked): window title "wisp — app launcher"; welcome line "This will install wisp — the app launcher for Windows."; finish line "wisp has been installed. Use the wisp tray icon to open the launcher anytime." (MUI2 welcome/finish text).
    Create `packaging/build-installer.ps1`:
    - `$ErrorActionPreference = "Stop"`; param `[string]$Config = "release"`.
    - Step 1: run `.\deploy.ps1 -Config $Config` (produce build/deploy/wisp).
    - Step 2: download VC_redist: `Invoke-WebRequest "https://aka.ms/vs/17/release/vc_redist.x64.exe" -OutFile "build\deploy\wisp\vc_redist.x64.exe"` (official URL only — T-06-03 mitigation; verify file is non-trivial size >1MB as sanity).
    - Step 3: locate makensis (winget location or Get-Command) and run `& $makensis "packaging\installer.nsi"` from the repo root (so the relative `..\build\deploy\wisp` path in the .nsi resolves — or pass /D path override if cleaner); output installer to `build\deploy\wisp-setup.exe` (OutFile path in .nsi).
    - Echo resulting installer path + size. Exit non-zero on any failure.
    Run the build gate: `.\packaging\build-installer.ps1` → exit 0 + `build\deploy\wisp-setup.exe` exists.
  </action>
  <verify>
    <automated>.\packaging\build-installer.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Test-Path "build\deploy\wisp-setup.exe"</automated>
  </verify>
  <acceptance_criteria>
    - `packaging/installer.nsi` contains `RequestExecutionLevel user` (grep -c >= 1) and does NOT request admin (`RequestExecutionLevel admin` absent — grep -c "RequestExecutionLevel admin" == 0)
    - `packaging/installer.nsi` contains `SetShellVarContext Current` (grep -c >= 1), `$LocalAppData\Programs\wisp` install dir (grep -c "Programs\\wisp" >= 1), HKCU uninstall keys (grep -c "Uninstall\\wisp" >= 1), `CreateShortcut` for wisp.lnk (grep -c "wisp.lnk" >= 1), the VC_redist registry gate (grep -c "VC\\Runtimes\\x64" >= 1)
    - `packaging/installer.nsi` contains the locked copy: "wisp — app launcher", "This will install wisp", "Use the wisp tray icon" (grep -c each >= 1)
    - `packaging/build-installer.ps1` contains `deploy.ps1` invocation, `aka.ms/vs/17/release/vc_redist.x64.exe` download, and `makensis` invocation (grep -c each >= 1)
    - `build-installer.ps1` exits 0 and `build\deploy\wisp-setup.exe` exists
  </acceptance_criteria>
  <done>Installer pipeline green: per-user NSIS setup builds from deploy output with VC_redist gate.</done>
</task>

<task type="auto">
  <name>task 2: LGPL compliance evidence — relink test + verify-lgpl.ps1 + docs</name>
  <files>packaging/relink-test/main.cpp, packaging/verify-lgpl.ps1, packaging/LGPL-COMPLIANCE.md, packaging/THIRD-PARTY-NOTICES.txt</files>
  <read_first>
    - packaging/THIRD-PARTY-NOTICES.txt (current stub — extend, don't destroy)
    - .planning/research/STACK.md (LGPL obligations section — dynamic linking carve-out, source offer wording)
    - 06-RESEARCH.md (D-15 relink test design, dumpbin path)
  </read_first>
  <action>
    Create `packaging/relink-test/main.cpp` — minimal console binary proving dynamic linkage (D-15): QCoreApplication-based; prints `QLibraryInfo::location(QLibraryInfo::LibrariesPath)` and a "RELINK OK" line; `return 0`. Link against `Qt6Core.lib` from the Qt install (import lib — dynamic-link import, NOT static lib).
    Create `packaging/verify-lgpl.ps1`:
    - `$ErrorActionPreference = "Stop"`; dumpbin path: `Get-ChildItem "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC" -Directory | ForEach-Object { Join-Path $_.FullName "bin\Hostx64\x64\dumpbin.exe" } | Where-Object { Test-Path $_ } | Select-Object -First 1` (verified path pattern from RESEARCH).
    - Check 1 — dynamic linkage: `& $dumpbin /DEPENDENTS "build\deploy\wisp\wisp.exe"` and assert output contains Qt6 entries (Qt6Core.dll / Qt6Gui.dll / Qt6Qml.dll / Qt6Quick.dll) — grep the output.
    - Check 2 — relink test: compile relink-test against the DEPLOYED folder: `cl /EHsc packaging\relink-test\main.cpp /I C:\Qt\6.11.1\msvc2022_64\include /link C:\Qt\6.11.1\msvc2022_64\lib\Qt6Core.lib` (cl from VS2022 dev environment — resolve via vcvars64.bat or the MSVC bin path; fallback: use cmake with a tiny target). Then run the binary with `$env:PATH = "build\deploy\wisp"` ONLY (no C:\Qt in PATH — RESEARCH Pitfall 5: must resolve the DEPLOYED Qt6Core.dll, not the dev Qt), assert exit 0 and output contains "RELINK OK". Print the resolved LibrariesPath as evidence.
    - Exit 0 only if both checks pass; print PASS/FAIL lines.
    Create `packaging/LGPL-COMPLIANCE.md`: compliance evidence doc (D-15): how to run verify-lgpl.ps1; what the relink test proves (deployed Qt6Core.dll is a usable dynamic dependency — Qt is dynamically linked, not statically embedded); the source offer statement (Qt open-source source: https://code.qt.io / qt.io download page; offer to provide source on request, 3-year validity per LGPL); THIRD-PARTY-NOTICES.txt shipping location (deploy folder + installer).
    Update `packaging/THIRD-PARTY-NOTICES.txt`: add the Qt LGPLv3 notice (copyright + license reference) and the source-offer paragraph (URL + offer wording).
    Run the verification: `.\packaging\verify-lgpl.ps1` → exit 0.
  </action>
  <verify>
    <automated>.\packaging\verify-lgpl.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - `packaging/verify-lgpl.ps1` exists with `dumpbin` resolution + `/DEPENDENTS` invocation (grep -c "dumpbin" >= 2) and the relink run with deploy-only PATH (grep -c "RELINK OK" >= 1)
    - `packaging/relink-test/main.cpp` links Qt6Core dynamically (import lib, no QTPLUGIN/static flags) and prints "RELINK OK" (grep -c "RELINK OK" >= 1)
    - `packaging/LGPL-COMPLIANCE.md` documents: relink test procedure, source offer (grep -c "source offer\|source code" >= 1), dynamic-link evidence (grep -c "dynamic" >= 1)
    - `packaging/THIRD-PARTY-NOTICES.txt` contains LGPL + source offer (grep -c "LGPL\|source" >= 2)
    - `verify-lgpl.ps1` exits 0 (both checks pass)
  </acceptance_criteria>
  <done>LGPL evidence produced: dumpbin shows Qt6 imports, relink binary runs against deployed DLLs, compliance doc + NOTICES + source offer in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: Clean-VM validation runbook + human verification on Win10 22H2 / Win11 24H2</name>
  <files>packaging/VM-RUNBOOK.md</files>
  <action>
    Write `packaging/VM-RUNBOOK.md` FIRST (automated part of this task — the runbook must exist before the checkpoint, per D-16):
    - Header: purpose (D-16 clean-VM validation), preconditions (Win10 22H2 VM + Win11 24H2 VM snapshots, no dev tools, no Qt, no VC runtime).
    - Steps (per VM, numbered, with expected outcomes): 1) copy wisp-setup.exe to the VM; 2) run installer → completes WITHOUT UAC prompt, installs to %LOCALAPPDATA%\Programs\wisp, Start Menu shortcut "wisp" appears; 3) launch wisp → tray icon present; 4) press hotkey (Alt+Space default) → launcher opens; 5) type + Enter a known app → launches; 6) search a file → result appears; 7) hotkey-conflict re-verification: register Alt+Space in another launcher (e.g. PowerToys Run), try to capture in wisp → tray balloon "wisp - hotkey in use" + red labels; 8) verify THIRD-PARTY-NOTICES.txt exists next to wisp.exe in the install folder.
    - Failure notes section: what to record if a step fails (screenshot, error text).
    Then CHECKPOINT: the user runs the runbook on the two VMs and reports results.
  </action>
  <what-built>
    packaging/VM-RUNBOOK.md (D-16 runbook) + the installer (wisp-setup.exe) + LGPL compliance evidence, ready for human clean-machine verification.
  </what-built>
  <how-to-verify>
    Automated portion: `Test-Path packaging/VM-RUNBOOK.md` and it contains all 8 numbered steps with expected outcomes + a failure-notes section.
    Then human verification: user executes the runbook steps on Win10 22H2 and Win11 24H2 VMs.
  </how-to-verify>
  <verify>
    <automated>if (-not (Test-Path "packaging/VM-RUNBOOK.md")) { exit 1 }; $s = Get-Content "packaging/VM-RUNBOOK.md" -Raw; foreach ($need in @("Win10", "Win11", "wisp-setup.exe", "Start Menu", "hotkey in use")) { if (-not $s.Contains($need)) { exit 1 } }</automated>
  </verify>
  <done>VM-RUNBOOK.md exists with all 8 steps + failure notes; user reports per-VM results (approved or step failures with details).</done>
  <resume-signal>Report per-VM results: "approved" (all steps pass on both VMs), or describe which VM/step failed with details for a fix plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → clean machine | NSIS script runs on unknown machines; installs files + registry keys (HKCU only) + possibly executes VC_redist |
| build pipeline → internet | build-installer.ps1 downloads VC_redist from the web |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | VC_redist download / execution | mitigate | Download ONLY from official `aka.ms/vs/17/release/vc_redist.x64.exe` (Microsoft-controlled redirect, RESEARCH A3); sanity size check (>1MB); the redist is Microsoft-signed; executed only when registry check fails |
| T-06-08 | Elevation | Installer privilege creep | mitigate | `RequestExecutionLevel user` — installer cannot elevate (D-13); HKCU-only writes; no HKLM, no Program Files |
| T-06-09 | Tampering | Installer payload integrity | accept | windeployqt output built from the repo release build; per-user install limits blast radius; code signing deferred to v2 (CONTEXT Deferred Ideas) |
</threat_model>

<verification>
- `.\packaging\build-installer.ps1` → wisp-setup.exe exists
- `.\packaging\verify-lgpl.ps1` → dumpbin Qt6 imports + relink binary RELINK OK
- VM-RUNBOOK.md exists with the 8-step checklist for both VMs
- Human checkpoint: runbook passes on Win10 22H2 + Win11 24H2
</verification>

<success_criteria>
- Per-user NSIS installer builds; no UAC; installs to %LOCALAPPDATA%\Programs\wisp with Start Menu shortcut
- VC_redist installed on clean machines via registry-gated silent exec
- LGPL evidence mechanical (dumpbin + relink test) + documented (LGPL-COMPLIANCE.md, NOTICES, source offer)
- Human-verified clean-VM runs on Win10 22H2 and Win11 24H2 per VM-RUNBOOK.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-tray-settings-autostart-packaging/06-05-SUMMARY.md`
</output>
