---
phase: 06-tray-settings-autostart-packaging
plan: 06-02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [qml/Theme.qml, qml/SettingsWindow.qml, qml/ColorDialog.qml, qml/HotkeyCaptureDialog.qml, CMakeLists.txt]
autonomous: true
requirements: [SYS-03, VISU-03]
must_haves:
  truths:
    - "Settings window opens as a 480x360 dark tool window with Hotkey / Accent color / Start with Windows rows (UI-SPEC geometry locked; D-01: dedicated QML window, not an extension of HotkeyCaptureDialog)"
    - "Clicking a swatch applies the accent live with no Save button (D-06: live apply); the picker is a curated swatch row + custom entry, not a palette dump (D-05:); 'Custom...' opens the staged dark color dialog (D-07:)"
    - "All colors/spacings come from Theme tokens — the 9 swatch values live in Theme.qml as tokens (D-08:); zero literals in new QML; the shipped HotkeyCaptureDialog literal debt is tokenized (UI-SPEC line 17)"
  artifacts:
    - path: "qml/Theme.qml"
      provides: "Extended token singleton: 33+ new tokens + accentSwatches 9-array + danger"
      contains: "accentSwatches"
    - path: "qml/SettingsWindow.qml"
      provides: "Settings window surface (448x328 inside 480x360)"
      contains: "Settings"
    - path: "qml/ColorDialog.qml"
      provides: "Custom color dialog (248x288 inside 280x320, ApplicationModal)"
      contains: "Custom color"
    - path: "qml/HotkeyCaptureDialog.qml"
      provides: "Tokenized literals (danger/fieldRadius/fieldHeight)"
      contains: "Theme.danger"
  key_links:
    - from: "qml/SettingsWindow.qml"
      to: "qml/Theme.qml"
      via: "token references"
      pattern: "Theme\\.(danger|fieldRadius|fieldHeight|accentSwatches|toggleTrack|swatch)"
    - from: "qml/ColorDialog.qml"
      to: "SettingsStore.setAccent (via controller in 06-03)"
      via: "Q_INVOKABLE on OK commit"
      pattern: "setAccent"
---

<objective>
The settings window's complete visual surface: Theme.qml token extension (D-08 token-only rule), SettingsWindow.qml, the staged custom ColorDialog.qml (D-07), and paying the shipped-literal debt in HotkeyCaptureDialog.qml (UI-SPEC line 17).

Purpose: VISU-03 (accent picker) and SYS-03 (settings window) are locked down to the pixel by the approved UI-SPEC — this plan renders that contract in token-only QML. Pure QML: no C++ controller yet (06-03 owns that).
Output: 3 QML files + Theme.qml extension + CMakeLists QML_FILES additions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tray-settings-autostart-packaging/06-CONTEXT.md
@.planning/phases/06-tray-settings-autostart-packaging/06-UI-SPEC.md
@.planning/phases/06-tray-settings-autostart-packaging/06-RESEARCH.md
@.planning/phases/06-tray-settings-autostart-packaging/06-PATTERNS.md
@.planning/phases/01-core-shell/01-UI-SPEC.md
@.planning/phases/05-theme-visual-polish/05-UI-SPEC.md

<interfaces>
From qml/Theme.qml (existing singleton — extend, never replace):
- `readonly property color surface: "#1E1E1E"`, `surfaceSecondary: "#2D2D30"`, `border: "#3F3F46"`, `accent` (mutable, default #0078D4), `accentLight`/`accentDark` (derived Qt.lighter/darker 1.45/1.4), `textPrimary: "#F5F5F5"`, `textSecondary: "#A0A0A0"`, `hoverBg`, `danger` (NEW #E5484D), `animFade` (120ms), `shadowMargin` (16), `radiusSurface` (12), `fieldRadius` (6), `fieldHeight` (36), `fontTitle`/`fontSubtitle`/`fontKeycap` roles.
- Existing shadows: static `assets/shadow.png` 16px opacity 0.45 (MainWindow shell precedent).

From qml/MainWindow.qml (shell analog — replicate structure):
- `Window { flags: Qt.Tool | Qt.FramelessWindowHint; color: "transparent"; visible: false }` + surface Rectangle with `border` + shadow Image + `centerOnScreen()` helper.
- NO inline `component` blocks (qmlcachegen rejects) — concrete widgets only.

From qml/HotkeyCaptureDialog.qml (wells/buttons analog — and the tokenization target):
- Well: Rectangle radius 6 (→ Theme.fieldRadius), height 36 (→ Theme.fieldHeight), `surfaceSecondary` fill, 1px border.
- OK button: 90x32, accent fill + white text (declared primary-button exception).
- Validation label color `#E5484D` (→ Theme.danger).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Extend Theme.qml tokens + tokenize HotkeyCaptureDialog.qml literals</name>
  <files>qml/Theme.qml, qml/HotkeyCaptureDialog.qml</files>
  <read_first>
    - qml/Theme.qml (full — token table, derivation functions, existing token names)
    - qml/HotkeyCaptureDialog.qml (full — locate every literal: #E5484D, radius 6, height 36 wells)
  </read_first>
  <action>
    Extend `qml/Theme.qml` with EXACTLY these new tokens (UI-SPEC Spacing Scale + Color sections — values locked):
    Geometry: `settingsWindowWidth: 480`, `settingsWindowHeight: 360`, `settingsSurfaceWidth: 448`, `settingsSurfaceHeight: 328`, `colorDialogWindowWidth: 280`, `colorDialogWindowHeight: 320`, `colorDialogSurfaceWidth: 248`, `colorDialogSurfaceHeight: 288`.
    Swatches: `accentSwatches: [ "#0078D4", "#00B7C3", "#2EA043", "#F14E4E", "#F0883E", "#C99A2E", "#8E5CF7", "#D669B8", "#5C6BC0" ]` — readonly property var, the 9 locked values in this exact order (UI-SPEC table); `swatchSize: 24`, `swatchGap: 8`, `swatchRingSize: 28`, `ringWidth: 2`, `swatchRadius: 6`, `swatchWellBg: "#2D2D30"`.
    Fields/rows: `fieldRadius: 6`, `fieldHeight: 36`, `settingsRowHotkey: 64`, `settingsRowAccent: 88`, `settingsRowAutostart: 64`, `settingsRowGap: 12`, `settingsPad: 24`.
    Toggle: `toggleWidth: 40`, `toggleHeight: 20`, `knobSize: 16`, `toggleTrackRadius: 10`, `knobRadius: 8`, `toggleTrackOff: "#3F3F46"`, `toggleTrackOn: accent` (binding — NOT a literal), `knobColor: "#F5F5F5"`.
    Color dialog: `svSize: 160`, `hueBarWidth: 24`, `hueBarRadius: 6`.
    Color: `danger: "#E5484D"` (NEW — UI-SPEC Color table).
    Guard contract (UI-SPEC line 127): if the cyan swatch's derived accentDark at factor 1.4 is below 4.5:1, clamp the derived FACTOR in Theme.qml (never hand-pick a shade constant, never exclude the swatch). Keep the existing accentLight/accentDark derivation functions unchanged unless the visual pass requires the clamp — note any clamp change in the SUMMARY.
    Tokenize `qml/HotkeyCaptureDialog.qml` (behavior ZERO change — D-03): replace literal `#E5484D` → `Theme.danger`; well radius literal 6 → `Theme.fieldRadius`; well height literal 36 → `Theme.fieldHeight`. No other edits to that file.
  </action>
  <verify>
    <automated>$t = @(Select-String -Path "qml/Theme.qml" -Pattern "accentSwatches").Count; if ($t -lt 1) { exit 1 }; $n = @(Select-String -Path "qml/HotkeyCaptureDialog.qml" -Pattern "#[0-9A-Fa-f]{6}").Count; if ($n -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `qml/Theme.qml` contains `accentSwatches` with all 9 hex values (`grep -c "0078D4.*00B7C3.*2EA043" qml/Theme.qml` — or verify all 9 hexes individually with grep, >= 9 matches)
    - `qml/Theme.qml` contains `danger: "#E5484D"` (grep -c "E5484D" >= 1)
    - `qml/Theme.qml` contains `toggleTrackOn: accent` (binding, not hex — grep -c "toggleTrackOn" >= 1 and the line does NOT contain "#")
    - `qml/Theme.qml` contains all geometry tokens (grep -c "settingsWindowWidth\|settingsWindowHeight\|settingsSurfaceWidth\|settingsSurfaceHeight\|colorDialogWindowWidth\|colorDialogWindowHeight\|colorDialogSurfaceWidth\|colorDialogSurfaceHeight" >= 8)
    - `qml/HotkeyCaptureDialog.qml` has ZERO hex literals left (grep -cE "#[0-9A-Fa-f]{6}" == 0) and contains `Theme.danger` + `Theme.fieldRadius` + `Theme.fieldHeight` (grep -c each >= 1)
    - qmlcachegen passes (covered by the 06-02-03 build gate)
  </acceptance_criteria>
  <done>Theme.qml carries the full Phase-6 token set; HotkeyCaptureDialog.qml literal debt paid with zero behavior change.</done>
</task>

<task type="auto">
  <name>task 2: SettingsWindow.qml — the locked 480x360 settings surface</name>
  <files>qml/SettingsWindow.qml</files>
  <read_first>
    - qml/MainWindow.qml (shell: flags, transparent color, shadow Image, centerOnScreen, Connections)
    - qml/HotkeyCaptureDialog.qml (wells, keycap value, focus handling)
    - .planning/phases/06-tray-settings-autostart-packaging/06-UI-SPEC.md Geometry & Window Contract (lines 168-221) + Copywriting Contract (lines 225-244)
  </read_first>
  <action>
    Create `qml/SettingsWindow.qml` — a `Window` root (second QML window; controller loads it in 06-03) implementing the LOCKED contract:
    Window: `flags: Qt.Tool | Qt.FramelessWindowHint`, `color: "transparent"`, `visible: false`, fixed `width: Theme.settingsWindowWidth` (480) / `height: Theme.settingsWindowHeight` (360), `title: "wisp — settings"` (a11y/taskbar identity). Surface: Rectangle 448x328 (`settingsSurfaceWidth/Height`), `radius: Theme.radiusSurface`, `color: Theme.surface`, 1px `border` border, `clip: true`, centered in window with 16px shadow margin + static shadow Image (`assets/shadow.png`, opacity 0.45 — same as MainWindow).
    Content (margins `Theme.settingsPad` 24, column): header Text "Settings" (fontTitle 15/600, textPrimary); 12px gap (`settingsRowGap`); THREE rows separated by 1px border hairlines:
    1. Hotkey row, height 64 (`settingsRowHotkey`): left column — label "Hotkey" (Title 15/400, textPrimary) + hint "Click to change" (Subtitle 12/400, textSecondary); right — hotkey value well: Rectangle `height: Theme.fieldHeight` (36), `radius: Theme.fieldRadius` (6), `color: Theme.surfaceSecondary`, 1px border, min width ~120, containing Text of the current combo (Keycap 12/600, textPrimary — value supplied by controller context property in 06-03; wire the property name now, e.g. `settingsController.currentHotkey`). Clickable (MouseArea) → signal `hotkeyRowClicked()` up to the controller (06-03 connects it to HotkeyCaptureDialog). Elide on overflow, never reflow (UI-SPEC text rule 3).
    2. Accent row, height 88 (`settingsRowAccent`): label "Accent color" (Title 15/400); below — horizontal swatch strip: 9 swatches from `Theme.accentSwatches` (24x24 `swatchSize`, `radius: Theme.swatchRadius` 6, gap `swatchGap` 8, left-aligned), each with a selection ring (28px `swatchRingSize` footprint, 2px `ringWidth` accentLight ring shown when the swatch color == current accent), click → `settingsController.applyAccent(color)` (Q_INVOKABLE from 06-03 → SettingsStore.setAccent, D-06 live apply); then 16px gap, then "Custom…" text button (Subtitle 12/400, textSecondary → textPrimary on hover) → `settingsController.openColorDialog()`. Keyboard: Tab focuses the strip, ←/→ moves selection (ring), Enter/Space applies (UI-SPEC Interaction Contract).
    3. Autostart row, height 64 (`settingsRowAutostart`): label "Start with Windows" (Title 15/400) + description "Launches quietly in the tray when you sign in" (Subtitle 12/400, textSecondary); right — toggle: track 40x20 (`toggleWidth/Height`), `radius: Theme.toggleTrackRadius` 10, fill `Theme.toggleTrackOff` when off / `Theme.toggleTrackOn` (accent binding) when on, 16px knob (`knobSize`, `radius: Theme.knobRadius` 8, `Theme.knobColor` white) sliding with `Behavior on x` 120ms (`Theme.animFade`), click/Space toggles → `settingsController.toggleAutostart()` (writes/removes Run key, D-10/D-12); state loaded on window open (06-03 controller refreshes `autostartEnabled`).
    NO inline `component` blocks. NO literals — every value via Theme.* (grep gate). No close animation; open fade-in 120ms is controller-driven (06-03). Row hover: `hoverBg` only (never accent).
  </action>
  <verify>
    <automated>cmake --build build/dev --target wisp; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $n = @(Select-String -Path "qml/SettingsWindow.qml" -Pattern "#[0-9A-Fa-f]{6}").Count; if ($n -ne 0) { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - `qml/SettingsWindow.qml` exists, has `Window` root with `Qt.Tool | Qt.FramelessWindowHint` and `width: Theme.settingsWindowWidth` (grep -c "FramelessWindowHint\|settingsWindowWidth" >= 2)
    - ZERO hex literals (grep -cE "#[0-9A-Fa-f]{6}" == 0)
    - Contains the three locked copy strings: "Hotkey", "Accent color", "Start with Windows" (grep -c each >= 1)
    - Contains "Click to change" and "Launches quietly in the tray when you sign in" and "Custom…" (grep -c each >= 1)
    - Contains 9-swatch strip bound to `Theme.accentSwatches` (grep -c "accentSwatches" >= 1) and toggle bound to `Theme.toggleTrackOn`/`toggleTrackOff` (grep -c "toggleTrack" >= 1)
    - No inline `component` keyword (grep -c "component " == 0)
    - qmlcachegen passes (build gate)
  </acceptance_criteria>
  <done>Settings window surface renders the full locked contract: three rows, 9 swatches, toggle, custom entry — token-only.</done>
</task>

<task type="auto">
  <name>task 3: ColorDialog.qml + CMakeLists QML_FILES registration + build gate</name>
  <files>qml/ColorDialog.qml, CMakeLists.txt</files>
  <read_first>
    - qml/HotkeyCaptureDialog.qml (modal shell analog, 90x32 button geometry)
    - CMakeLists.txt lines 65-85 (qt_add_qml_module QML_FILES list)
  </read_first>
  <action>
    Create `qml/ColorDialog.qml` — ApplicationModal staged color dialog (D-07, UI-SPEC Geometry & Window Contract):
    Window: `flags: Qt.Tool | Qt.FramelessWindowHint | Qt.ApplicationModal`, `color: "transparent"`, fixed `width: Theme.colorDialogWindowWidth` (280) / `height: Theme.colorDialogWindowHeight` (320), `title: "Custom color"`, `modality: Qt.ApplicationModal`. Surface 248x288 (`colorDialogSurfaceWidth/Height`), radius `Theme.radiusSurface` 12, `Theme.surface`, 1px border, shadow Image as siblings. Centered on primary screen on open (same logic as capture dialog).
    Content (margins 16): header "Custom color" (Title 15/600, textPrimary); SV square `svSize` 160x160 — Rectangle with a two-stop hue gradient (the CURRENT hue) plus a vertical transparent→black overlay, crosshair cursor, drag → hue/saturation update (staged state); vertical hue bar 24x160 (`hueBarWidth`), rainbow gradient, radius `Theme.hueBarRadius` 6, drag → hue change; live preview 32x32 swatch + hex readout Text `#RRGGBB` (Subtitle 12/400, textSecondary); OK / Cancel buttons — EXACT copy of capture dialog geometry: 90x32, radius 6, OK = accent fill + white text (declared primary-button exception), Cancel = `Theme.surfaceSecondary` + textPrimary.
    Staging contract (D-06): edits stage internally; OK → `settingsController.commitCustomColor(hex)` (06-03 → SettingsStore.setAccent); Cancel/Esc discards; Enter confirms. `forceActiveFocus` on open. No animation.
    Register in CMakeLists.txt: add `qml/SettingsWindow.qml` and `qml/ColorDialog.qml` to the `qt_add_qml_module(wisp_qml ... QML_FILES ...)` list (after `qml/HotkeyCaptureDialog.qml`, mirroring existing entries).
  </action>
  <verify>
    <automated>cmake --preset dev && cmake --build build/dev --target wisp</automated>
  </verify>
  <acceptance_criteria>
    - `qml/ColorDialog.qml` exists with `Qt.ApplicationModal` and `Theme.colorDialogWindowWidth` (grep -c "ApplicationModal\|colorDialogWindowWidth" >= 2)
    - ZERO hex literals in ColorDialog.qml (grep -cE "#[0-9A-Fa-f]{6}" == 0)
    - Contains "OK" and "Cancel" buttons with 90x32 geometry via Theme tokens (grep -c "90\|Theme.fieldHeight" — button width/height reference Theme tokens or the literal 90/32 per capture-dialog precedent; verify visually consistent with HotkeyCaptureDialog.qml buttons)
    - Contains hex readout Text and staged commit call `commitCustomColor` (grep -c "commitCustomColor" >= 1)
    - CMakeLists.txt QML_FILES list contains both new files (grep -c "SettingsWindow.qml\|ColorDialog.qml" >= 2)
    - `cmake --build build/dev --target wisp` succeeds (qmlcachegen compiles all 4 QML files)
  </acceptance_criteria>
  <done>Color dialog staged + registered; full QML module compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML surface → SettingsStore | Color dialog OK / swatch clicks call setAccent; values originate from a bounded swatch list or the dialog's staged HSV state — no freeform input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | (none — no injection surface) | SettingsWindow.qml / ColorDialog.qml | accept | No web/remote content reaches QML; accent values are bounded (9-swatch list or dialog-derived HSV clamped to [0,1]); SettingsStore.setAccent already ignores invalid colors (D-16, Phase 5) |
| T-06-01 | Tampering | hex readout / staged value path | mitigate | Hex string generated from staged HSV state (QColor::name()), never freeform text; setAccent validates |
</threat_model>

<verification>
- Build gate: `cmake --preset dev && cmake --build build/dev --target wisp` (qmlcachegen compiles all QML)
- Grep gates: zero hex literals in both new QML files and HotkeyCaptureDialog.qml; no inline `component`
</verification>

<success_criteria>
- Theme.qml carries all 33+ Phase-6 tokens + 9-value accentSwatches + danger; toggleTrackOn is a binding, not a literal
- HotkeyCaptureDialog.qml tokenized with zero behavior change
- SettingsWindow.qml implements the full locked contract (rows, swatches, ring, toggle, custom entry, keyboard nav)
- ColorDialog.qml implements the staged contract (SV square, hue bar, hex readout, OK/Cancel)
- QML module builds clean; no C++ controller yet (06-03)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tray-settings-autostart-packaging/06-02-SUMMARY.md`
</output>
