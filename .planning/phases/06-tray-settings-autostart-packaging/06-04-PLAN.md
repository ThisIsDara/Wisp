---
phase: 06-tray-settings-autostart-packaging
plan: 06-04
type: execute
wave: 4
depends_on: [06-03]
files_modified: [src/app/main.cpp]
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]
must_haves:
  truths:
    - "Launching wisp while it is already running surfaces the existing instance's launcher window and the second process exits (D-09)"
    - "Launching wisp with --autostart boots quiet: resident in tray, window hidden until hotkey/tray summon (D-11)"
    - "Tray 'Settings' opens the settings window; the launcher hotkey still summons the launcher (D-04)"
    - "Tray disc reflects the current accent at startup and live on accent change (UI-SPEC tray icon contract)"
  artifacts:
    - path: "src/app/main.cpp"
      provides: "Boot wiring: single-instance guard, --autostart parse, tray→settings, accent→tray, showRequested→launcher"
      contains: "tryAcquire"
  key_links:
    - from: "src/app/main.cpp"
      to: "WinSingleInstance::tryAcquire"
      via: "boot guard (first statement block)"
      pattern: "tryAcquire"
    - from: "src/app/main.cpp"
      to: "WinSingleInstance::signalShow / showRequested"
      via: "second-instance path"
      pattern: "signalShow"
    - from: "src/app/main.cpp"
      to: "SettingsStore::accentChanged → TrayIcon::setAccent"
      via: "connect"
      pattern: "accentChanged"
    - from: "src/app/main.cpp"
      to: "TrayIcon::settingsRequested → SettingsWindow::open"
      via: "connect"
      pattern: "settingsRequested"
---

<objective>
Boot-order wiring in main.cpp: single-instance guard FIRST (before window creation), `--autostart` parse suppressing the first show, tray Settings → settings window, accent → tray disc, and second-instance showRequested → launcher summon.

Purpose: SYS-01 (second instance surfaces the existing one), SYS-02 (quiet autostart boot), SYS-03 (tray-only settings entry) all converge on main.cpp's boot discipline (CONTEXT Integration Points). This is the last code plan — after it, the app is fully resident.
Output: main.cpp wiring only.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tray-settings-autostart-packaging/06-CONTEXT.md
@.planning/phases/06-tray-settings-autostart-packaging/06-UI-SPEC.md
@.planning/phases/06-tray-settings-autostart-packaging/06-RESEARCH.md
@.planning/phases/06-tray-settings-autostart-packaging/06-PATTERNS.md
@.planning/phases/06-tray-settings-autostart-packaging/06-03-SUMMARY.md (if exists — SettingsWindow/TrayIcon APIs)

<interfaces>
From 06-01 (already shipped): WinSingleInstance { bool tryAcquire(); void signalShow(); void startWatching(); signal void showRequested(); } · AutostartManager { bool isEnabled(); void setEnabled(bool); }
From 06-03 (shipped): TrayIcon::settingsRequested() signal, TrayIcon::setAccent(QColor) · SettingsWindow { void open(); }
Existing (Phase 2/5): LauncherController::showUserRequested() (tray Open wisp path, fullscreen guard), SettingsStore::accentChanged(QColor), HotkeyManager::start(), tray.show() → connect → hotkeys.start() boot order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Single-instance guard + --autostart quiet boot + showRequested summon</name>
  <files>src/app/main.cpp</files>
  <read_first>
    - src/app/main.cpp (full — current boot order: QApplication setup, tray.show(), connects, hotkeys.start(); find where the launcher window is first shown)
    - src/core/LauncherController.h (showUserRequested / canShow surface)
    - src/core/WinSingleInstance.h (06-01 API)
  </read_first>
  <action>
    Modify `src/app/main.cpp`:
    - **Single-instance guard FIRST** (CONTEXT Integration Points: "single-instance check runs FIRST (before window creation)"): at the very top of main(), before constructing QApplication/windows: create WinSingleInstance; if `tryAcquire()` returns false → call `signalShow()` (create-or-open the named event + SetEvent — the first instance's watcher picks it up, RESEARCH Pitfall 1 fix) and `return 0` (second instance exits silently, no UI surface — UI-SPEC second-instance contract). Only the first instance continues.
    - **--autostart parse**: `const bool autostartBoot = QCoreApplication::arguments().contains(QStringLiteral("--autostart"));` — when true, suppress the initial launcher window show (boot quiet to tray, window hidden — D-11); the tray is still shown (resident), hotkey/tray summon later (existing paths).
    - **showRequested summon**: `connect(&singleInstance, &WinSingleInstance::showRequested, &controller, &LauncherController::showUserRequested)` — second instance signals the first to SHOW THE LAUNCHER (D-09; not settings, not a toast).
    - Call `singleInstance.startWatching()` after the connect (before or after tray.show() — but the watcher must be running before the event can fire; startWatching before show() is fine).
    - Keep the existing boot order for everything else (tray.show() → connects → hotkeys.start()); do NOT reorder unrelated steps.
  </action>
  <verify>
    <automated>cmake --build build/dev --target wisp && ctest --test-dir build/dev -R "tst_launcher|tst_hotkey" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/main.cpp` calls `tryAcquire()` before window construction (grep -n "tryAcquire" — line number < the line constructing the launcher window)
    - `src/app/main.cpp` contains the second-instance path `signalShow` + return (grep -c "signalShow" >= 1 and `return 0` or equivalent within the guard)
    - `src/app/main.cpp` parses `--autostart` (grep -c "--autostart" >= 1) and conditionally skips the first show
    - `src/app/main.cpp` connects `showRequested` → `showUserRequested` (grep -c "showRequested" >= 1)
    - `singleInstance.startWatching()` is called (grep -c "startWatching" >= 1)
    - Build + tst_launcher/tst_hotkey green (exit 0)
  </acceptance_criteria>
  <done>Single-instance guard, quiet autostart boot, and second-instance summon wired in boot order.</done>
</task>

<task type="auto">
  <name>task 2: Settings entry + accent-to-tray live binding + wave gate</name>
  <files>src/app/main.cpp</files>
  <read_first>
    - src/app/main.cpp (current state after task 1)
    - src/ui/SettingsWindow.h (06-03 — open() API)
    - src/tray/TrayIcon.h (settingsRequested / setAccent)
    - src/core/SettingsStore.h (accentChanged)
  </read_first>
  <action>
    Continue in `src/app/main.cpp`:
    - Construct `SettingsWindow settingsWindow;` (alongside the other controllers; QML host owns its engine — mirror HotkeyCaptureDialog construction).
    - **Tray → settings**: `connect(tray, &TrayIcon::settingsRequested, &settingsWindow, &SettingsWindow::open)` — D-04: settings opens from tray ONLY; no launcher affordance.
    - **Accent → tray disc**: at startup, after the store is constructed: `tray->setAccent(settingsStore.accent());` (same silent-fallback path as the launcher, UI-SPEC tray contract) AND `connect(&settingsStore, &SettingsStore::accentChanged, tray, [tray](const QColor &c) { tray->setAccent(c); })` — live repaint on picker change (the Phase-5 reactive path; PATTERNS anti-pattern 1: never reach into the store from TrayIcon).
    - Ensure the settings QML host is registered in the same module path the 06-02 QML expects (qrc:/qt/qml/wisp/… — mirror HotkeyCaptureDialog registration exactly).
    - Do NOT add any settings affordance to the launcher window (D-04).
    Run the wave gate: `cmake --preset dev && cmake --build build/dev && ctest --test-dir build/dev --output-on-failure`. Then a manual smoke: launch wisp twice — the second launch must exit while the first shows the launcher (document result in the SUMMARY; if the smoke can't run headless, record as pending manual step in the SUMMARY).
  </action>
  <verify>
    <automated>cmake --preset dev && cmake --build build/dev && ctest --test-dir build/dev --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/main.cpp` constructs SettingsWindow and connects `settingsRequested` → `open` (grep -c "settingsRequested" >= 1 and "SettingsWindow" >= 1)
    - `src/app/main.cpp` calls `setAccent(settingsStore.accent())` at startup (grep -c "setAccent" >= 1)
    - `src/app/main.cpp` connects `accentChanged` → tray setAccent (grep -c "accentChanged" >= 1)
    - Full build + ctest green: 0 failures, >= 19 tests
    - Manual smoke documented in SUMMARY (second launch → first shows launcher, second exits)
  </acceptance_criteria>
  <done>Resident wiring complete: tray→settings, accent→tray live, full suite green, second-instance smoke verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| second process → first process | Any same-session process can SetEvent on the named event → first instance shows its launcher window |
| argv → boot behavior | --autostart argument changes boot posture (hidden vs shown) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Spoofing | WinSingleInstance show event | accept | Session-local `Local\` namespace; worst case a session process pops the launcher — nuisance only (RESEARCH A2) |
| T-06-07 | Tampering | --autostart arg parsing | mitigate | Exact-string match on the documented arg (D-12); no other argv interpretation; unknown args ignored |
| T-06-01 | Tampering | Run-key value (written by AutostartManager via settings) | mitigate | Quoted applicationFilePath value (06-01 T-06-01 disposition stands) |
</threat_model>

<verification>
- Full build + full ctest suite green (>= 19 tests)
- Manual smoke: second launch surfaces first instance (documented in SUMMARY)
</verification>

<success_criteria>
- Guard order: tryAcquire before window creation; second instance signals + exits silently
- --autostart → quiet boot (tray resident, window hidden)
- Tray Settings → settings window open; accent → tray disc live (startup + accentChanged)
- No launcher-window settings affordance added
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-tray-settings-autostart-packaging/06-04-SUMMARY.md`
</output>
