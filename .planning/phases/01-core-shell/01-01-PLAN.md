---
phase: 01-core-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
  - CMakePresets.json
  - .gitignore
  - build.ps1
  - src/app/main.cpp
  - src/app/wisp.rc
  - qml/MainWindow.qml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Product name is `wisp` (user rename from 'Rofi' — D-01); project/repo keep the name 'Rofi-Windows'"
    - "Qt 6.11.1 installed at C:/Qt/6.11.1/msvc2022_64 via aqtinstall — the machine's existing Qt 6.5.3/5.15.2 is never referenced (D-05, D-06)"
    - "Executable target is named `wisp` producing wisp.exe (D-02)"
    - "wisp.exe embeds VERSIONINFO: FileVersion 0.1.0, ProductName 'wisp', CompanyName 'TID' (D-04)"
    - "main.cpp sets QCoreApplication organizationName 'TID' / applicationName 'wisp' — seeds the D-03 config root"
    - "The project builds from the CLI: build.ps1 → cmake --preset → Ninja (D-07)"
    - "No SetProcessDpiAwareness* / SetProcessDpiAwarenessContext call anywhere (UI-SPEC DPI lock)"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "Qt6 Quick/Qml app target wisp, qt_add_qml_module(wisp URI wisp), wisp.rc resource, C++20"
      contains: "qt_standard_project_setup"
    - path: "CMakePresets.json"
      provides: "configure + build presets for the dev workflow"
      contains: "\"generator\": \"Ninja\""
    - path: "src/app/wisp.rc"
      provides: "VERSIONINFO with FileVersion/ProductName/CompanyName"
      contains: "\"wisp\""
    - path: "src/app/main.cpp"
      provides: "QGuiApplication + QQmlApplicationEngine entry, QML module load, org/name/version seed"
      min_lines: 15
    - path: "qml/MainWindow.qml"
      provides: "Placeholder window: Qt.Tool | FramelessWindowHint, transparent, centered"
      contains: "FramelessWindowHint"
  key_links:
    - from: "src/app/main.cpp"
      to: "qml/MainWindow.qml"
      via: "engine.loadFromModule(\"wisp\", \"MainWindow\")"
      pattern: "loadFromModule"
---

<objective>
Stand up the buildable Qt 6.11.1 + QML skeleton of the wisp launcher: install the locked toolchain (aqtinstall → C:\Qt), create the CMake project (target `wisp` → wisp.exe, URI `wisp` QML module, VERSIONINFO resource), seed the CLI-first build workflow (build.ps1 + CMakePresets), and deliver a placeholder window with the Phase-1-locked window contract (Qt.Tool | FramelessWindowHint, 640×400 surface, transparent, centered on primary screen) so the repo is runnable end-to-end from a single script.

**Purpose:** Everything in later phases depends on this shell existing and building from a clean checkout with no GUI toolchain. This plan proves the toolchain lock (6.11.1 — NOT the machine's 6.5.3), the dynamic-linking-only build shape, and the CLI-first workflow, which are all retrofit-proof decisions that must land day one (PITFALLS #8, #9).

**Output:** `cmake --preset dev && cmake --build --preset dev` from build.ps1 produces `build/dev/wisp.exe`; running it shows an empty frameless transparent 640×400 window centered on the primary screen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-core-shell/01-CONTEXT.md
@.planning/phases/01-core-shell/01-UI-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Toolchain facts (CONTEXT.md D-05, D-06, D-07):
  - VS 2022 Community C++ tools installed; vcvars64.bat at C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat
  - CMake 4.3.4 on PATH; Ninja bundled with VS at C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\Ninja\ninja.exe
  - Machine Qt is 6.5.3 msvc2019_64 + 5.15.2 — DO NOT USE (D-05); must install 6.11.1 win64_msvc2022_64
  - aqtinstall traces present in %APPDATA%\aqt (user's known-comfortable path, D-06)

Locked product identity (D-01..D-04):
  - Product name: wisp. Executable: wisp.exe. Version: 0.1.0. QSettings org/app: TID/wisp.
  - VERSIONINFO embedded now so Task Manager/autostart entries look right from day one.

Window contract for the placeholder (UI-SPEC Geometry & Window Contract + CONTEXT decisions):
  - flags: Qt.Tool | Qt.FramelessWindowHint (NEVER Qt::Popup)
  - Surface 640×400 logical px fixed; window canvas 672×432 (640×400 + 16px shadow margin per side — shadow lands in Plan 01-02)
  - Centered on primary screen availableGeometry().center(), logical px via Qt QScreen — never GetSystemMetrics
  - color: "transparent" + C++ Qt::WA_TranslucentBackground on the QQuickWindow (alpha buffer via QQuickWindow::setDefaultAlphaBuffer(true))
  - DPI: Qt PMv2 default — never call SetProcessDpiAwareness* anywhere (incl. main.cpp)
  - Window title / accessibility name: "wisp"
  - No blur in this phase; static shadow is Plan 01-02
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Install Qt 6.11.1 (win64_msvc2022_64) via aqtinstall into C:\Qt</name>
  <files>
    (no repo files — machine-level toolchain install)
  </files>
  <read_first>
    @.planning/phases/01-core-shell/01-CONTEXT.md (D-05, D-06 — toolchain lock, aqtinstall path)
    @.planning/research/STACK.md (Qt version rationale: 6.11.1 open-source, never 6.8 LTS, never the machine's 6.5.3)
  </read_first>
  <action>
    Install the locked Qt line with aqtinstall:

    1. `python -m pip install --upgrade aqtinstall`
    2. `python -m aqt install-qt windows desktop 6.11.1 win64_msvc2022_64 --outputdir C:\Qt`
       (creates C:\Qt\6.11.1\msvc2022_64 with bin/, lib/, qml/, include/, etc.)
    3. Confirm the install by running `C:\Qt\6.11.1\msvc2022_64\bin\qmake.exe -query QT_VERSION` which must print `6.11.1`.
    4. Confirm `C:\Qt\6.11.1\msvc2022_64\bin\windeployqt.exe` exists (needed by Plan 01-03).
    5. Do NOT add anything from C:\Qt\6.5.3 (msvc2019_64) or 5.15.2 to PATH, CMAKE_PREFIX_PATH, or any repo file — they must never be referenced.

    If aqt reports the archive is already installed (re-run on this machine), verify the two existence checks above and move on — the install task is idempotent.
  </action>
  <verify>
    <automated>& "C:\Qt\6.11.1\msvc2022_64\bin\qmake.exe" -query QT_VERSION 2>&1; Test-Path "C:\Qt\6.11.1\msvc2022_64\bin\windeployqt.exe"</automated>
  </verify>
  <acceptance_criteria>
    - `C:\Qt\6.11.1\msvc2022_64\bin\qmake.exe -query QT_VERSION` outputs exactly `6.11.1`
    - `C:\Qt\6.11.1\msvc2022_64\bin\windeployqt.exe` exists
    - No file in the repo references `6.5.3` or `msvc2019_64` or `5.15.2`
  </acceptance_criteria>
  <done>Qt 6.11.1 toolchain installed at C:\Qt — the locked version, not the machine's stale one</done>
</task>

<task type="auto">
  <name>task 2: Create CMake project scaffold (CMakeLists.txt, CMakePresets.json, build.ps1, .gitignore) targeting wisp.exe</name>
  <files>
    CMakeLists.txt
    CMakePresets.json
    build.ps1
    .gitignore
  </files>
  <read_first>
    @.planning/research/ARCHITECTURE.md (Recommended Project Structure — src/app, src/core, src/win firewall, qml/, tests/, packaging/)
    @.planning/phases/01-core-shell/01-CONTEXT.md (D-07 CLI-first, D-04 version 0.1.0)
  </read_first>
  <action>
    Create the four files:

    **CMakeLists.txt** (repo root):
    - `cmake_minimum_required(VERSION 3.25)`, `project(wisp VERSION 0.1.0 LANGUAGES CXX)`
    - `set(CMAKE_CXX_STANDARD 20)` + `set(CMAKE_CXX_STANDARD_REQUIRED ON)`
    - `qt_standard_project_setup()` — dynamic linking only (LGPL lock; never build Qt statically)
    - `find_package(Qt6 6.11 REQUIRED COMPONENTS Quick Qml Gui Core)` (no Widgets — tray is Phase 6)
    - `qt_add_executable(wisp WIN32 src/app/main.cpp src/app/wisp.rc)` → wisp.exe
    - `qt_add_qml_module(wisp URI wisp VERSION 0.1.0 QML_FILES qml/MainWindow.qml)`
    - `target_link_libraries(wisp PRIVATE Qt6::Quick Qt6::Qml Qt6::Gui)`
    - Do NOT yet create the tests/ wiring (Plan 01-03) or src/core/src/win directories (empty now is fine — ARCHITECTURE says src/win stays empty-or-stub this phase)

    **CMakePresets.json** (repo root, version 6):
    - configure preset `dev`: generator `Ninja`, binaryDir `${sourceDir}/build/dev`, cacheVariables: `CMAKE_PREFIX_PATH: "C:/Qt/6.11.1/msvc2022_64"`, `CMAKE_BUILD_TYPE: "Debug"`
    - configure preset `release`: same generator + binaryDir `build/release`, `CMAKE_BUILD_TYPE: "Release"`
    - build presets `dev` and `release` mapping to the configure presets

    **build.ps1** (repo root, CLI-first driver per D-07):
    - `param([string]$Config = "dev")`
    - Resolve vcvars64.bat at `C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat` (required so the Ninja generator finds cl.exe); run `cmake --preset $Config` then `cmake --build --preset $Config` inside a `cmd /c "vcvars64 && cmake ..."` invocation (vcvars64.bat is a cmd script; set $env:ErrorActionPreference="Stop" and propagate non-zero exit codes)
    - Prints the output exe path `build/$Config/wisp.exe` on success

    **.gitignore** (repo root):
    - `build/`, `CMakeUserPresets.json`, `*.user`

    Do NOT add any DPI-awareness manifest or SetProcessDpiAwareness call — Qt owns PMv2 (UI-SPEC DPI lock).
  </action>
  <verify>
    <automated>Get-Content CMakeLists.txt | Select-String -Pattern "qt_standard_project_setup|qt_add_executable\(wisp|qt_add_qml_module\(wisp"; Get-Content CMakePresets.json | Select-String -Pattern '"generator": "Ninja"'</automated>
  </verify>
  <acceptance_criteria>
    - CMakeLists.txt contains `qt_standard_project_setup()` and `qt_add_executable(wisp WIN32`
    - CMakeLists.txt contains `qt_add_qml_module(wisp` with `URI wisp` and `QML_FILES qml/MainWindow.qml`
    - CMakeLists.txt contains `find_package(Qt6 6.11 REQUIRED COMPONENTS Quick Qml Gui Core)` and does NOT reference Widgets
    - CMakePresets.json declares a `dev` configure preset with `"generator": "Ninja"` and CMAKE_PREFIX_PATH `C:/Qt/6.11.1/msvc2022_64`
    - build.ps1 exists and references vcvars64.bat and `cmake --preset` / `cmake --build --preset`
    - .gitignore contains `build/`
  </acceptance_criteria>
  <done>CMake project scaffold in place — target wisp.exe, Ninja presets, CLI-first build script</done>
</task>

<task type="auto">
  <name>task 3: Create main.cpp + VERSIONINFO resource + placeholder MainWindow.qml; build and smoke-run wisp.exe</name>
  <files>
    src/app/main.cpp
    src/app/wisp.rc
    qml/MainWindow.qml
  </files>
  <read_first>
    CMakeLists.txt (just created — verify target/source names match)
    @.planning/phases/01-core-shell/01-CONTEXT.md (D-01..D-04 identity, D-08/D-09 lifecycle reserved for Plan 01-02)
    @.planning/phases/01-core-shell/01-UI-SPEC.md (Geometry & Window Contract)
  </read_first>
  <action>
    Create the three source files:

    **src/app/main.cpp**:
    - `QGuiApplication app(argc, argv);`
    - `QCoreApplication::setOrganizationName("TID");` `QCoreApplication::setApplicationName("wisp");` `QCoreApplication::setApplicationVersion("0.1.0");` (D-03, D-04 seeds)
    - `QQuickWindow::setDefaultAlphaBuffer(true);` BEFORE creating the engine/window (required for the transparent window contract)
    - `QQmlApplicationEngine engine;` `engine.loadFromModule("wisp", "MainWindow");` then `if (engine.rootObjects().isEmpty()) return -1;`
    - After load, cast first root object to `QQuickWindow*` and call `window->setFlag(Qt::WA_TranslucentBackground);`
    - `return app.exec();`
    - NO DPI API calls anywhere.

    **src/app/wisp.rc**:
    - `#include <winver.h>` + a `VS_VERSION_INFO VERSIONINFO` block: `FILEVERSION 0,1,0,0`, `PRODUCTVERSION 0,1,0,0`, `FILEOS VOS_NT_WINDOWS32`, `FILETYPE VFT_APP`
    - StringFileInfo (040904b0): CompanyName `TID`, FileDescription `wisp`, FileVersion `0.1.0`, InternalName `wisp`, OriginalFilename `wisp.exe`, ProductName `wisp`, ProductVersion `0.1.0`
    - VarFileInfo Translation 0x409, 1200

    **qml/MainWindow.qml** (placeholder — the designed shell with Theme/animation/surface is Plan 01-02):
    ```qml
    import QtQuick
    import QtQuick.Window

    Window {
        id: root
        title: "wisp"
        flags: Qt.Tool | Qt.FramelessWindowHint
        width: 672
        height: 432
        color: "transparent"
        visible: true

        Rectangle {
            anchors.centerIn: parent
            width: 640
            height: 400
            color: "#1E1E1E"
            radius: 12
        }

        Component.onCompleted: {
            // Centered on the primary screen's available geometry (logical px — UI-SPEC)
            x = Screen.availableX + Math.round((Screen.availableWidth - width) / 2)
            y = Screen.availableY + Math.round((Screen.availableHeight - height) / 2)
        }
    }
    ```
    (Widths/heights here are placeholders for the surface contract; Theme.qml tokens land in Plan 01-02.)

    **Build and smoke-run:**
    1. `powershell -ExecutionPolicy Bypass -File build.ps1` → must exit 0 and print `build/dev/wisp.exe`
    2. Start `build/dev/wisp.exe`, wait ~2s, assert the process is still alive (window showing, no crash), then stop it.
    3. Verify the version resource: `(Get-Item build/dev/wisp.exe).VersionInfo` → ProductName `wisp`, FileVersion `0.1.0`, CompanyName `TID`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File build.ps1; $vi = (Get-Item build/dev/wisp.exe).VersionInfo; $vi.ProductName; $vi.FileVersion; $vi.CompanyName</automated>
  </verify>
  <acceptance_criteria>
    - `powershell -ExecutionPolicy Bypass -File build.ps1` exits 0 and `build/dev/wisp.exe` exists
    - wisp.exe VersionInfo: ProductName == `wisp`, FileVersion == `0.1.0`, CompanyName == `TID`
    - main.cpp contains `setOrganizationName("TID")`, `setApplicationName("wisp")`, `setDefaultAlphaBuffer(true)`, `setFlag(Qt::WA_TranslucentBackground)` and does NOT contain `SetProcessDpiAwareness`
    - qml/MainWindow.qml contains `flags: Qt.Tool | Qt.FramelessWindowHint` and does NOT contain `Popup`
    - Launching wisp.exe keeps the process alive for ≥2s (no startup crash)
  </acceptance_criteria>
  <done>wisp.exe builds from the CLI, embeds correct version info, and runs showing the placeholder window</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolchain install (task 1) | aqtinstall downloads Qt archives from the official Qt download servers to C:\Qt — one-time machine-level action, no repo impact |
| Build environment (tasks 2-3) | cmake + Ninja + cl.exe execute in the user's session; outputs are local build artifacts under build/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | aqtinstall (task 1) | mitigate | Pin exact version 6.11.1 win64_msvc2022_64 from official Qt servers; verify qmake reports 6.11.1 and windeployqt exists before proceeding |
| T-01-02 | S | build.ps1 | mitigate | Script invokes vcvars64.bat path verified via Test-Path; fails loudly (ErrorActionPreference Stop, exit-code propagation) rather than building with a wrong toolchain |
| T-01-03 | E | build artifacts | accept | No secrets, network I/O, or user data in Phase 1; wisp.exe is a static local UI shell (no elevation, asInvoker default) |
</threat_model>

<verification>
1. `powershell -ExecutionPolicy Bypass -File build.ps1` exits 0 (dev preset)
2. `build/dev/wisp.exe` exists; VersionInfo ProductName=wisp, FileVersion=0.1.0, CompanyName=TID
3. Running wisp.exe shows a frameless transparent window centered on the primary screen; process stays alive
4. Repo contains no reference to Qt 6.5.3 / msvc2019_64 / 5.15.2 and no SetProcessDpiAwareness call
</verification>

<success_criteria>
- [ ] Qt 6.11.1 win64_msvc2022_64 installed at C:\Qt and verified (qmake -query QT_VERSION == 6.11.1)
- [ ] CMake project builds wisp.exe (Ninja preset, C++20, Qt6 Quick/Qml/Gui/Core, dynamic linking)
- [ ] VERSIONINFO embedded: ProductName "wisp", CompanyName "TID", FileVersion 0.1.0
- [ ] build.ps1 is the single CLI entry point producing build/dev/wisp.exe
- [ ] Placeholder window honors the locked contract: Qt.Tool | FramelessWindowHint, 640×400 surface, transparent, centered, title "wisp"
- [ ] Zero DPI-manual-API calls; Qt owns PMv2
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-shell/01-01-SUMMARY.md`
</output>
