---
phase: 01-core-shell
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - packaging/THIRD-PARTY-NOTICES.txt
  - deploy.ps1
  - src/app/FrameTimeProbe.h
  - src/app/FrameTimeProbe.cpp
  - src/app/main.cpp
  - tests/tst_shell.cpp
  - CMakeLists.txt
autonomous: true
requirements: [VISU-01]
must_haves:
  truths:
    - "packaging/THIRD-PARTY-NOTICES.txt stub exists with LGPLv3 header + dynamic-linking statement + Qt 6.11.1 module list (PROJECT.md licensing constraint / STACK.md LGPL Compliance — PITFALLS #8)"
    - "deploy.ps1 runs windeployqt --release --qmldir qml → standalone folder that launches wisp.exe without Qt dev tools (PITFALLS #9 — deploy script seed)"
    - "Frame-time logging in debug builds: frames >17ms during animation logged (UI-SPEC Interaction perf guard: 60fps + first show <100ms)"
    - "Qt Test smoke test (tst_shell) passes: window contract + Theme.qml tokens load (VISU-01 evidence)"
  artifacts:
    - path: "packaging/THIRD-PARTY-NOTICES.txt"
      provides: "LGPL compliance seed — Qt 6.11.1, dynamic linking lock, module list, placeholder for final notices"
      contains: "LGPL"
    - path: "deploy.ps1"
      provides: "Standalone deploy script — windeployqt --qmldir, output staging folder"
      contains: "windeployqt"
    - path: "src/app/FrameTimeProbe.cpp"
      provides: "Debug-build frame-time logger connected to QQuickWindow::afterRendering"
      contains: "afterRendering"
    - path: "tests/tst_shell.cpp"
      provides: "Qt Test smoke test — window flags/geometry/animation contract + Theme.qml tokens"
      contains: "QTEST_MAIN"
  key_links:
    - from: "deploy.ps1"
      to: "packaging/THIRD-PARTY-NOTICES.txt"
      via: "Copy-Item"
      pattern: "THIRD-PARTY-NOTICES"
    - from: "src/app/main.cpp"
      to: "src/app/FrameTimeProbe.h"
      via: "#include"
      pattern: "FrameTimeProbe"
    - from: "tests/tst_shell.cpp"
      to: "qml/Theme.qml"
      via: "loadFromModule / QQmlComponent"
      pattern: "Theme"
---

<objective>
Close out Phase 1's compliance and verification seed work: create the LGPL compliance scaffold (THIRD-PARTY-NOTICES.txt stub + dynamic-linking lock — PITFALLS #8), the standalone deploy script (`windeployqt --release --qmldir` — PITFALLS #9), the debug-build frame-time performance guard (VISU-01's 60fps evidence: first show <100ms, no frames >17ms during animation), and a Qt Test smoke test that pins the window contract and Theme.qml tokens so Phase 3/5 can't silently break them.

**Purpose:** The two "looks done but isn't" compliance/deployment items and the performance contract are seeded NOW — retrofitting them at release is the classic blocked-release failure. The smoke test is the regression net the phase's success criteria run on.

**Output:** `deploy.ps1` produces a self-contained folder; debug builds log slow frames; `ctest` runs a passing tst_shell.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-core-shell/01-CONTEXT.md
@.planning/phases/01-core-shell/01-UI-SPEC.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- LGPL scaffold (PITFALLS #8 + CONTEXT.md decisions):
  - Dynamic linking only — lock it in the deploy/build scripts; never static Qt
  - THIRD-PARTY-NOTICES.txt stub: LGPLv3 text pointer + exact Qt version + module list + "shipped in installer root, verified on clean VMs in Phase 6"
  - windeployqt may copy MSVC runtime DLLs — those are NOT for redistribution; the official VC_redist.x64.exe is Phase 6's job. The stub documents this.

Deploy (PITFALLS #9):
  - windeployqt --release --qmldir <qml dir> <exe> — the --qmldir flag is MANDATORY (omitting it = missing QtQuick modules on target machines)
  - Run from the Qt 6.11.1 bin: C:\Qt\6.11.1\msvc2022_64\bin\windeployqt.exe
  - Deploy target: the Release build from build/release/wisp.exe → build/deploy/wisp/

Perf guard (UI-SPEC Interaction Contract — "Perf guard: first show → first painted frame < 100ms; animation holds 60fps (frame-time logging in debug build, drop-counting)"):
  - FrameTimeProbe: QObject connected to QQuickWindow::afterRendering
  - Debug builds (#ifdef QT_DEBUG): log any inter-frame gap > 17ms with a frame-time counter; log time from first afterRendering to open-animation completion
  - Keep it cheap: timer reads + qDebug only — no effect on the release path

Smoke test (UI-SPEC + CONTEXT "Optional Qt Test smoke test for the shell" — discretionary, doing it):
  - tests/tst_shell.cpp — Qt Test, links against the wisp QML module
  - Verifies: MainWindow window contract (flags Qt.Tool|Frameless, surface/window geometry, title "wisp", transparent) + Theme.qml token values (spot-check surface color, durations, easings)
  - Wired with include(CTest) + add_test in CMakeLists.txt
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Create LGPL compliance scaffold — packaging/THIRD-PARTY-NOTICES.txt stub</name>
  <files>
    packaging/THIRD-PARTY-NOTICES.txt
  </files>
  <read_first>
    @.planning/research/PITFALLS.md (#8 LGPL compliance — notices content, source offer, relink)
    @.planning/research/STACK.md (LGPL Compliance section — dynamic linking carve-out, notice requirements)
    @.planning/research/SUMMARY.md (Gaps to Address — "Settings storage…", LGPL verification deferred to Phase 6)
  </read_first>
  <action>
    Create `packaging/THIRD-PARTY-NOTICES.txt` as a stub with these sections (exact content structure):

    1. **Header**: "wisp — Third-Party Notices" + generated date + product version 0.1.0
    2. **Qt (LGPLv3)**: "This application uses Qt version 6.11.1, © The Qt Company Ltd and contributors, licensed under the GNU Lesser General Public License v3 (LGPLv3)." + statement that Qt is dynamically linked (the application and Qt remain separate works; user may re-link against their own Qt build — Phase 6 relink test)
    3. **Qt modules in use**: Qt Core, Qt Gui, Qt Quick, Qt QML (list exactly the components from find_package in CMakeLists.txt)
    4. **Source offer placeholder**: "[Source offer — TBD Phase 6: URL hosting the exact Qt 6.11.1 source used, or a bundled copy; valid 3+ years]" — marked clearly as placeholder, not a fake URL
    5. **Microsoft Visual C++ Runtime note**: "wisp is built with MSVC; the VC++ runtime is distributed per the Microsoft Visual C++ Redistributable license via VC_redist.x64.exe (shipped by the installer, Phase 6). windeployqt-copied compiler DLLs are NOT used for redistribution."
    6. **Footer**: "This file is a Phase 1 scaffold; final verification (relink test, source offer, clean-VM install) is a Phase 6 release gate (PITFALLS #8)."

    No invented URLs, no invented license texts — this is a STUB that names what must be completed.
  </action>
  <verify>
    <automated>Test-Path packaging/THIRD-PARTY-NOTICES.txt; Select-String -Path packaging/THIRD-PARTY-NOTICES.txt -Pattern "LGPL|6.11.1|dynamically linked"</automated>
  </verify>
  <acceptance_criteria>
    - packaging/THIRD-PARTY-NOTICES.txt exists
    - Contains the strings "LGPL", "6.11.1", "dynamically linked", "Qt Core", "Qt Gui", "Qt Quick", "Qt QML", and a clearly-marked source-offer placeholder (contains "TBD" or "placeholder" — no fabricated URL)
    - Contains an explicit statement that windeployqt-copied compiler DLLs are not redistributed (VC_redist is Phase 6)
  </acceptance_criteria>
  <done>LGPL compliance scaffold seeded — the release gate (Phase 6) has its artifact to complete instead of create</done>
</task>

<task type="auto">
  <name>task 2: Create deploy.ps1 — windeployqt --qmldir standalone-folder deployment</name>
  <files>
    deploy.ps1
  </files>
  <read_first>
    CMakePresets.json (release preset binaryDir build/release)
    @.planning/research/PITFALLS.md (#9 windeployqt gaps — --qmldir mandatory, clean-env launch test)
    @.planning/research/STACK.md (windeployqt usage, VC redist licensing)
  </read_first>
  <action>
    Create `deploy.ps1` at repo root:

    ```powershell
    # deploy.ps1 — produce a standalone, self-contained deploy folder
    param([string]$Config = "release")
    $ErrorActionPreference = "Stop"
    $qtBin   = "C:\Qt\6.11.1\msvc2022_64\bin"
    $windeployqt = Join-Path $qtBin "windeployqt.exe"
    $exe     = Join-Path (Join-Path "build" $Config) "wisp.exe"
    $outDir  = Join-Path (Join-Path "build" "deploy") "wisp"

    if (-not (Test-Path $exe)) { throw "Missing $exe — run build.ps1 -Config release first" }
    if (-not (Test-Path $windeployqt)) { throw "windeployqt not found at $windeployqt" }

    if (Test-Path $outDir) { Remove-Item -Recurse -Force $outDir }
    New-Item -ItemType Directory -Force -Path $outDir | Out-Null
    Copy-Item $exe $outDir
    Copy-Item "packaging\THIRD-PARTY-NOTICES.txt" $outDir

    # --qmldir is MANDATORY: without it windeployqt omits QtQuick modules (PITFALLS #9)
    & $windeployqt --release --qmldir qml --no-translations (Join-Path $outDir "wisp.exe")
    if ($LASTEXITCODE -ne 0) { throw "windeployqt failed with exit $LASTEXITCODE" }

    Write-Host "Deployed to $outDir"
    Write-Host "VC runtime: install VC_redist.x64.exe on target machines (Phase 6) — do not copy dev-machine msvcp/vcruntime DLLs."
    ```

    Then execute it and prove the folder runs standalone:
    - Run `powershell -ExecutionPolicy Bypass -File build.ps1 -Config release` first (Release build exists)
    - Run `powershell -ExecutionPolicy Bypass -File deploy.ps1`
    - Verify: `build/deploy/wisp/wisp.exe` exists, `build/deploy/wisp/Qt6Quick.dll` exists, `build/deploy/wisp/qml/QtQuick/` exists (the qml/ tree proves --qmldir worked)
    - Launch test with a CLEAN environment (simulates a machine without Qt on PATH): `powershell -NoProfile -Command "$env:Path='C:\Windows\System32;C:\Windows'; Start-Process 'build/deploy/wisp/wisp.exe'; Start-Sleep 3; Get-Process wisp -ErrorAction SilentlyContinue"` → process must be alive (window shown), then stop it.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File deploy.ps1; Test-Path build/deploy/wisp/Qt6Quick.dll; Test-Path build/deploy/wisp/qml/QtQuick; Test-Path build/deploy/wisp/THIRD-PARTY-NOTICES.txt</automated>
  </verify>
  <acceptance_criteria>
    - deploy.ps1 exists and contains `windeployqt` with `--qmldir qml` and `--release`
    - deploy.ps1 fails loudly (throw) when build/release/wisp.exe or windeployqt.exe is missing
    - Running deploy.ps1 produces build/deploy/wisp/ containing wisp.exe, Qt6Quick.dll, qml/QtQuick/, THIRD-PARTY-NOTICES.txt
    - Launching build/deploy/wisp/wisp.exe with a PATH containing only System32 stays alive ≥3s (no missing-DLL crash)
    - deploy.ps1 prints the VC_redist note (does not copy dev-machine MSVC DLLs)
  </acceptance_criteria>
  <done>Standalone deploy folder proven — the Phase 6 installer starts from this output instead of reinventing it</done>
</task>

<task type="auto">
  <name>task 3: Add debug-build frame-time perf guard (FrameTimeProbe) + Qt Test smoke test (tst_shell), wire into CMake</name>
  <files>
    src/app/FrameTimeProbe.h
    src/app/FrameTimeProbe.cpp
    src/app/main.cpp
    tests/tst_shell.cpp
    CMakeLists.txt
  </files>
  <read_first>
    src/app/main.cpp (from Plans 01-01/01-02 — probe wiring point)
    CMakeLists.txt (from Plans 01-01/01-02 — test target wiring)
    qml/Theme.qml + qml/MainWindow.qml (from Plan 01-02 — values the test asserts)
    @.planning/phases/01-core-shell/01-UI-SPEC.md (Interaction Contract perf guard)
  </read_first>
  <action>
    Create the perf guard:

    **src/app/FrameTimeProbe.h** — QObject with:
    - `explicit FrameTimeProbe(QQuickWindow* window, QObject* parent = nullptr)`
    - private slot `void onFrameRendered()`

    **src/app/FrameTimeProbe.cpp**:
    - `#include <QQuickWindow>`, `<QElapsedTimer>`, `<QDebug>`
    - Constructor: `connect(window, &QQuickWindow::afterRendering, this, &FrameTimeProbe::onFrameRendered);`
    - Members: `QElapsedTimer m_timer; qint64 m_lastMs = -1; int m_slowFrames = 0;`
    - `onFrameRendered()`: `qint64 now = m_timer.restart()`-style delta; if `m_lastMs >= 0`: `qint64 dt = now - m_lastMs; if (dt > 17) { ++m_slowFrames; qDebug() << "[perf] slow frame:" << dt << "ms (accumulated:" << m_slowFrames << ")"; }` ; `m_lastMs = now;`
    - Guard the whole logging with `#ifdef QT_DEBUG` so release builds carry zero cost (release path compiled out)

    **src/app/main.cpp** wiring (after window creation, `#ifdef QT_DEBUG`): construct `FrameTimeProbe` on the window with the engine's rootObjects().first() cast to QQuickWindow* — a local variable whose lifetime spans app.exec() (heap-allocate with `new` + no parent deletion issues is acceptable; or keep a `static`/local holder that outlives exec).

    **tests/tst_shell.cpp** — Qt Test smoke test:
    - `QTEST_MAIN`-style `class ShellTest : public QObject` with `Q_OBJECT`
    - Test 1 `windowContract()`: create `QQmlApplicationEngine`, `engine.loadFromModule("wisp", "MainWindow")` (link the wisp QML module — see CMake below); take `rootObjects().first()` cast to `QQuickWindow*`; QVERIFY non-null; QCOMPARE on `flags()` containing `Qt::Tool` and `Qt::FramelessWindowHint`; QCOMPARE `width()==672` / `height()==432` (Theme window canvas); QCOMPARE `title()=="wisp"`; QCOMPARE `color()` transparent (`QColor(Qt::transparent)`)
    - Test 2 `themeTokens()`: `QQmlComponent` loading `qml/Theme.qml` from the module (via engine or a `QQmlEngine` + `QQmlComponent` with `loadFromModule("wisp", "Theme")`); QCOMPARE property `surface` == `QColor("#1E1E1E")`, `animOpenDuration` == 150, `animCloseDuration` == 140, `easingOpen` == Easing.OutCubic value, `rowHeight` == 44, `radiusSurface` == 12
    - Note: if `loadFromModule` with a singleton needs the import in the test's module resolution, add a minimal `qmldir`/module wiring — the wisp module plugin target is linked below, so module resolution works

    **CMakeLists.txt** additions (after existing content):
    ```cmake
    include(CTest)
    if(BUILD_TESTING)
        qt_add_executable(tst_shell tests/tst_shell.cpp)
        target_link_libraries(tst_shell PRIVATE Qt6::Quick Qt6::Qml Qt6::Gui wisp_qmlmoduleplugin)
        add_test(NAME tst_shell COMMAND tst_shell)
    endif()
    ```
    (`wisp_qmlmoduleplugin` is the QML module plugin target generated by the existing `qt_add_qml_module(wisp ...)` — verify the actual target name with `cmake --build` output / presets if it differs, and use the correct one; the key requirement is the test links the wisp module so `loadFromModule("wisp", ...)` resolves.)

    Add `src/app/FrameTimeProbe.cpp` to the `qt_add_executable(wisp ...)` sources in CMakeLists.txt.

    Then: `powershell -ExecutionPolicy Bypass -File build.ps1` (debug, tests enabled) and run `ctest --test-dir build/dev --output-on-failure` → tst_shell passes.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File build.ps1; ctest --test-dir build/dev --output-on-failure 2>&1 | Select-String -Pattern "tst_shell|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/FrameTimeProbe.h/.cpp exist; FrameTimeProbe.cpp contains `afterRendering` and logs frames with `dt > 17` in `#ifdef QT_DEBUG` (release path compiled out)
    - src/app/main.cpp instantiates FrameTimeProbe on the QQuickWindow in `#ifdef QT_DEBUG`
    - tests/tst_shell.cpp exists with a windowContract test (flags Qt::Tool + FramelessWindowHint, 672×432, title "wisp", transparent color) and a themeTokens test (#1E1E1E, 150/140 durations, 44 row, 12 radius)
    - CMakeLists.txt contains `include(CTest)` and `add_test(NAME tst_shell COMMAND tst_shell)` and FrameTimeProbe.cpp in the wisp target sources
    - `ctest --test-dir build/dev --output-on-failure` reports tst_shell passing (0 failures)
  </acceptance_criteria>
  <done>Phase-1 performance contract has an automated guard (frame logging + contract test) — Phase 3/5 regressions get caught here</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy output | build/deploy/wisp/ — local staging folder; contents are copies of build artifacts + Qt DLLs + notices |
| Build/test environment | Debug build + ctest execute locally in the user session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | deploy.ps1 | mitigate | Deploys from verified local build (build/$Config/wisp.exe) only; fails loudly when exe/windeployqt missing; writes only under build/deploy/ |
| T-03-02 | I | THIRD-PARTY-NOTICES.txt | mitigate | Stub contains NO fabricated URLs/license text — source-offer placeholder explicitly marked TBD Phase 6 (avoiding a compliance misrepresentation) |
| T-03-03 | E | deploy output | accept | Local artifacts only; no admin rights used; VC runtime explicitly left to official VC_redist (no unlicensed DLL copying — PITFALLS #9/#8) |
| T-03-04 | S | tst_shell | accept | Test-only binary; reads Theme/MainWindow from the module; no external I/O |
</threat_model>

<verification>
1. `packaging/THIRD-PARTY-NOTICES.txt` exists with LGPL/dynamic-linking/Qt-module/placeholder sections
2. `powershell -ExecutionPolicy Bypass -File build.ps1 -Config release` then `powershell -ExecutionPolicy Bypass -File deploy.ps1` → build/deploy/wisp/ contains wisp.exe + Qt6Quick.dll + qml/QtQuick/ + THIRD-PARTY-NOTICES.txt
3. Deployed wisp.exe launches with a System32-only PATH (process alive ≥3s)
4. `ctest --test-dir build/dev --output-on-failure` passes (tst_shell: window contract + theme tokens)
5. Debug run of wisp.exe logs `[perf] slow frame:` entries for any frame >17ms during the open/close animations (VISU-01 60fps evidence — no such entries on a healthy machine)
</verification>

<success_criteria>
- [ ] LGPL scaffold in place: THIRD-PARTY-NOTICES.txt stub (dynamic-linking lock, Qt 6.11.1 + module list, honest placeholders)
- [ ] deploy.ps1 produces a standalone folder (windeployqt --release --qmldir qml) proven to launch without Qt on PATH
- [ ] Debug frame-time guard exists: >17ms frames logged during animation (VISU-01 60fps contract has evidence)
- [ ] tst_shell Qt Test passes — window contract + Theme.qml tokens pinned
- [ ] Phase 1 five success criteria from ROADMAP all demonstrable: (1) centered frameless widget, (2) 150-200ms 60fps open animation, (3) Escape reverse-animation dismiss, (4) crisp on mixed-DPI via Qt PMv2 (no manual DPI calls), (5) deploy folder standalone + LGPL scaffold exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-shell/01-03-SUMMARY.md`
</output>
