---
phase: 12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - qml/ShortcutsWindow.qml
  - src/ui/SettingsWindow.h
  - src/ui/SettingsWindow.cpp
  - qml/SettingsWindow.qml
  - qml/Theme.qml
  - CMakeLists.txt
  - tests/tst_shell.cpp
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can open a themed keyboard-shortcuts reference window from the Settings window"
    - "ShortcutsWindow lists all 11 app shortcuts with label, description, and keycap chips"
    - "ShortcutsWindow is a frameless Tool window matching SettingsWindow/ColorDialog chrome"
    - "Settings window shows a 'Show shortcuts' button that opens ShortcutsWindow"
    - "ShortcutsWindow stays open when Settings window is active (anotherOfOurWindowsIsActive exemption)"
    - "Settings Scan and Updates sections use header-on-top layout with the Show-shortcuts row fitting without scroll"
    - "All 23 tests pass (ctest green), including the ShortcutsWindow geometry and openShortcuts handoff assertions"
  artifacts:
    - path: "qml/ShortcutsWindow.qml"
      provides: "Themed sub-window listing 11 keyboard shortcuts as label+description+keycap rows"
      contains: "property var shortcuts"
    - path: "src/ui/SettingsWindow.h"
      provides: "openShortcuts() Q_INVOKABLE declaration and m_shortcutsWindow member"
      contains: "openShortcuts"
    - path: "src/ui/SettingsWindow.cpp"
      provides: "openShortcuts() lazy-load implementation via QQmlComponent module import"
      contains: "ShortcutsWindow"
    - path: "qml/SettingsWindow.qml"
      provides: "Show-shortcuts row (64px) and openShortcuts() QML bridge function"
      contains: "openShortcuts"
    - path: "qml/Theme.qml"
      provides: "Shortcuts and settings geometry tokens (shortcutsWindow*, settingsWindowHeight 864, settingsRowShortcuts)"
      contains: "shortcutsWindowWidth"
    - path: "CMakeLists.txt"
      provides: "ShortcutsWindow.qml registered in QML_FILES"
      contains: "ShortcutsWindow.qml"
    - path: "tests/tst_shell.cpp"
      provides: "Settings 480x864 geometry, ShortcutsWindow 560x580 geometry, openShortcuts invokeMethod + reuse assertions"
      contains: "openShortcuts"
  key_links:
    - from: "qml/SettingsWindow.qml"
      to: "src/ui/SettingsWindow.cpp"
      via: "openShortcuts() QML bridge → settingsController.openShortcuts() Q_INVOKABLE"
      pattern: "openShortcuts"
    - from: "src/ui/SettingsWindow.cpp"
      to: "qml/ShortcutsWindow.qml"
      via: "QQmlComponent module import lazy-load (openColorDialog precedent)"
      pattern: "ShortcutsWindow"
    - from: "qml/SettingsWindow.qml"
      to: "qml/Theme.qml"
      via: "settingsWindowHeight/width, settingsRowShortcuts, settingsSurfaceHeight/width token references"
      pattern: "settingsWindowHeight|settingsRowShortcuts"
    - from: "qml/ShortcutsWindow.qml"
      to: "qml/Theme.qml"
      via: "shortcutsWindowWidth/Height, shortcutsSurfaceWidth/Height, shortcutRowHeight, shortcutRowGap token references"
      pattern: "shortcutsWindow"
    - from: "tests/tst_shell.cpp"
      to: "src/ui/SettingsWindow.cpp"
      via: "settings.openShortcuts() + invokeMethod('openShortcuts') on QML window"
      pattern: "openShortcuts"
---

<objective>
Document the shipped Phase 12 implementation (shortcuts reference window + Settings polish) as a verifiable plan so /gsd-execute-phase and /gsd-verify-work can audit the phase against its design contract.

Purpose: The phase is already implemented and 23/23 ctest green. This plan records the shipped shape so downstream verification tools can confirm every decision (D-01..D-16) is satisfied in code.
Output: Verification that ShortcutsWindow, SettingsWindow changes, Theme tokens, CMake registration, and tests all match the UI-SPEC design contract.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche/12-CONTEXT.md
@.planning/phases/12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche/RESEARCH.md
@.planning/phases/12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche/12-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/ui/SettingsWindow.h:
```cpp
class SettingsWindow : public QObject {
    Q_INVOKABLE void openShortcuts();  // Phase 12: ShortcutsWindow reference list
    // ...
private:
    QPointer<QQuickWindow> m_shortcutsWindow; // Phase 12: ShortcutsWindow reference
};
```

From qml/ShortcutsWindow.qml:
```qml
Window {
    property var settingsController: null
    property var shortcuts: [
        { label: "Open wisp", description: "Show or hide the launcher from anywhere", keys: ["Alt", "Space"] },
        // ... 11 rows total
    ]
    function closeWindow() { root.hide(); root.close() }
}
```

From qml/SettingsWindow.qml:
```qml
Window {
    property var settingsController: null
    function openShortcuts() {
        if (settingsController) settingsController.openShortcuts()
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify ShortcutsWindow.qml matches UI-SPEC design contract</name>
  <files>qml/ShortcutsWindow.qml, qml/Theme.qml</files>
  <read_first>qml/ShortcutsWindow.qml, qml/Theme.qml, .planning/phases/12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche/12-UI-SPEC.md</read_first>
  <action>
Verify the shipped ShortcutsWindow.qml and Theme.qml tokens against the design contract. Specifically confirm these shipped invariants:

1. **Window geometry (D-13):** Window uses `Theme.shortcutsWindowWidth` (560) and `Theme.shortcutsWindowHeight` (580); surface uses `Theme.shortcutsSurfaceWidth` (528) and `Theme.shortcutsSurfaceHeight` (548). All four tokens exist in Theme.qml.

2. **Window chrome (D-02):** `flags: Qt.Tool | FramelessWindowHint`, `color: "transparent"`, title `"wisp — keyboard shortcuts"`, shadow Image anchors.fill parent, surface Rectangle anchors.centerIn parent.

3. **Drag header + close X:** Drag header 32px with `startSystemMove()`, title text "Keyboard shortcuts". Close button top-right: `removeButtonSize` square, `\uE711` MDL2 glyph, hover → `surfaceSecondary` bg + `textPrimary` color.

4. **Shortcut data (D-04):** `property var shortcuts` array with exactly 11 entries, each `{label, description, keys[]}`. Verify all 11 labels match the UI-SPEC list: "Open wisp", "Run selection", "Open containing folder", "Run as administrator", "Quick-select result", "Navigate results", "Page through results", "First / last result", "Show hidden entries", "Run a shell command", "Close launcher".

5. **Row layout (D-15, D-16):** Row height = `Theme.shortcutRowHeight` (42px), gap = `Theme.shortcutRowGap` (2px). Keycap chip height = `Theme.spaceSm * 2 + Theme.fontSizeKeycap` (28px) — NOT `parent.height`. Keycaps use `anchors.right: parent.right` (not implicit-width arithmetic).

6. **Hairlines (D-10):** `Theme.separator` rectangle under every row except the last (`visible: index < root.shortcuts.length - 1`).

7. **ScrollView:** scrollbars off (`ScrollBar.horizontal.policy: ScrollBar.AlwaysOff`, `ScrollBar.vertical.policy: ScrollBar.AlwaysOff`), clip true.

8. **Esc close:** `Keys.onEscapePressed` on surface calls `root.closeWindow()`.

9. **Center on open:** `Component.onCompleted` centers via `Screen.availableX/Y + availableWidth/Height`.

10. **settingsController injection:** `property var settingsController: null` (not readonly — per D-03 precedent).
  </action>
  <verify>
    <automated>cd C:\Users\Trick\Desktop\Rofi-Windows && rg -n "shortcutsWindowWidth|shortcutsWindowHeight|shortcutsSurfaceWidth|shortcutsSurfaceHeight|shortcutRowHeight|shortcutRowGap" qml/Theme.qml | rg "readonly property int"</automated>
  </verify>
  <done>All 10 invariants hold: geometry tokens match UI-SPEC (560x580 window, 528x548 surface, 42px rows, 2px gaps, 28px chips), 11 shortcut entries with correct labels, frameless Tool chrome, drag header + close X, hairlines under all-but-last, ScrollView with scrollbars off, Esc close, settingsController injected as writable property.</done>
</task>

<task type="auto">
  <name>task 2: Verify SettingsWindow C++ wiring and QML Show-shortcuts row</name>
  <files>src/ui/SettingsWindow.h, src/ui/SettingsWindow.cpp, qml/SettingsWindow.qml, CMakeLists.txt</files>
  <read_first>src/ui/SettingsWindow.h, src/ui/SettingsWindow.cpp, qml/SettingsWindow.qml, CMakeLists.txt</read_first>
  <action>
Verify the C++ controller wiring and QML Settings surface against the design contract:

**SettingsWindow.h:**
1. `Q_INVOKABLE void openShortcuts()` is declared (line ~80).
2. `QPointer<QQuickWindow> m_shortcutsWindow` member exists (line ~149), initial value is null (implicit QPointer default).
3. `anotherOfOurWindowsIsActive()` is a private method (D-05 — exemptions any active top-level window).

**SettingsWindow.cpp — openShortcuts() (lines 421-444):**
4. Lazy-load pattern matches openColorDialog(): `QQmlComponent` with `"import wisp\nShortcutsWindow {\n}"` and `qrc:/qt/qml/wisp/ShortcutsWindow.qml` URL.
5. `component.beginCreate` + `setProperty("settingsController", QVariant::fromValue(this))` + `completeCreate` — per-instance injection (D-03).
6. `qobject_cast<QQuickWindow *>(winObj)` → store in `m_shortcutsWindow`.
7. Subsequent calls reuse the cached `m_shortcutsWindow` (no re-create).
8. `centerOnPrimary(m_shortcutsWindow)` + `show()` + `requestActivate()`.

**SettingsWindow.qml — Show-shortcuts row (lines 1020-1077):**
9. Row height = `Theme.settingsRowShortcuts` (64px, D-13/D-14).
10. Left column: "Shortcuts" title (fontSizeTitle) + "Every key wisp understands" subtitle (fontSizeSubtitle).
11. Right: accent "Show shortcuts" button — `accentDark` hover, `onAccentText` color, `settingsRowScanItem` height.
12. Button onClicked → `root.openShortcuts()` bridge function (line 94-97): null-guards `settingsController.openShortcuts()`.
13. Hairline separator (`Theme.separator`) at top of the row.
14. The 6 rows total: hotkey(64) + accent(88) + autostart(64) + scan(200) + updates(160) + shortcuts(64) = 640; 5 gaps × 12 = 60; 8 top + 24 bottom = 732 within 768 available (832 surface − 32 drag − 32 margins). No scroll needed (D-06).

**Settings polish (D-08..D-12):**
15. Scan section: header-on-top with `settingsSectionHeader` (32px), title "Scan locations", subtitle "Folders wisp searches for files and folders" (D-11).
16. Add-folder button always visible in scan header, right-aligned, accent fill (D-12).
17. Root list height-capped: `Math.min(roots.length, 2) * settingsRowScanItem` (D-07 — no full-height ListView).
18. Remove button: bordered well with `hoverBg` fill + `dangerText`/`dangerBorder` on hover (D-09).
19. Empty placeholder: "No folders yet" (D-11).
20. Hairlines under every scan-root entry but the last (D-10).
21. Updates section: header-on-top with "Updates" + "Keep wisp current" subtitle.

**CMakeLists.txt:**
22. `qml/ShortcutsWindow.qml` is listed in `QML_FILES` (line ~94).

**Theme.qml tokens:**
23. `settingsWindowHeight: 864` and `settingsSurfaceHeight: 832` (D-14, grew from 812/780).
24. `settingsRowShortcuts: 64` exists.
25. `settingsSectionHeader: 32` exists.
26. All `shortcutsWindow*` and `shortcutsSurface*` tokens exist with correct values.
  </action>
  <verify>
    <automated>cd C:\Users\Trick\Desktop\Rofi-Windows && rg -c "openShortcuts" src/ui/SettingsWindow.cpp src/ui/SettingsWindow.h qml/SettingsWindow.qml && rg -n "ShortcutsWindow.qml" CMakeLists.txt && rg -n "settingsWindowHeight: 864|settingsSurfaceHeight: 832|settingsRowShortcuts: 64" qml/Theme.qml</automated>
  </verify>
  <done>All 26 invariants hold: openShortcuts() lazy-load + controller injection wired identically to openColorDialog, Show-shortcuts row renders at 64px with accent button, Settings vertical budget fits within 768 available without scroll, Scan/Updates use header-on-top, CMake registers ShortcutsWindow.qml, Theme tokens match UI-SPEC geometry (480x864 window, 560x580 shortcuts window).</done>
</task>

<task type="auto">
  <name>task 3: Verify tests pass — geometry, invocation, and reuse assertions</name>
  <files>tests/tst_shell.cpp</files>
  <read_first>tests/tst_shell.cpp</read_first>
  <action>
Verify the test file covers all Phase 12 verification requirements:

**settingsWindowContract test (lines 97-174):**
1. Settings window loads and shows via `settings.open()` → `findWindowByTitle("wisp — settings")`.
2. Settings geometry: `QCOMPARE(settingsWin->width(), 480)` and `QCOMPARE(settingsWin->height(), 864)` — the Phase 12 grown size (D-14).
3. CR-01: `invokeMethod(settingsWin, "openHotkeyCapture")` → capture dialog shows.
4. CR-02: hide + re-open capture dialog → still visible (reuse guarantee).
5. **Phase 12 ShortcutsWindow:** `invokeMethod(settingsWin, "openShortcuts")` → `findWindowByTitle("wisp — keyboard shortcuts")` exists and is visible.
6. ShortcutsWindow geometry: `QCOMPARE(shortcutsWin->width(), 560)` and `QCOMPARE(shortcutsWin->height(), 580)` (D-13).
7. **ShortcutsWindow reuse (CR-02 analog):** hide + `settings.openShortcuts()` → shortcutsWin is visible again (single-instance lazy-load guarantee).

**Run ctest to confirm all 23 tests pass:**
8. Execute `ctest --test-dir build/dev --output-on-failure` and confirm 23/23 green.
  </action>
  <verify>
    <automated>cd C:\Users\Trick\Desktop\Rofi-Windows && ctest --test-dir build/dev --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <done>All Phase 12 test assertions present: settings 480x864, shortcuts 560x580, openShortcuts invokeMethod succeeds, shortcuts window visible after invocation, reuse on second invocation, ctest 23/23 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML ↔ C++ controller injection | settingsController is set via setProperty — the QML surface null-guards every call; no trust boundary escalation (display-only window) |
| User input in ShortcutsWindow | Zero user input — static display window, no text fields, no actions beyond close. No attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ShortcutsWindow shortcuts array | accept | Static QML data, not user-editable; no persistence, no injection vector |
| T-12-02 | Information Disclosure | Shortcut reference window | accept | Public keyboard shortcuts are intentional disclosure; no PII |
| T-12-03 | Elevation of Privilege | openShortcuts() lazy-load | accept | QQmlComponent with module import (not arbitrary URL); same pattern as openColorDialog, already shipped in Phase 6 |
</threat_model>

<verification>
Phase 12 verification checklist:

1. `grep -c "openShortcuts" src/ui/SettingsWindow.cpp` returns ≥ 1
2. `grep -c "m_shortcutsWindow" src/ui/SettingsWindow.h` returns ≥ 1
3. `grep -c "ShortcutsWindow.qml" CMakeLists.txt` returns ≥ 1
4. `grep -c "shortcutsWindowWidth" qml/Theme.qml` returns ≥ 1
5. `grep -c "settingsRowShortcuts" qml/Theme.qml` returns ≥ 1
6. `grep -c "openShortcuts" qml/SettingsWindow.qml` returns ≥ 2 (function + bridge)
7. `grep -c "settingsWindowHeight: 864" qml/Theme.qml` returns 1
8. `ctest --test-dir build/dev --output-on-failure` → 23/23 pass
9. No deferred ideas implemented: no empty-query hint row, no help/? command, no tray Shortcuts item
</verification>

<success_criteria>
- ShortcutsWindow.qml exists with 11 shortcut rows, 560x580 geometry, frameless Tool chrome, keycap chips, hairlines
- SettingsWindow.cpp::openShortcuts() lazy-loads ShortcutsWindow via QQmlComponent module import with controller injection
- SettingsWindow.qml shows 64px "Show shortcuts" row with accent button
- Theme.qml has shortcutsWindow* and settingsWindowHeight 864 tokens
- CMakeLists.txt registers ShortcutsWindow.qml in QML_FILES
- tests/tst_shell.cpp covers openShortcuts invocation, 560x580 geometry, and reuse assertion
- ctest 23/23 green
- No deferred ideas present (D-01 rejection of empty-query hint row and help command honored)
</success_criteria>

<output>
After completion, create `.planning/phases/12-show-keyboard-shortcuts-help-empty-query-hint-row-in-launche/12-01-SUMMARY.md`
</output>
