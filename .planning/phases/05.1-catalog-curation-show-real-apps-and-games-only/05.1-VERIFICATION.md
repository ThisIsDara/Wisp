---
phase: 05.1-catalog-curation-show-real-apps-and-games-only
verified: 2026-08-11T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 05.1: Catalog Curation — Verification Report

**Phase Goal:** The default catalog shows only real apps and games the user actually uses — rule-hidden noise is absent by default, a persisted per-row Hide/Unhide prunes whatever the rules miss, hidden entries stay recoverable via a "Show hidden (N)" mode, and the "Add executable…" path remains the escape hatch.
**Verified:** 2026-08-11
**Status:** passed
**Re-verification:** No — initial verification (no prior VERIFICATION.md existed in the phase directory)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CUR-01: Default catalog shows only real apps/games; noise absent by default; marking runs after dedupe (no UWP-twin resurrection) | ✓ VERIFIED | Allowlist semantics in `CurationRules::matches` (CurationRules.cpp:227-251 — not-on-allowlist → hidden); pipeline order dedupe → markCurated → sort (AppCatalog.cpp:192-195); `markingAfterDedupe_Pitfall1` (tst_catalog.cpp:259-278); `nonAllowlistedHidden_CUR01` covers all four measured noise buckets (tst_curation.cpp:151-194) |
| 2 | CUR-02: Per-row Hide persists across restarts and rebuilds; inert keys; user overrides beat rules (shown wins) | ✓ VERIFIED | CurationStore QSettings INI persistence (CurationStore.cpp:23-46, sync after every write); `hidePersistsAcrossInstances_CUR02` + `lastActionWins` (tst_curation.cpp:233-267); `rebuildRemarkFromStore_CUR02` proves re-mark on rebuild AND shown-overrides-hiddenIds (tst_catalog.cpp:284-316); `uninstalledHiddenIdInert_CUR02` (tst_catalog.cpp:321-335) |
| 3 | CUR-03: Hidden entries excluded from search by default; "Show hidden (N)" reveal mode with dimmed rows; Ctrl+H hide/unhide; query preserved on hide | ✓ VERIFIED | Visibility branch in ALL three order-building sites (ResultsModel.cpp:48, 105-106, 120-121); showHidden Q_PROPERTY + setShowHidden (ResultsModel.h:27, ResultsModel.cpp:332-350); footer gated on `hiddenCount > 0` (MainWindow.qml:547); Ctrl+H case (MainWindow.qml:133-144); dimming via `model.isHidden ? Theme.disabledOpacity : Theme.fullOpacity` (ResultsRow.qml:29); `hidePreservesQuery_Pitfall3` (tst_model.cpp:716-734); `showHiddenToggleReveals_CUR03` (tst_model.cpp:637-664) |
| 4 | CUR-04: User-added executables (Source::File) structurally immune to curation; escape hatch intact | ✓ VERIFIED | Triple guard: `matches()` returns false for File (CurationRules.cpp:231-232); markCurated skips File (AppCatalog.cpp:72-73); hideSelected/unhideSelected File no-op (ResultsModel.cpp:369-370, 409-410); plus dedupeLnkOverUwp File passthrough (AppCatalog.cpp:45-48 — review-driven fix); tested in `fileRowsNeverCurated_CUR01/CUR04` + `hideSelectedNoOpOnFile_CUR04` |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/core/CurationRules.{h,cpp}` | Pure allowlist matcher, ~280 tokens, File immune | ✓ VERIFIED | 149 curated names across all promised buckets (launchers, games, messengers, media, browsers, dev tools, VPNs, utilities — CurationRules.cpp:44-204) → 298 pre-normalized tokens incl. apostrophe-stripped variants (M-02 fix, lines 213-223) |
| `src/core/CurationStore.{h,cpp}` | QSettings INI persistence, last-action-wins, empty-id guard, INI-safe ids, sync error surfacing | ✓ VERIFIED | writeOverride (CurationStore.cpp:33-46): empty guard, `isIniSafe` (= [ ] rejection, L-04 fix), opposite-group removal, `sync()` per write, `qWarning` on status != NoError (L-03 fix) |
| `src/core/AppCatalog.{h,cpp}` | CurationData/CurationSource seam; markCurated post-dedupe pre-sort; File passthrough | ✓ VERIFIED | AppCatalog.h:58-63; AppCatalog.cpp:69-79 (shownIds > hiddenIds > rules), pipeline line 193-195 |
| `src/core/ResultsModel.{h,cpp}` | IsHiddenRole, showHidden+NOTIFY, hide/unhide query-preserving, hiddenCount+NOTIFY, HideStore seam | ✓ VERIFIED | ResultsModel.h:27-28, 72-80; hideSelected marks ALL same-id rows (no break — M-01 fix, ResultsModel.cpp:384-392); unhideSelected no-ops on visible rows (L-01 fix, :414-415); NO setEntries/ensureFresh on the hide path — verified by reading |
| `src/app/main.cpp` | Single CurationStore; both seams to the same instance; ensureFresh count unchanged | ✓ VERIFIED | Line 57 construct; lines 105-110 both lambdas capture `&curationStore`; exactly one ensureFresh (line 156, visibleChanged handler) |
| `qml/MainWindow.qml` | Ctrl+H, showHiddenRow footer, in-window ctxMenu + dismissal catcher | ✓ VERIFIED | Lines 133-144, 545-578, 625-699; catcher is a SIBLING below the menu (z: Theme.catcherZ=15 < Theme.menuZ=20, H-01 fix); background click-blocker (H-02 fix); closes on Escape/typing/reopen (M-03 fix: lines 51, 400, 810) |
| `qml/ResultsRow.qml` | isHidden dimming; right-click → contextMenuRequested | ✓ VERIFIED | Line 29 (Theme tokens, no literals — L-06 fix); signal line 230, emitted with ListView-space coords line 246-247 |
| `qml/Theme.qml` | disabledOpacity + menu tokens + z ordering | ✓ VERIFIED | Lines 35-38, 78-81 |
| `tests/tst_curation.cpp` | Store/rules suite | ✓ VERIFIED | 10 slots (superset of the claimed 7), all four requirements covered |
| `tests/tst_catalog.cpp` | 4 curation integration slots | ✓ VERIFIED | markingAfterDedupe_Pitfall1, rebuildRemarkFromStore_CUR02, uninstalledHiddenIdInert_CUR02, fileRowsNeverCurated_CUR04 |
| `tests/tst_model.cpp` | 8 curation slots incl. review regressions | ✓ VERIFIED | showHiddenToggleReveals_CUR03, unhideWritesShownIds_CUR03, hideSelectedNoOpOnFile_CUR04, hidePreservesQuery_Pitfall3, hideMarksNotRemoves_CUR02, hideMarksAllSameIdRows_M01, unhideNoOpOnVisibleRow_L01, hiddenCountCountsAllHidden_CUR02 |
| `tests/tst_shell.cpp` | QML load gate (new wiring type-compiles) | ✓ VERIFIED | windowContract loads MainWindow.qml (tst_shell.cpp:19-21); Theme tokens asserted at 680 (post-b98dbe5) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| CurationStore (main.cpp:57) | AppCatalog::setCurationSource | lambda capture `&curationStore` returning `CurationData{hiddenIds(), shownIds()}` | ✓ WIRED | main.cpp:105-107; read on UI thread at onBuildFinished (WR-01) |
| CurationStore | ResultsModel::setHideStore | lambda `hidden ? hide(id) : show(id)` | ✓ WIRED | main.cpp:108-110 — SAME instance as the catalog seam |
| ResultsModel::hideSelected | CurationStore::hide | HideStore seam | ✓ WIRED | ResultsModel.cpp:374-375; StoreSpy tests prove the call |
| markCurated | CurationRules::matches | direct call, shownIds checked first | ✓ WIRED | AppCatalog.cpp:74-77 |
| MainWindow ctxMenu | ResultsModel::hideSelected/unhideSelected | ctxItem onClicked | ✓ WIRED | MainWindow.qml:690-696 |
| ResultsRow right-click | MainWindow ctxMenu | contextMenuRequested signal (index, isHidden, x, y) | ✓ WIRED | ResultsRow.qml:247 → MainWindow.qml:436 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| ResultsModel::setEntries | hidden flags | markCurated via catalog snapshot (rules + CurationStore id sets) | ✓ FLOWING | Real store reads at every build (main.cpp:105-107); rebuildRemarkFromStore_CUR02 proves re-mark parity |
| hiddenCount | m_entries hidden flags | real curated snapshot | ✓ FLOWING | NOTIFY emitted at every mutation site (setEntries :72, hideSelected :403, unhideSelected :434) |
| Footer label | resultsModel.hiddenCount | Q_PROPERTY + NOTIFY | ✓ FLOWING | MainWindow.qml:571-573; QML binding re-evaluates on NOTIFY (the 5cfaf80 fix) |
| Persistence round-trip | curationHidden/curationShown INI groups | real temp INI in tests; %APPDATA% in production | ✓ FLOWING | hidePersistsAcrossInstances_CUR02 (tst_curation.cpp:233-246) |

### Behavioral Spot-Checks

Skipped — the verification instruction for this phase explicitly forbids running builds; the suite is documented green 17/17 and the user-approved human checkpoint already exercised the live behaviors (catalog, hide/unhide, footer, persistence, escape hatch, right-click menu). Static verification above confirms every claimed behavior is present in code.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| CUR-01 | 05.1-01/02 | Default catalog shows only real apps and games; noise absent by default; marking after dedupe | ✓ SATISFIED | CurationRules.cpp:227-251; AppCatalog.cpp:69-79, 192-195; tst_curation.cpp:151-194; tst_catalog.cpp:259-278 |
| CUR-02 | 05.1-01/02/03 | Hide persists across restarts/rebuilds; inert keys; user overrides win | ✓ SATISFIED | CurationStore.cpp:33-46; AppCatalog.cpp:74-77; tst_curation.cpp:233-267; tst_catalog.cpp:284-335 |
| CUR-03 | 05.1-03/04 | Hidden excluded by default; Show hidden (N) mode; Ctrl+H; query preserved | ✓ SATISFIED | ResultsModel.cpp:48, 105-106, 120-121, 366-434; MainWindow.qml:133-144, 545-578; tst_model.cpp:637-734 |
| CUR-04 | 05.1-01/02/03 | File rows structurally immune to curation | ✓ SATISFIED | CurationRules.cpp:231-232; AppCatalog.cpp:45-48, 72-73; ResultsModel.cpp:369-370, 409-410; tst_catalog.cpp:339-362; tst_model.cpp:696-714 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | TODO/FIXME/HACK/placeholder-stub scans clean across all 7 phase files | — | — |

The only "placeholder" hits are the legitimate search-field placeholderText and icon-placeholder comments; `return {}` hits are invalid-index guards in `data()`/`snapshotSelected()`.

### Human Verification Required

None outstanding — the phase's blocking human checkpoint was performed and approved (user verified: curated catalog contents, Ctrl+H hide/unhide, Show-hidden footer, persistence across restarts, Add-executable escape hatch, right-click context menu), documented in 05.1-04-SUMMARY.md ("Human checkpoint: approved"). All always-human items (visual appearance, animation feel, right-click interaction) were covered by that session.

## Gaps Summary

No gaps found. All four requirements (CUR-01..CUR-04) are delivered end-to-end in the code:

- **Pipeline:** markCurated runs strictly after dedupeLnkOverUwp and before sortAlphabetical (AppCatalog.cpp:193-195); shownIds beats hiddenIds beats default rules (:75-77); File rows pass through untouched.
- **Persistence:** CurationStore writes to the wisp INI with last-action-wins, empty-id and INI-hostile-char guards, sync-after-every-write, and status surfacing (CurationStore.cpp:33-46).
- **Model:** all three order-building sites apply the hidden skip; hide/unhide preserve the query (no setEntries/ensureFresh — verified by reading); all same-id rows marked; unhide no-ops on visible rows; hiddenCount is a NOTIFY property emitted at every mutation site.
- **Wiring:** both seams bind the same CurationStore instance; ensureFresh count unchanged (1).
- **UI:** Ctrl+H, hiddenCount-gated footer with live label, in-window overlay menu with sibling dismissal catcher (z 15 < 20), background click-blocker, Escape/typing/hide dismissal.
- **Review fixes (f4c4d8d):** all critical (H-01, H-02) and medium (M-01, M-02, M-03) findings verified fixed in code, with regression tests added for M-01 and L-01 (hideMarksAllSameIdRows_M01, unhideNoOpOnVisibleRow_L01).

**Documented deviations (not gaps, both user-approved):**
1. ROADMAP goal's "blocklist-over-allowlist" mechanism was replaced by a curated ~280-token ALLOWLIST at the user's explicit checkpoint feedback ("around 200-300 handpicked automatically"). The goal's outcome — only real apps and games visible by default — is preserved and user-verified.
2. L-05 (short common-word tokens like "word", "edge" widen the allowlist) remains per the review's own disposition: "Acceptable as an allowlist tradeoff (checkpoint-approved)".

**Informational (accepted residual):** keyboard nav (↑/↓) can move the selection while the context menu is open, so the menu label may describe a different row than the new selection; the menu action then operates on the visibly-ticked row (closeMenu + hideSelected on current selection). The M-03 lifecycle fixes (typing/Escape/reopen/outside-press) are all in place; this residual was within the review's accepted scope and the user approved the shipped behavior.

---

_Verified: 2026-08-11_
_Verifier: OpenCode (gsd-verifier)_
