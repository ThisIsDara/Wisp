---
phase: 05.1
slug: catalog-curation-show-real-apps-and-games-only
status: planned
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-11
---

# Phase 05.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Qt Test (Qt6::Test) via CTest |
| **Config file** | CMakeLists.txt (`BUILD_TESTING`, `qt_add_executable(tst_*)`) |
| **Quick run command** | `ctest --test-dir build/dev -R "tst_catalog|tst_model|tst_curation" --output-on-failure` |
| **Full suite command** | `ctest --test-dir build/dev --output-on-failure` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `ctest --test-dir build/dev -R "tst_catalog|tst_model|tst_curation" --output-on-failure`
- **After every plan wave:** Run `ctest --test-dir build/dev --output-on-failure`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~30 seconds

---

## Per-task Verification Map

| task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-01-01 | 01 | 1 | CUR-01..04 | — | N/A (scaffold) | unit | `.\build.ps1` stays green + grep gates (tst_curation not yet registered) | ❌ W0 new | pending |
| 05.1-01-02 | 01 | 1 | CUR-01, CUR-02 | T-05.1-01 | id keys constrained (targetPath/aumid, empty rejected); values = literal `1` | unit | `.\build.ps1 && ctest --test-dir build/dev -R tst_curation --output-on-failure` | ❌ W0 new | pending |
| 05.1-01-03 | 01 | 1 | CUR-01, CUR-02 | — | — | unit | `.\build.ps1 && ctest --test-dir build/dev --output-on-failure` (17 suites) | — | pending |
| 05.1-02-01 | 02 | 2 | CUR-01, CUR-02, CUR-04 | — | — | unit | `.\build.ps1` RED (tst_catalog fails to compile — CurationData missing) | ✅ extends | pending |
| 05.1-02-02 | 02 | 2 | CUR-01, CUR-02, CUR-04 | T-05.1-03 | markCurated = QSet::contains + bool OR only; UI-thread value-copy; worker never touches store (WR-01) | unit | `.\build.ps1 && ctest --test-dir build/dev -R "tst_catalog\|tst_curation" --output-on-failure` | ✅ extends | pending |
| 05.1-03-01 | 03 | 2 | CUR-02, CUR-03, CUR-04 | — | — | unit | `.\build.ps1` RED (tst_model fails to compile — IsHiddenRole missing) | ✅ extends | pending |
| 05.1-03-02 | 03 | 2 | CUR-02, CUR-03, CUR-04 | T-05.1-05 | hide/unhide take NO parameters — ids derived internally; store seam receives only internal ids | unit | `.\build.ps1 && ctest --test-dir build/dev -R tst_model --output-on-failure` + full suite | ✅ extends | pending |
| 05.1-04-01 | 04 | 3 | CUR-02, CUR-03 | — | — | unit | `.\build.ps1` + grep gates (setCurationSource/setHideStore in main.cpp) | ✅ exists | pending |
| 05.1-04-02 | 04 | 3 | CUR-03 | — | — | unit | `.\build.ps1 && ctest --test-dir build/dev -R tst_shell --output-on-failure` (QML syntax gate) + full suite | ✅ exists | pending |
| 05.1-04-03 | 04 | 3 | CUR-01..04 | — | — | manual | human checkpoint (reference-list pass, hide/restart/unhide, add-exe escape) | — | pending |

*Status: ? pending ● green ● red ● flaky*

---

## Wave 0 Requirements

- [x] `tests/tst_curation.cpp` — curation-rule + hide-persistence tests (fake scanners via AppCatalog::setScanners, SettingsStore test precedent per tst_settings.cpp) — **created in 05.1-01-01**
- [x] extended `tests/tst_catalog.cpp` — filter applied after dedupe, hidden entries excluded from swap — **created in 05.1-02-01**
- [x] extended `tests/tst_model.cpp` — hide action live-removes row, query preserved — **created in 05.1-03-01**

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Default catalog shows ~the ~60-app reference list, no noise buckets | CONTEXT.md decisions | Requires real Start Menu (262 .lnk) | Launch wisp, open launcher, empty query — verify apps/games present, uninstallers/toolchains/system utils absent |
| Per-row Hide + "Show hidden (N)" footer | CONTEXT.md decisions | QML interaction | Ctrl+H on a row; row vanishes; footer appears; relaunch (fresh catalog build) — still hidden |
| "Add executable…" escape hatch still works | Phase 4 (D-06/D-10/D-11) | Native dialog + QML | Add an exe, confirm it appears and survives rebuild |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** {pending / approved 2026-08-11}
