---
phase: 07-self-managed-file-scan
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/ResultsModel.cpp
  - tests/tst_model.cpp
autonomous: true
requirements: [LAUN-02, LAUN-03]
must_haves:
  truths:
    - "A scanned .exe whose case-folded path matches a catalog/tracked/added row's targetPath renders exactly once — the app row wins (D-03)"
    - "UWP rows (empty targetPath) never suppress a scan row with the same display name (Pitfall 10)"
    - "Dedupe is case-insensitive on the normalized path (T-07-03), and paths from different sources are compared consistently"
  artifacts:
    - path: "src/core/ResultsModel.cpp"
      provides: "D-03 path-set dedupe in mergeFiles"
      contains: "appPaths"
    - path: "tests/tst_model.cpp"
      provides: "D-03 dedupe test cases"
      contains: "scanRowSuppressed"
  key_links:
    - from: "src/core/ResultsModel.cpp"
      to: "m_fileEntries"
      via: "mergeFiles emit loop drops fromFiles rows whose case-folded targetPath is in the app-path set"
      pattern: "targetPath.toCaseFolded"
---

<objective>
Model-side dedupe (D-03): in `ResultsModel::mergeFiles`, scanned file rows whose case-folded path duplicates an app-catalog row (Lnk/tracked/added) are suppressed — the catalog row (icon, display name) survives. UWP rows have empty targetPath and can never collide.

Purpose: This is the display-truth location (RESEARCH Pattern 4 — mergeFiles already owns the merged order), independent of the index backend, so it ships in Wave 1 parallel to the index work. When 07-03/07-04 wire scanned entries into the pipeline, dedupe is already proven by tst_model. The file-cap (kMaxFileRows=5), path tier (kPathMatchScore=100), and all roles stay untouched (PATTERNS line 163-164: no role changes required).
Output: `ResultsModel.cpp` dedupe + `tst_model.cpp` D-03 cases.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-self-managed-file-scan-user-selected-directory-partition-sca/07-RESEARCH.md
@.planning/phases/07-self-managed-file-scan-user-selected-directory-partition-sca/07-PATTERNS.md
@.planning/phases/07-self-managed-file-scan-user-selected-directory-partition-sca/07-CONTEXT.md

<interfaces>
<!-- Consumer contracts (no changes here — read-only reference). -->
From src/core/AppEntry.h: `struct AppEntry { enum class Source { Lnk, Uwp, File }; QString displayName; QString targetPath; ... bool isFolder; }` — UWP rows have EMPTY targetPath; Lnk rows carry the resolved target path; File rows the full path.
From src/core/ResultsModel.h: `Row { int entryIndex; bool fromFiles; bool fromAdded; }` — mergeFiles builds `QVector<Candidate { Row row; FuzzyMatcher::Result result; }>` from `m_order` (app rows) + `m_fileEntries` (file rows, `Row{ i, true }`), sorts score-desc/alpha-asc, then emits with the `fileRows >= kMaxFileRows` cap (ResultsModel.cpp:165-209).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: mergeFiles D-03 dedupe — suppress scan rows duplicating path-bearing app rows</name>
  <files>src/core/ResultsModel.cpp</files>
  <action>
    Modify `ResultsModel::mergeFiles()` (ResultsModel.cpp:165-209) with the D-03 path-set dedupe. Two insertions, one in each loop:

    1. BEFORE the candidate-building loop (after the function-opening comment, before line 174's `for (int i = 0; i < m_order.size(); ++i)`), build the app-path set from the CURRENT app rows (m_order at this point contains app rows; the existing fromFiles skip at lines 174-177 means file rows from an earlier merge never pollute the set):
       ```cpp
       // D-03: path-set dedupe — scanned rows duplicating an app-catalog row
       // (Lnk/tracked/added) are suppressed; the catalog row (icon, display
       // name) wins. Built from path-bearing rows ONLY — UWP rows have an
       // empty targetPath and can never collide (Pitfall 10). Keys are
       // case-folded native paths (T-07-03: one consistent fold, no raw vs
       // normalized mixing — paths arrive normalized from every source).
       QSet<QString> appPaths;
       for (int i = 0; i < m_order.size(); ++i) {
           if (m_order.at(i).fromFiles)
               continue;
           const QString p = entryAt(m_order.at(i)).targetPath;
           if (!p.isEmpty())
               appPaths.insert(p.toCaseFolded());
       }
       ```
       Add `#include <QSet>` if not already present.

    2. In the final emit loop (lines 200-208), after the existing `fromFiles && fileRows >= kMaxFileRows` check, add:
       ```cpp
       if (c.row.fromFiles
           && appPaths.contains(entryAt(c.row).targetPath.toCaseFolded()))
           continue; // D-03: catalog row already renders this path — scan row suppressed
       ```

    Do NOT change: the sort comparator, kMaxFileRows cap logic, kPathMatchScore tier, Candidate struct, or any role/data plumbing. The dedupe set is built from `m_order` app rows at merge time, so tracked/added rows (which flow through the app channels) are covered by the same set (D-03: "Start Menu lnk/UWP/tracked/added").
  </action>
  <read_first>
    src/core/ResultsModel.cpp lines 165-209 (mergeFiles — the exact insertion points)
    src/core/ResultsModel.h lines 108-141 (Row struct, entryAt, mergeFiles contract, kMaxFileRows)
    src/core/AppCatalog.cpp lines 22-50 (dedupeLnkOverUwp — case-folded identity shape)
    src/core/AppEntry.h (targetPath semantics: empty for UWP)
  </read_first>
  <acceptance_criteria>
    - `src/core/ResultsModel.cpp` contains `QSet<QString> appPaths;` and `appPaths.insert(p.toCaseFolded());` and `appPaths.contains(entryAt(c.row).targetPath.toCaseFolded())`
    - The dedupe set is built BEFORE the candidate loop; the suppression happens in the final emit loop after the file-cap check
    - The `fromFiles && fileRows >= kMaxFileRows` cap logic is unchanged (grep `kMaxFileRows` still present in the emit loop)
    - `src/core/ResultsModel.cpp` includes `<QSet>` (add if missing)
  </acceptance_criteria>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .\build.ps1 2>&1 | Select-String -Pattern "error" -NotMatch | Select-Object -First 1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <done>wisp_core builds with the dedupe in place; existing tst_model still passes (regression).</done>
</task>

<task type="auto">
  <name>task 2: tst_model D-03 cases — app row wins, case-folded suppression, UWP never suppresses</name>
  <files>tests/tst_model.cpp</files>
  <action>
    Extend `tests/tst_model.cpp` with D-03 dedupe slots following the existing setFileResults/mergeFiles test structure (tst_model.cpp already exercises `setEntries` + `setFileResults` — check its existing helpers and reuse them; add a local `fileEntry(name, path)` helper if absent, mirroring tst_filesearch.cpp:33-40).

    New slots (each starts from a fresh ResultsModel; hoisted named consts, no braced-init-lists inside QCOMPARE — tst_filesearch convention):

    1. `scanRowSuppressedWhenCatalogHasSamePath_D03`: `setEntries({lnkEntry("Calculator", "C:\\Program Files\\Calc\\Calc.exe")})`; `setQuery("calc")`; `setFileResults(1, "calc", {fileEntry("Calc.exe", "C:\\Program Files\\Calc\\Calc.exe")})` → `rowCount() == 1`; the rendered row is the CATALOG row: assert `data(index(0), DisplayNameRole) == "Calculator"` (distinct display name proves which source won).
    2. `suppressionIsCaseFolded_D03`: catalog row `targetPath "C:\\Program Files\\Calc\\Calc.exe"` + file row `"c:\\program files\\calc\\calc.exe"` → `rowCount() == 1` (T-07-03: case-folded comparison).
    3. `uwpRowNeverSuppressesScanRow_D03`: setEntries with a UWP row (`source = Source::Uwp`, `targetPath = ""`, `displayName "Store App"`) + file row `fileEntry("Store App.exe", "D:\\tools\\Store App.exe")`; setQuery("store") → `rowCount() == 2` (Pitfall 10: empty-path rows never enter the set).
    4. `distinctPathsBothRender_D03`: catalog `C:\\a\\x.exe` + file `D:\\b\\x.exe` (same display name, different path) → `rowCount() == 2` — dedupe is path-based, not name-based (D-03: "same resolved path").
    5. `trackedStyleRowSuppressesScanRow_D03`: catalog row carrying a path but Source::File (mimics tracked/added rows — they flow through the app channel) `"C:\\tools\\g.exe"` + scan row same path → `rowCount() == 1` (D-03 covers tracked/added, not just Lnk).

    Keep all existing tst_model slots untouched (regression — the file cap and ranking behaviors must not move).
  </action>
  <read_first>
    tests/tst_model.cpp (existing structure: setEntries/setFileResults usage, role asserts — read the whole file once)
    tests/tst_filesearch.cpp lines 33-48 (hoisted const + fileEntry helper conventions)
    src/core/ResultsModel.h lines 31-39 (role names: DisplayNameRole, SubtitleRole)
  </read_first>
  <acceptance_criteria>
    - `tests/tst_model.cpp` contains slot names: `scanRowSuppressedWhenCatalogHasSamePath_D03`, `suppressionIsCaseFolded_D03`, `uwpRowNeverSuppressesScanRow_D03`, `distinctPathsBothRender_D03`, `trackedStyleRowSuppressesScanRow_D03`
    - Each slot asserts `rowCount()` and, for the suppression case, the surviving row's DisplayNameRole value (catalog source wins)
    - `ctest --test-dir build/dev -R tst_model --output-on-failure` passes
  </acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build/dev -R tst_model --output-on-failure</automated>
  </verify>
  <done>tst_model green with the 5 new D-03 slots; all pre-existing slots still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index rows → merged model | Scanned .exe paths (case variants, different separator styles from dialog/enumeration) cross into mergeFiles alongside catalog paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | ResultsModel::mergeFiles dedupe | mitigate | Keys are `targetPath.toCaseFolded()` — one consistent fold applied to BOTH sides at the same site; paths arrive native-normalized from every producer (LaunchHistory normalize, FileIndex normalize, catalog resolution), so 8.3 short-name aliases or separator drift cannot split or false-collide keys; UWP empty paths are excluded from the set by the `!p.isEmpty()` guard, so scan rows can never be suppressed by a path-less row |
| T-07-01 | DoS | mergeFiles emit loop | mitigate | Dedupe is a single `QSet::contains` per file row — O(1); no new sort or quadratic pass; the kMaxFileRows=5 cap continues to bound file-row work |
</threat_model>

<verification>
- `ctest --test-dir build/dev -R tst_model --output-on-failure` → green
- `ctest --test-dir build/dev --output-on-failure` → full suite green (tst_model extension must not break other suites; FileSearch pipeline untouched)
- `powershell -ExecutionPolicy Bypass -File .\build.ps1` → wisp exe still builds (no seam changes in this plan)
</verification>

<success_criteria>
- mergeFiles suppresses scanned rows whose case-folded path duplicates a path-bearing app row (Lnk/tracked/added); catalog row survives with its icon/display name
- UWP rows provably cannot suppress scan rows (empty-path guard)
- Decision D-03 implemented; all other decisions covered by sibling plans (D-01/D-04: 07-04, D-02/D-06/D-07/D-08 store: 07-01, D-05/D-09/D-10: 07-05, D-08 worker + D-09 scheduling: 07-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-self-managed-file-scan-user-selected-directory-partition-sca/07-02-SUMMARY.md`
</output>
