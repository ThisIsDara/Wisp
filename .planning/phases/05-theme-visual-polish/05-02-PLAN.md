---
phase: 5
plan: 2
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/core/IconCache.h, src/core/IconCache.cpp, tests/tst_iconcache.cpp, CMakeLists.txt]
autonomous: true
requirements: [VISU-02]
must_haves:
  truths:
    - "Icon cache never exceeds its configured cap (~500 entries ≈ 8 MB) — the 'no unbounded growth' proof (D-03)"
    - "Evicting oldest entry happens when the cap is reached; a get() reorders the entry to newest"
    - "Cache is safe when called from Qt's provider thread (reentrancy + QMutex guard)"
    - "Cache miss returns a null QImage — failures are never cached (extraction retried on demand, sub-10ms)"
  artifacts:
    - path: src/core/IconCache.h
      provides: "Bounded LRU contract: get/insert/size, cap configurable, QMutex-guarded"
      contains: "class IconCache"
    - path: src/core/IconCache.cpp
      provides: "LRU implementation (QHash + QList order, eviction)"
      contains: "m_order"
    - path: tests/tst_iconcache.cpp
      provides: "Unit tests: cap honored, oldest evicted, hits re-order, thread-safe race"
      contains: "oldestEvicted"
    - path: CMakeLists.txt
      provides: "IconCache.cpp registered in wisp_core; tst_iconcache target wired"
      contains: "tst_iconcache"
  key_links:
    - from: src/core/IconProvider.cpp (plan 05-04)
      to: src/core/IconCache.cpp
      via: "get(id) on miss → extract → insert(id, img)"
      pattern: "m_cache->get|m_cache->insert"
    - from: CMakeLists.txt
      to: src/core/IconCache.cpp
      via: "wisp_core source list (LNK2019 rule)"
      pattern: "IconCache.cpp"
---

<objective>
Ship the bounded in-memory LRU icon cache (D-03) with its unit suite.

Purpose: D-03 is a hard success criterion — "cache without unbounded growth". The LRU is the single cache for extracted icons (64px QImages, cap 500 ≈ 8 MB), lives in `src/core/` as pure testable C++, and is guarded by a QMutex so Qt's provider thread (plan 05-04) can call it safely. LaunchHistory's QMutex discipline (WR-01) is the copied pattern; tst_history's QtConcurrent race test is the copied thread-safety smoke.
Output: `src/core/IconCache.{h,cpp}`, `tests/tst_iconcache.cpp` (all green), `CMakeLists.txt` extended.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-visual-polish/05-CONTEXT.md
@.planning/phases/05-theme-visual-polish/05-PATTERNS.md
@.planning/phases/05-theme-visual-polish/05-RESEARCH.md

**Wave note:** `depends_on: [05-01]` is the CMakeLists.txt file-ownership serialization (05-01 edited it for the seam sources + tst_icons; this plan appends IconCache.cpp + tst_iconcache). No code dependency on 05-01 — IconCache is a standalone pure class; plan 05-04 wires IconProvider to it.

Contracts consumed here: none from 05-01 (IconCache keys are opaque QString provider ids). Contract created here for 05-04: `IconCache::get(const QString &key) -> QImage` (null on miss), `IconCache::insert(const QString &key, const QImage &img)` (no-op for null images — caller contract), `IconCache::size() -> int`, ctor `IconCache(int capacity = 500)`.
</context>

<tasks>

<task type="auto">
  <name>task 1: IconCache.h + IconCache.cpp — bounded LRU with QMutex discipline</name>
  <files>src/core/IconCache.h, src/core/IconCache.cpp</files>
  <read_first>
    src/core/LaunchHistory.cpp (lines 44-48 READ-MODIFY-WRITE under one QMutexLocker — the discipline to copy; WR-01)
    .planning/phases/05-theme-visual-polish/05-RESEARCH.md (Pattern 1 key contract details: 64px cache entries, ~500 cap ≈ 8 MB, cache key = full provider id, failures never cached; Pitfall 2)
    .planning/phases/05-theme-visual-polish/05-PATTERNS.md (IconCache section lines 74-98: reentrancy contract — non-recursive QMutex, never call one public method from inside another)
    src/core/LaunchHistory.h (plain-class shape, mutable QMutex member — lines 16-53)
  </read_first>
  <action>
    Create src/core/IconCache.h:
    - class IconCache (plain class, NOT QObject — the LaunchHistory.h:16-53 shape). Members: `mutable QMutex m_mutex;` (non-recursive, per PATTERNS reentrancy contract), `QHash<QString, QImage> m_map;`, `QList<QString> m_order;` (front = LRU/oldest, back = MRU/newest), `int m_capacity = 500;`.
    - API: `explicit IconCache(int capacity = 500);` (D-03: ~500 ≈ 8 MB at 64×64×4 = 16 KB); `QImage get(const QString &key);` (returns null QImage on miss — MISS IS NOT CACHED); `void insert(const QString &key, const QImage &img);` (silently ignore null img — the failure-not-cached contract); `int size() const;` (test/cap assertion helper).
    - Header doc comment MUST state the threading contract: called from Qt's single provider thread per engine (plan 05-04); QMutex guards reentrancy and any future concurrent use; every public method takes and releases the lock exactly once (no nested public calls — non-recursive mutex).
    Create src/core/IconCache.cpp:
    - get: QMutexLocker locker(&m_mutex); look up m_map; on hit: move key to m_order back (removeOne + append — O(n) at n ≤ 500 is fine), return the QImage (implicit sharing → cheap copy); on miss: return null QImage.
    - insert: QMutexLocker locker(&m_mutex); if img.isNull() return; if key already in m_map → update value + move to back; else → m_map[key] = img; m_order.append(key); then eviction: while (m_order.size() > m_capacity) { remove front key from m_map and m_order } — the boundedness proof (D-03).
    - size: QMutexLocker locker(&m_mutex); return m_order.size();
    - No disk, no signals, no QObject — pure in-memory utility (D-03: no disk persistence).
  </action>
  <verify>
    <automated>cmake --build build/dev --target wisp_core</automated>
  </verify>
  <acceptance_criteria>
    - rg "class IconCache" src/core/IconCache.h → match
    - rg "mutable QMutex m_mutex" src/core/IconCache.h → match
    - rg "QImage get\(" src/core/IconCache.h → match AND rg "void insert\(" src/core/IconCache.h → match
    - rg "m_capacity" src/core/IconCache.h → match AND rg "= 500" src/core/IconCache.h → match (default cap, D-03)
    - rg "QMutexLocker" src/core/IconCache.cpp → match (≥3 — get/insert/size each lock once)
    - rg "removeFirst" src/core/IconCache.cpp → match (eviction path)
    - wisp_core target builds clean
  </acceptance_criteria>
  <done>Bounded LRU implemented with one-lock-per-public-method discipline; cap 500 default; null-image inserts ignored.</done>
</task>

<task type="auto">
  <name>task 2: tst_iconcache.cpp + CMakeLists.txt wiring</name>
  <files>tests/tst_iconcache.cpp, CMakeLists.txt</files>
  <read_first>
    tests/tst_history.cpp (lines 33-53 suite skeleton; lines 173-210 concurrentAccessThreadSafe_WR01 — the QtConcurrent race shape to copy verbatim; lines 212-213 QTEST_MAIN)
    CMakeLists.txt (lines 13-29 wisp_core source list; lines 80-145 BUILD_TESTING block incl. set_tests_properties ENVIRONMENT_MODIFICATION at 139-145)
    .planning/phases/05-theme-visual-polish/05-RESEARCH.md (Validation Map D-03 rows: cap honored, oldest evicted, hits re-order, thread-safe insert/get)
  </read_first>
  <action>
    Create tests/tst_iconcache.cpp — suite skeleton copied from tst_history.cpp:33-46 (class TstIconCache : QObject, Q_OBJECT, private slots, QTEST_MAIN(TstIconCache), #include "tst_iconcache.moc"). QImage fixtures: build a small 8x8 image per key (QImage(8, 8, QImage::Format_ARGB32)) — cheap, distinct objects; use QTemporaryDir-free plain local instances. Cases (Validation Map D-03 rows):
    - capHonored: IconCache(500); insert 501 distinct keys; QCOMPARE(cache.size(), 500) — the boundedness proof
    - oldestEvicted: IconCache(2); insert "a","b","c"; get("a") is null (evicted); get("b") and get("c") non-null
    - hitReorders: IconCache(2); insert "a","b"; get("a") (touch); insert "c"; now get("b") is null (b was oldest after the a-touch) and get("a") non-null — proves hits re-order
    - missReturnsNull: IconCache(2); get("nope") → QVERIFY(img.isNull()) — miss never cached
    - insertNullIgnored: insert("x", QImage()) → size() unchanged, get("x") null
    - threadSafeInsertGet: copy tst_history.cpp:173-210 verbatim shape — atomic start flag + QtConcurrent::run of a writer lambda (insert N distinct keys in a loop) and reader lambdas (random get of the same key range) + waitForFinished; then QCOMPARE(cache.size(), N) and no crash — the QMutex smoke test
    CMakeLists.txt (LNK2019 + BUILD_TESTING pattern):
    - add src/core/IconCache.cpp to the wisp_core qt_add_library source list (lines 13-29)
    - qt_add_executable(tst_iconcache tests/tst_iconcache.cpp); target_link_libraries(tst_iconcache PRIVATE Qt6::Core Qt6::Gui Qt6::Test wisp_core); add_test(NAME tst_iconcache COMMAND tst_iconcache)
    - append tst_iconcache to the set_tests_properties ENVIRONMENT_MODIFICATION list (lines 139-145)
  </action>
  <verify>
    <automated>cmake --build build/dev --target tst_iconcache && ctest --test-dir build/dev -R tst_iconcache --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - rg "QTEST_MAIN\(TstIconCache\)" tests/tst_iconcache.cpp → match
    - rg "capHonored" tests/tst_iconcache.cpp → match AND rg "oldestEvicted" → match AND rg "hitReorders" → match AND rg "threadSafeInsertGet" → match
    - rg "QtConcurrent" tests/tst_iconcache.cpp → match (race smoke test)
    - rg "IconCache.cpp" CMakeLists.txt → match (wisp_core list)
    - rg -c "tst_iconcache" CMakeLists.txt → 3 (qt_add_executable, add_test, set_tests_properties)
    - ctest -R tst_iconcache: 100% pass
  </acceptance_criteria>
  <done>All six LRU behaviors proven by automated tests incl. the concurrency race; source + target wired in CMake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider-thread callers → IconCache | Cache keys are opaque provider ids (untrusted strings from .lnk content); the cache treats them as opaque — no parsing, no filesystem access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | DoS (self) | IconCache memory growth | mitigate | Cap enforced in insert() eviction loop + asserted by tst_iconcache capHonored (D-03: 500 × 16 KB ≈ 8 MB). QML double-caching prevented later via Image { cache: false } (05-05). |
| T-05-07 | Tampering | Cache key strings (opaque, from .lnk-derived ids) | accept | Keys are never parsed or used for I/O here — only hashed/compared as opaque QStrings. Extraction seam (05-01) owns defensive parsing. |
| T-05-08 | Integrity | Concurrent get/insert from provider thread + race window | mitigate | Every public method takes the QMutex exactly once (non-recursive, no nested public calls — PATTERNS reentrancy contract); threadSafeInsertGet race test guards regressions. |
| T-05-09 | Info disclosure | None — in-memory bitmaps only, no persistence, no network | accept | No PII, no egress; cache is process-local. |
</threat_model>

<verification>
- `cmake --build build/dev` succeeds (wisp_core + all test targets).
- `ctest --test-dir build/dev -R tst_iconcache --output-on-failure` → all cases green.
- Acceptance-criteria grep gates (see tasks) all match.
</verification>

<success_criteria>
- IconCache exists as a pure, non-QObject, testable utility in src/core with the exact API 05-04 consumes (get/insert/size, cap default 500).
- All six unit cases pass — boundedness, eviction order, touch-reorder, null semantics, concurrency.
- No QML, no Windows API, no main.cpp changes in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-visual-polish/05-02-SUMMARY.md`
</output>
