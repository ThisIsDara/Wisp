---
phase: 5
plan: 3
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/core/SettingsStore.h, src/core/SettingsStore.cpp, tests/tst_settings.cpp, CMakeLists.txt]
autonomous: true
requirements: [VISU-02]
must_haves:
  truths:
    - "Accent reads from the settings INI at startup; missing or corrupt value silently falls back to #0078D4 (D-16)"
    - "setAccent persists to the INI, syncs, and notifies via accentChanged — the Phase-6 picker path (D-13/D-14)"
    - "The value round-trips across SettingsStore instances over the same INI file"
    - "No QML-side config, no inline QSettings in main.cpp — the store is the single accent owner (D-14)"
  artifacts:
    - path: src/core/SettingsStore.h
      provides: "QObject store: accent property + NOTIFY, setAccent Q_INVOKABLE, makeSettings factory"
      contains: "class SettingsStore"
    - path: src/core/SettingsStore.cpp
      provides: "QSettings INI persistence with silent #0078D4 fallback"
      contains: "theme/accent"
    - path: tests/tst_settings.cpp
      provides: "Unit tests: default, corrupt, round-trip, signal, cross-instance persistence"
      contains: "persistsAcrossInstances"
    - path: CMakeLists.txt
      provides: "SettingsStore.cpp registered in wisp_core; tst_settings target wired"
      contains: "tst_settings"
  key_links:
    - from: src/app/main.cpp (plan 05-05)
      to: src/core/SettingsStore.cpp
      via: "context property settingsStore (registered before loadFromModule)"
      pattern: "settingsStore"
    - from: qml/MainWindow.qml (plan 05-05)
      to: src/core/SettingsStore.cpp
      via: "Theme.accent = settingsStore.accent + Connections onAccentChanged"
      pattern: "settingsStore.accent"
---

<objective>
Ship the SettingsStore — the single source of truth for the accent value (D-13/D-14), unit-tested.

Purpose: All accent usages (selection bg, left bar, highlight color, chips) bind to Theme.accent, which is fed by this store from `%APPDATA%\TID\wisp\wisp.ini` at startup with silent fallback to `#0078D4` (D-16). The LaunchHistory makeSettings factory is copied verbatim (same INI, non-colliding key `theme/accent`). Phase 6's accent picker will call setAccent() — the notify path is built and tested now.
Output: `src/core/SettingsStore.{h,cpp}`, `tests/tst_settings.cpp` (all green), `CMakeLists.txt` extended.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-visual-polish/05-CONTEXT.md
@.planning/phases/05-theme-visual-polish/05-PATTERNS.md
@.planning/phases/05-theme-visual-polish/05-RESEARCH.md

**Wave note:** `depends_on: [05-02]` is the CMakeLists.txt file-ownership serialization. No code dependency on 05-01/05-02.

**QObject decision (D-14 planner's call):** SettingsStore derives QObject (research Pattern 4 sketch — the QML `Connections { target: settingsStore; function onAccentChanged() }` wiring in 05-05 REQUIRES the NOTIFY signal). LaunchHistory's plain-class shape is NOT copied for the base class — only the makeSettings factory and QSettings discipline are.

Contract created here for 05-04/05-05: `SettingsStore(const QString &settingsPath = {}, QObject *parent = nullptr)`; `QColor accent() const` (+ Q_PROPERTY NOTIFY accentChanged); `Q_INVOKABLE void setAccent(const QColor &c)`; context property name `settingsStore`; INI key `theme/accent`.
</context>

<tasks>

<task type="auto">
  <name>task 1: SettingsStore.h + SettingsStore.cpp — QSettings INI accent store</name>
  <files>src/core/SettingsStore.h, src/core/SettingsStore.cpp</files>
  <read_first>
    src/core/LaunchHistory.cpp (lines 24-30 makeSettings factory — COPY VERBATIM; lines 48/58 sync-after-write discipline; line 112 value-with-default pattern)
    src/core/LaunchHistory.h (mutable QMutex member + ctor shape)
    .planning/phases/05-theme-visual-polish/05-RESEARCH.md (Pattern 4 sketch lines 259-273: QObject + Q_PROPERTY + Q_INVOKABLE; readAccent semantics D-16; Pitfall 7 UI-thread-only contract)
    .planning/phases/05-theme-visual-polish/05-PATTERNS.md (SettingsStore section lines 32-70; Shared Pattern 1 makeSettings; Shared Pattern 11 silent fallback)
  </read_first>
  <action>
    Create src/core/SettingsStore.h:
    - `class SettingsStore : public QObject` with Q_OBJECT; `Q_PROPERTY(QColor accent READ accent NOTIFY accentChanged)`.
    - Public: `explicit SettingsStore(const QString &settingsPath = {}, QObject *parent = nullptr);` (settingsPath empty → the default UserScope TID/wisp INI via makeSettings; non-empty → the tst QTemporaryDir seam); `QColor accent() const;` (live read via readAccent()); `Q_INVOKABLE void setAccent(const QColor &c);` (Phase-6 picker entry point).
    - Signal: `void accentChanged(const QColor &accent);`
    - Private: `QColor readAccent() const;` and `QSettings m_settings;` (constructed with the makeSettings factory in the member-init list — non-copyable member, class is move-less by design, LaunchHistory precedent).
    - Header doc comment MUST state the threading contract: UI-thread-only (Pitfall 7) — accent read at startup, set from the picker; if a worker ever needs it, add the LaunchHistory QMutex discipline (WR-01). No mutex now.
    Create src/core/SettingsStore.cpp:
    - Copy the `makeSettings(const QString &settingsPath)` factory VERBATIM from LaunchHistory.cpp:24-30 into an anonymous namespace (QSettings(IniFormat, UserScope, "TID", "wisp") when empty; QSettings(settingsPath, IniFormat) otherwise).
    - readAccent() (D-16 semantics): `const QString raw = m_settings.value(QStringLiteral("theme/accent"), QStringLiteral("#0078D4")).toString();` → `QColor c = QColor::fromString(raw);` → `return c.isValid() ? c : QColor(QStringLiteral("#0078D4"));` — missing key, corrupt string, and unparseable color ALL silently return the default (no toasts, no warnings — D-16).
    - setAccent(const QColor &c): if (!c.isValid()) return; store via c.name() (HexArgb form if alpha < 255 — keep full fidelity for Phase 6); `m_settings.setValue(...)` then `m_settings.sync();` (LaunchHistory.cpp:48/58 discipline — sync after EVERY write) then `emit accentChanged(c);`.
    - accent() const → return readAccent(); (live read — always fresh, no staleness after external edits; the QML binding consumes the value once at startup + via signal).
  </action>
  <verify>
    <automated>cmake --build build/dev --target wisp_core</automated>
  </verify>
  <acceptance_criteria>
    - rg "class SettingsStore" src/core/SettingsStore.h → match AND rg ": public QObject" → match
    - rg "Q_PROPERTY\(QColor accent" src/core/SettingsStore.h → match AND rg "NOTIFY accentChanged" → match
    - rg "Q_INVOKABLE void setAccent" src/core/SettingsStore.h → match
    - rg "theme/accent" src/core/SettingsStore.cpp → match
    - rg "QSettings\(QSettings::IniFormat, QSettings::UserScope" src/core/SettingsStore.cpp → match (factory verbatim)
    - rg "QColor\(QStringLiteral\(\"#0078D4\"\)\)" src/core/SettingsStore.cpp → match (D-16 fallback)
    - rg "sync\(\)" src/core/SettingsStore.cpp → match AND rg "emit accentChanged" → match
    - wisp_core target builds clean
  </acceptance_criteria>
  <done>SettingsStore persists accent to the shared INI with silent fallback; NOTIFY wired for QML; factory copied verbatim; sync-after-write discipline.</done>
</task>

<task type="auto">
  <name>task 2: tst_settings.cpp + CMakeLists.txt wiring</name>
  <files>tests/tst_settings.cpp, CMakeLists.txt</files>
  <read_first>
    tests/tst_history.cpp (lines 33-53 suite skeleton + QTemporaryDir seam; lines 59-67 cross-instance reload pattern)
    CMakeLists.txt (lines 13-29 wisp_core source list; lines 80-145 BUILD_TESTING block incl. set_tests_properties at 139-145)
    .planning/phases/05-theme-visual-polish/05-RESEARCH.md (Validation Map D-16 rows: missing → #0078D4, corrupt → #0078D4, set→read round-trip, accentChanged emitted, persists across instances)
  </read_first>
  <action>
    Create tests/tst_settings.cpp — suite skeleton copied from tst_history.cpp:33-46 (class TstSettings : QObject, Q_OBJECT, private slots, QTEST_MAIN(TstSettings), #include "tst_settings.moc"). EVERY case goes through a REAL temp INI via the QTemporaryDir seam (tst_history.cpp:50-53 — NEVER %APPDATA%): `QTemporaryDir dir; QVERIFY(dir.isValid()); const QString iniPath = dir.filePath(QStringLiteral("wisp.ini"));` then `SettingsStore store(iniPath);`. Cases (Validation Map D-16):
    - missingKeyDefaultsTo0078D4: fresh INI → QCOMPARE(store.accent(), QColor("#0078D4"))
    - corruptValueFallsBack: pre-write the INI with QSettings(iniPath, IniFormat) setValue("theme/accent", "not-a-color") + sync BEFORE constructing the store → accent() == QColor("#0078D4") (silent fallback)
    - setReadRoundTrip: store.setAccent(QColor("#E81123")); QCOMPARE(store.accent(), QColor("#E81123"))
    - accentChangedEmitted: QSignalSpy spy(&store, &SettingsStore::accentChanged); setAccent(...) → QCOMPARE(spy.count(), 1) and the argument is the new QColor (spy.at(0).at(0).value<QColor>())
    - persistsAcrossInstances: store1.setAccent(QColor("#107C10")); then `SettingsStore store2(iniPath);` (the tst_history.cpp:59-67 reload pattern) → QCOMPARE(store2.accent(), QColor("#107C10")) — proves the value survived to disk
    - invalidSetIgnored: store.setAccent(QColor()); → accent unchanged and spy.count() == 0
    CMakeLists.txt (LNK2019 + BUILD_TESTING pattern):
    - add src/core/SettingsStore.cpp to the wisp_core qt_add_library source list (lines 13-29)
    - qt_add_executable(tst_settings tests/tst_settings.cpp); target_link_libraries(tst_settings PRIVATE Qt6::Core Qt6::Gui Qt6::Test wisp_core); add_test(NAME tst_settings COMMAND tst_settings)
    - append tst_settings to the set_tests_properties ENVIRONMENT_MODIFICATION list (lines 139-145)
  </action>
  <verify>
    <automated>cmake --build build/dev --target tst_settings && ctest --test-dir build/dev -R tst_settings --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - rg "QTEST_MAIN\(TstSettings\)" tests/tst_settings.cpp → match
    - rg "QTemporaryDir" tests/tst_settings.cpp → match (seam — no %APPDATA% anywhere)
    - rg "missingKeyDefaultsTo0078D4" tests/tst_settings.cpp → match AND rg "corruptValueFallsBack" → match AND rg "persistsAcrossInstances" → match AND rg "accentChangedEmitted" → match
    - rg "QSignalSpy" tests/tst_settings.cpp → match
    - rg "SettingsStore.cpp" CMakeLists.txt → match (wisp_core list)
    - rg -c "tst_settings" CMakeLists.txt → 3 (qt_add_executable, add_test, set_tests_properties)
    - ctest -R tst_settings: 100% pass
  </acceptance_criteria>
  <done>All six D-16 behaviors proven through a real temp INI; signal path verified; source + target wired in CMake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INI file on disk → SettingsStore | %APPDATA%\TID\wisp\wisp.ini is user-writable; accent value crosses from untrusted file content into QML as a color. |
| QML (05-05) → SettingsStore | setAccent is Q_INVOKABLE — callable from QML/JS in Phase 6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | Corrupt/malicious accent value in INI (e.g., "#0078D4;rm -rf", huge string) | mitigate | QColor::fromString validity gate — ANY unparseable value returns default #0078D4 (D-16, tested by corruptValueFallsBack). Stored via c.name() (canonical #rrggbb) — no path/file side effects from the value. No warnings, no toasts. |
| T-05-11 | DoS | Huge accent string causing memory blowup | accept | QSettings value bounded by INI file size (user-controlled, tiny); QColor::fromString fails fast on non-color strings. No regex, no allocation amplification. |
| T-05-12 | Integrity | Concurrent INI access with LaunchHistory (WR-01 lesson) | accept | UI-thread-only contract documented in header (Pitfall 7); LaunchHistory already serializes its own access with QMutex; SettingsStore writes only from the picker path (Phase 6) on the UI thread. |
| T-05-13 | Info disclosure | None — accent color only; no secrets in the INI | accept | No PII, no credentials; settings file already contains launch history per LaunchHistory. |
</threat_model>

<verification>
- `cmake --build build/dev` succeeds (wisp_core + all test targets).
- `ctest --test-dir build/dev -R tst_settings --output-on-failure` → all cases green.
- Acceptance-criteria grep gates (see tasks) all match.
</verification>

<success_criteria>
- SettingsStore exists as a QObject store with accent property + NOTIFY, Q_INVOKABLE setAccent, and the verbatim makeSettings factory.
- All six D-16 behaviors unit-proven through a real temp INI (never %APPDATA%).
- No QML, no Windows API, no main.cpp changes in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-visual-polish/05-03-SUMMARY.md`
</output>
