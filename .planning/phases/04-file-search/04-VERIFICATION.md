---
phase: 04-file-search
verified: 2026-08-10T02:32:24Z
status: passed
score: 22/22 must-haves verified
overrides_applied: 0
---

# Phase 4: File Search Verification Report

**Phase Goal:** Files join the search: Windows Search index queries run on worker threads with debounce and stale-result dropping, files open with their default app, containing folder opens with Ctrl+Enter, and indexer problems are explained rather than silently empty.
**Verified:** 2026-08-10T02:32:24Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | User can type a filename and .exe/folder results from the Windows Search index appear, merged with app results (LAUN-02) | ✓ VERIFIED | `src/win/WinSearchQuery.cpp` raw OLE DB pipeline (CollatorDSO.1 → IDBInitialize → IRowset) live-verified in 04-01; `queryFiles` called from main.cpp QueryFn seam; merge in `ResultsModel::mergeFiles`; human checkpoint step 1 (notepad → merged rows) approved |
| 2 | File query generated by ISearchQueryHelper (AQS escaping), never hand-concatenated SQL (T-04-01) | ✓ VERIFIED | `WinSearchQuery.cpp:487` `GenerateSQLFromUserQuery` is the ONLY SQL source; WHERE fragment is a compile-time constant (`buildWhereRestriction`, grep-verified exact string in tst_search); no SQL-buffer `+=` on user input |
| 3 | Only .exe files and folders come back from the index (D-09), capped at 30 upstream via put_QueryMaxResults | ✓ VERIFIED | `put_QueryMaxResults(30)` (line 465); `isAllowedResult` post-filter gate (folders kept, case-insensitive .exe); unit-tested in tst_search |
| 4 | Indexer trouble (disabled/building/unavailable) detectable on-query (D-16/D-17) | ✓ VERIFIED | `checkIndexStatus()` → `GetCatalogStatus` → `classifyCatalogStatus` mapping all 7 CatalogStatus values; test-covered in tst_search |
| 5 | Typing pauses ~150ms then exactly ONE file query fires for the latest text (D-12 debounce) | ✓ VERIFIED | `FileSearch.cpp` `kDebounceMs=150`, single-shot restart ("last text wins"); `debounceFiresOnceAfterQuiet_D12` green |
| 6 | Stale file results are never delivered — generation guard on both worker completion and model (D-15) + query-text guard (WR-03) | ✓ VERIFIED | `onFinished` drops `r.generation != m_generation` AND `r.query != m_query`; `ResultsModel::setFileResults` drops `generation < m_fileGeneration` AND text mismatch (guard appears exactly once); `m_fileGeneration` never reset by `setEntries`; suites `staleGenerationDropped_D15`, `staleTextDropped_WR03`, `staleTextDroppedInDebounceWindow_WR03`, `generationGuardMonotonicAcrossRefresh_WR03` green |
| 7 | Empty query bypasses the file pipeline entirely — apps only (D-14) | ✓ VERIFIED | `setQuery` empty → `m_debounce.stop()`; `queryFiles` returns early on empty; `buildAppOrder` renders apps only; `emptyQueryBypass_D14` + `emptyQueryAppsOnly_D14` green |
| 8 | Indexer Disabled/Unavailable skips the query and emits only the status; Building queries AND emits status (D-16/D-17 decision table) | ✓ VERIFIED | Worker lambda: `Disabled/Unavailable → return {gen, {}, st}`; Building falls through to query; `queryFn` failure → Unavailable (RESEARCH §2); suites `disabledSkipsQuery_D16`, `unavailableSkipsQuery`, `buildingQueriesAndReports_D17`, `queryFailureMapsUnavailable` green |
| 9 | Tracked/added executables join file results as a second source (D-06/D-07/D-10/D-11) | ✓ VERIFIED | Worker merges `trackedSource()` entries via `FuzzyMatcher::score` on displayName AND targetPath; `trackedSourceMerged_D06` green; seam wired to `LaunchHistory::trackedExecutables` in main.cpp |
| 10 | The three trouble-state copies are owned in C++ (single home) — QML renders, never invents (D-17/D-18) | ✓ VERIFIED | All three verbatim strings exist ONLY in `FileSearch::statusText()`; grep: 0 occurrences in `qml/`; 1 occurrence in `src/core/FileSearch.cpp` |
| 11 | Every wisp launch of an executable records {path → count} to the existing INI (D-10) | ✓ VERIFIED | `LaunchHistory::recordLaunch` (launchHistory group, QSettings IniFormat TID/wisp, `sync()` after write); wired via `launch.setHistory` + recordLaunch in default ResultReporter Launched branch; `launchTracking_D10`, `recordAndReloadRoundTrip_D10` green |
| 12 | Manually added executables persist in their own store and never collide with launch counts (D-11) | ✓ VERIFIED | `addedExecutables` group, union-dedupe (added wins), forward-slash normalization; `manualAddPersists_D11`, `unionDedupe`, `forwardSlashNormalization` green |
| 13 | Ctrl+Enter on a file/folder opens Explorer with the file selected and the path quoted (LAUN-03) | ✓ VERIFIED | `WinLaunch::revealInExplorer` — `/select,"<path>"` quoted, native-normalized, FLAG_NO_UI, CancelledByUser classification; `revealCtrlEnter_LAUN03`, `revealSnapshotFreeze_D12` green; QML `launchFromKey` Ctrl-only branch |
| 14 | Ctrl+Shift+Enter on a file/folder launches silently normal — no elevation, no hint, no refusal UI (D-05) | ✓ VERIFIED | `effectiveElevated = false` for Source::File in `launchEntry`; grep: zero `runas` occurrences in LaunchController.cpp; `fileRowElevatedSilentlyNormal_D05`, `folderRowElevatedSilentlyNormal_D05` green (zero signals asserted) |
| 15 | Reveal success dismisses the launcher (D-13); reveal failure surfaces launchFailed, never a crash | ✓ VERIFIED | `revealSelected` classification table (Launched→dismiss, Cancelled→quiet, Failed→launchFailed); `revealFailureSignals` green |
| 16 | File results merge with app results in ONE interleaved ranked list — score decides order, no sectioning (D-01) | ✓ VERIFIED | `mergeFiles()`: union sort score-desc, displayName asc case-folded across both sources; `fileResultsMerge_D01` green |
| 17 | At most 5 file rows survive any merge — apps are never dropped (D-03) | ✓ VERIFIED | `kMaxFileRows=5` enforced structurally in `mergeFiles` (file-row positions only); `fileCap5_D03` green |
| 18 | Path-only file matches rank at a base score below every name match (D-07) | ✓ VERIFIED | `kPathMatchScore=100` fallback when name score is 0 (below the 400 subsequence tier); `pathOnlyBaseScore_D07` green |
| 19 | File rows show the full path as subtitle (D-02); folder rows expose IsFolderRole for the QML glyph (D-04) | ✓ VERIFIED | `data()` SubtitleRole: `Source::File → targetPath` (before Lnk/Uwp branches); `IsFolderRole` in roleNames + data(); `subtitleFullPath_D02`, `folderRowsIsFolderRole_D04` green |
| 20 | Stale generations are dropped model-side too — defense in depth (D-15) | ✓ VERIFIED | `setFileResults` guard `generation < m_fileGeneration` (exactly once); `staleGenerationDropped_D15` green; selection preserved/clamped on file arrival (`selectionPreservedOnMerge`) |
| 21 | Indexer trouble shows the locked status copy as a non-selectable row — never a blank dead-end (D-17/D-18) | ✓ VERIFIED | `statusRow` overlay gated `query !== "" && !fileSearch.indexerOk && resultsView.count === 0` (WR-04 fixed — never covers usable results); emptyState gated on `indexerOk`; renders `fileSearch.statusText` verbatim; human checkpoint step 7 (Stop-Service WSearch) approved |
| 22 | "Add executable…" pinned row persists picks to the INI and they appear immediately (D-11) | ✓ VERIFIED | `addExeRow` pinned at surface bottom (hairline + hover + MouseArea → `fileSearch.addExecutable()`); dialog seam (QFileDialog, *.exe filter) + store seam (history.addExecutable) in main.cpp; immediate re-dispatch (`addExecutableRedispatches_D11` green); human checkpoint step 6 approved |
| 23 | Folder rows show the distinct monogram glyph (D-04) | ✓ VERIFIED | `ResultsRow.qml` `text: model.isFolder ? "\u25B8" : initial`, `accentLight` tint; human checkpoint step 5 approved |
| 24 | Typing drives BOTH pipelines: apps re-filter instantly, file search debounces then fills in quietly (D-12/D-13) | ✓ VERIFIED | `onTextChanged: { resultsModel.setQuery(text); fileSearch.setQuery(text) }`; no spinner/indicator (quiet fill-in per `quietFillIn_D13`); human checkpoint step 8 approved |

**Score:** 22/22 truths verified (truths #1–22 above; #23–24 documented via phase checkpoint evidence)

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/core/AppEntry.h` | Source::File + isFolder contract | ✓ VERIFIED | `enum class Source { Lnk, Uwp, File }`, `bool isFolder = false`, File semantics documented |
| `src/win/WinSearchQuery.h` | Firewall contract | ✓ VERIFIED | FileResult, IndexerState, queryFiles(`bool *ok`), checkIndexStatus, 3 pure helpers; "ATL verified absent" note present; no class keyword |
| `src/win/WinSearchQuery.cpp` | Raw OLE DB COM pipeline | ✓ VERIFIED | GenerateSQLFromUserQuery, CLSIDFromProgID, GetCatalogStatus, CreateAccessor, GetNextRows, CoInitializeEx all present; 16/8-aligned bindings; WR-02 status/length-bounded reads |
| `src/core/FileSearch.h/.cpp` | Debounce + generation + worker coordinator | ✓ VERIFIED | kDebounceMs=150, generation counter, QtConcurrent worker, status decision table, tracked merge, D-17 status copy, setPool seam (WR-05) |
| `src/core/LaunchHistory.h/.cpp` | INI launch-tracking + manual store | ✓ VERIFIED | recordLaunch/addExecutable/trackedExecutables/launchCount; native-separator keys; sync() after writes; QMutex guard (WR-01); displayName never stored |
| `src/core/ResultsModel.h/.cpp` | Merged-result engine | ✓ VERIFIED | setFileResults (3-arg), IsFolderRole, Row struct, kMaxFileRows=5, kPathMatchScore=100, mergeFiles/buildAppOrder, D-15 guard, full-path subtitle |
| `src/core/LaunchController.cpp` | D-05 + reveal + tracking policy | ✓ VERIFIED | effectiveElevated mapping, recordLaunch in default reporter, revealSelected with D-12/D-13 classification, no `runas` |
| `src/win/WinLaunch.cpp` | revealInExplorer | ✓ VERIFIED | `/select,"path"` quoted + native-normalized, FLAG_NO_UI, CancelledByUser |
| `src/app/main.cpp` | End-to-end wiring | ✓ VERIFIED | history+fileSearch pre-load construction, all 5 seams, explicit status ordinal switch, fileSearch context property, resultsReady→setFileResults, launch.setHistory |
| `qml/MainWindow.qml` | Status row, reveal branch, Add-exe row, dual pipeline | ✓ VERIFIED | onTextChanged dual call, Ctrl-only reveal branch, statusRow (WR-04 gate), emptyState gate, addExeRow, Theme-token-only |
| `qml/ResultsRow.qml` | Folder glyph | ✓ VERIFIED | `model.isFolder ? "\u25B8" : initial`, accentLight; selection-indicator bar (checkpoint note) |
| `tests/tst_search.cpp` | Pure-helper suites | ✓ VERIFIED | classifyCatalogStatus (all 7 values), isAllowedResult, buildWhereRestriction exact string |
| `tests/tst_filesearch.cpp` | Coordinator suites | ✓ VERIFIED | 13 slots incl. all 11 plan suites + WR-03/WR-05 additions, dedicated pool |
| `tests/tst_history.cpp` | INI round-trip suites | ✓ VERIFIED | 8 slots (7 plan + concurrentAccessThreadSafe_WR01) |
| `tests/tst_model.cpp` | Merge suites | ✓ VERIFIED | 9 plan slots + staleTextDropped_WR03 + generationGuardMonotonicAcrossRefresh_WR03 |
| `tests/tst_launch.cpp` | Launch policy suites | ✓ VERIFIED | 9 new slots (fileRowEnterNormal_LAUN02 … revealSnapshotFreeze_D12) |
| `CMakeLists.txt` | Sources + test targets registered | ✓ VERIFIED | WinSearchQuery.cpp/FileSearch.cpp/LaunchHistory.cpp in wisp_core; tst_search/tst_filesearch/tst_history targets in BUILD_TESTING |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| WinSearchQuery.cpp | ISearchQueryHelper | CoCreateInstance(CSearchManager) → GetCatalog → GetQueryHelper | ✓ WIRED | CLSCTX_INPROC\|LOCAL_SERVER combined (machine-verified fix) |
| WinSearchQuery.cpp | GenerateSQLFromUserQuery | only SQL source | ✓ WIRED | line 487; no concatenation of user input |
| WinSearchQuery.cpp | GetCatalogStatus | on-query probe | ✓ WIRED | checkIndexStatus → classifyCatalogStatus |
| FileSearch.cpp | QtConcurrent::run | worker lambda | ✓ WIRED | dedicated pool seam (WR-05) |
| FileSearch.cpp | m_generation | stale-drop both sides | ✓ WIRED | onFinished + ResultsModel::setFileResults |
| FileSearch.cpp | FuzzyMatcher::score | tracked-source name AND path match | ✓ WIRED | D-06/D-07 |
| LaunchController.cpp | LaunchHistory::recordLaunch | default reporter Launched branch | ✓ WIRED | D-10; main.cpp `launch.setHistory(&history)` |
| LaunchController.cpp | WinLaunch::revealInExplorer | default Revealer bridge | ✓ WIRED | revealSelected |
| WinLaunch.cpp | ShellExecuteExW | open verb + explorer /select quoted | ✓ WIRED | FLAG_NO_UI, no shell |
| main.cpp | ResultsModel::setFileResults | FileSearch::resultsReady connect | ✓ WIRED | generation + query text carried end-to-end |
| main.cpp | WinSearchQuery::queryFiles | QueryFn lambda | ✓ WIRED | exactly 1 call site (grep-verified) |
| MainWindow.qml | fileSearch.setQuery | onTextChanged | ✓ WIRED | dual-pipeline D-12 |
| MainWindow.qml | launchController.revealSelected | Ctrl-only branch | ✓ WIRED | LAUN-03, branch order preserved (elevated > reveal > normal) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| ResultsModel (file rows) | m_fileEntries | setFileResults ← FileSearch worker ← WinSearchQuery::queryFiles (real OLE DB SystemIndex) | Yes — real index rows; post-filtered to .exe/folders; live-verified smoke (2 notepad.exe hits, 30 folder rows) | ✓ FLOWING |
| FileSearch tracked source | trackedSource() | LaunchHistory::trackedExecutables ← real wisp.ini (QSettings) | Yes — real persisted paths | ✓ FLOWING |
| Status row | fileSearch.statusText | FileSearch::statusText (C++ single home) | Yes — verbatim locked copies | ✓ FLOWING |
| ResultsModel app rows | m_entries | AppCatalog worker (03) — unchanged | Yes — regression-verified (all 03 suites green) | ✓ FLOWING |
| Subtitle for file rows | entry.targetPath | index System.ItemPathDisplay | Yes — full path | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full test suite (13/13 incl. 5 phase-4 suites) | `ctest --test-dir build/dev --output-on-failure` (Qt bin on PATH) | 100% tests passed, 0 failed — tst_shell, tst_hotkey, tst_launcher, tst_capture, tst_matcher, tst_model, tst_enum, tst_catalog, tst_launch, tst_tray, tst_search, tst_filesearch, tst_history | ✓ PASS |
| Debounce/generation/status/tracked suites | ctest -R tst_filesearch | Passed (3.49s — wait-based assertions, WR-05) | ✓ PASS |
| INI round-trip + concurrency (WR-01) | ctest -R tst_history | Passed (1.86s) | ✓ PASS |
| Merge/cap/tier/stale suites | ctest -R tst_model | Passed | ✓ PASS |
| OLE DB helper contracts | ctest -R tst_search | Passed (0.06s) | ✓ PASS |
| Launch policy + reveal + D-05 | ctest -R tst_launch | Passed | ✓ PASS |
| Live desktop behavior (real index, real Explorer, real UAC-free launches, typing feel) | Not headless-runnable | Covered by phase 04-05 task 3 blocking human checkpoint — **approved 8/8 with note** (selection-indicator note implemented in 5e4b76e) | ✓ PASS (documented checkpoint) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| LAUN-02 | 04-01, 04-02, 04-03, 04-04, 04-05 | User can search files via the Windows Search index; Enter opens the file with its default app | ✓ SATISFIED | OLE DB query pipeline → FileSearch coordinator → merged model → `launchClassic` open verb for File rows (source != Uwp → open verb, D-05 normal); human checkpoint steps 1-2, 5, 8 approved |
| LAUN-03 | 04-03, 04-05 | User can open the containing folder of a file result with Ctrl+Enter | ✓ SATISFIED | `revealInExplorer` (`explorer.exe /select,"path"`) + `revealSelected` (Source::File gate) + Ctrl-only branch in `launchFromKey`; human checkpoint step 3 approved |

Both phase requirement IDs (LAUN-02, LAUN-03) are accounted for. No orphaned requirements: REQUIREMENTS.md maps only these two to Phase 4, both are claimed by plans and both are satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| WinSearchQuery.cpp | 356-358 | IN-01: provider-allocated HROW array not freed (no `hrowsPtr != hrows.data()` check) | ℹ️ Info | Windows Search provider writes in place (WSOleDB sample pattern); leak only if provider allocates. Documented in 04-REVIEW as INFO; explicitly out of the 5-finding fix scope |
| LaunchHistory.cpp | 76-85 | IN-02: `trackedExecutables()` reconstructs entries without `isFolder` | ℹ️ Info | Launched folders lose the ▸ glyph visual (launch/reveal unaffected). Documented INFO, out of fix scope |
| LaunchHistory.cpp / main.cpp | 51 / 89-92 | IN-03: `addExecutable` accepts non-.exe paths (dialog filter advisory) | ℹ️ Info | User-intent-driven; undermines .exe-only contract but not an injection. Documented INFO, out of fix scope |
| WinLaunch.cpp | 120 | IN-04: `explorer.exe` by bare name | ℹ️ Info | CWD-hijack theoretical risk (lpDirectory=nullptr); documented INFO, out of fix scope |

No 🛑 blockers, no ⚠️ warnings found. All 5 review warnings (WR-01 data race, WR-02 OLE DB row bounds, WR-03 stale-text, WR-04 status-row overlap, WR-05 flaky tests) are FIXED and verified in the code (QMutex in LaunchHistory; DBSTATUS_S_OK + length-bounded reads; query-text end-to-end + monotonic generation; `count === 0` gate; dedicated pool + wait-based assertions) — commits 3ce32d0, 740e14c, 7bb2387, b202ab7, 1809d5a present in git log. The 4 INFO findings were consciously excluded from the fix scope by the review-fix iteration (documented `findings_in_scope: 5`); they are tracked in 04-REVIEW.md and are candidates for a future cleanup round, not phase gaps.

### Human Verification Required

No pending items. The phase's own design includes a **blocking human checkpoint** (04-05 task 3, `checkpoint:human-verify`, gate: blocking) which was **approved 8/8** covering: merged file rows, Enter launch, Ctrl+Enter Explorer reveal with file selected, Ctrl+Shift+Enter silent-normal (no UAC), folder ▸ glyph + open, Add executable… persistence to `%APPDATA%\TID\wisp\wisp.ini`, indexer-disabled status row via `Stop-Service WSearch` (apps still working), and typing feel (instant apps, ~150ms quiet fill-in, no stale rows). Approval evidence: 04-05-SUMMARY.md checkpoint section + the post-approval note commit `5e4b76e` (user-requested vibrant left selection-bar indicator). Live-desktop re-verification is optional per the maintainer's discretion.

### Gaps Summary

**No gaps found.** All 22 must-have truths verified against the actual codebase (not summary claims): debounce 150ms, generation guards on both sides (plus WR-03 query-text guard), empty-query apps-only, indexer status decision table, tracked-source merge, status copy single-homed, one interleaved score-desc list, 5-file cap, path-only tier 100, IsFolderRole, full-path subtitle, stale-drop model-side, launch tracking + add-executable persistence to INI, reveal-in-Explorer, D-05 silent-normal, Ctrl+Enter reveal branch, folder glyph, status row gating, Add-executable row. All grep gates from the 5 plans pass. Full ctest 13/13 green (Qt bin on PATH — the bare-shell `0xc0000135` failure is an environment PATH artifact, not a code failure). Commit history matches all claimed plan commits (971be1a…1809d5a). Both requirements (LAUN-02, LAUN-03) satisfied.

---

_Verified: 2026-08-10T02:32:24Z_
_Verifier: OpenCode (gsd-verifier)_
